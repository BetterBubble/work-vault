---
title: impl-modes-gate1-starttask
type: note
permalink: tacticum/00-board/impl-modes-gate1-starttask-1
tags:
- draft
archived-at: 2026-07-31 17:27
---

# impl-modes-gate1-starttask

status: draft
Воркер: implementer (lead-modes ТЗ#2, вариант «а»)
Worktree: /Users/bubblemac/tacticum-worktrees/modes-workflow · ветка feat/workflow-modes
Коммит: **a2ec93d** (`feat(iva-analysis-base): встроить 1-й гейт классификации режима в /start-task`)
Файл: /Users/bubblemac/tacticum-worktrees/modes-workflow/templates/iva-analysis-base/ingredients/commands/start-task.md

## Что вставлено
Блок «## Гейт классификации (выполняется ПЕРВЫМ, до хэндоффа)» — вставлен ПЕРЕД строкой `Hand off to the tacticum-workflow sub-agent with:`. Основа — §4 promposal (черновик промпта гейта), адаптирован под наши команды и §2 (порядок по приоритету сигналов) + §1.2 п.3 (состав роли). Ключевые фразы:

- **Шаг 0 — достаточность ТЗ:** пустое / одна строка на крупное / только ссылка-картинка → НЕ классифицировать, до 3 вопросов (что / где / как поймём готово), дождаться ответов.
- **Шаг 1 — классификация по приоритету** (первое совпадение выигрывает): РИСЕРЧ («выяснить/возможно ли/как устроено/шаги неизвестны/исследовать», плавающий дефект) → `/start-research`; СОСТАВНОЕ («+»/«и», два дефекта, «сначала…потом», inspect внутри кодовой, «A затем применить в B») → split A→B; РЕФАКТОРИНГ (migrate/замени X на Y/привести к, без смены поведения) → `/lite-task` (тип refactoring-S); ЛАЙТ (мелкая доработка без нового экрана/сценария, по макету, правка значения/конфига, feature-S) → `/lite-task` (тип feature-S); БАГФИКС (наблюдаемый дефект-реставрация в одном месте) → `/fix-bug`, **НЕ в /lite-task**; ИНАЧЕ → ПОЛНЫЙ конвейер (текущий путь).
- «Чему НЕ доверять»: тип тикета Jira, глагол заголовка, размер текста ТЗ; смотреть на ОБЪЕКТ.
- **Состав роли (§1.2 п.3):** предлагать только режимы с установленными лейнами; если лейна нет — «этот тип ведёт лейн X / роль Y — добавить лейн (re-provision) или передать роли Y», не деградировать молча.
- **Шаг 2 — предложение + ПОДТВЕРЖДЕНИЕ:** формат «Определил режим: … Признак: <цитата ТЗ>. Предлагаю: … Подтверди или выбери другой режим»; для составного — показать разбивку A/B; НИКОГДА не начинать Фазу 1 полного конвейера до подтверждения.

Маршрутизация согласована с proposal §1.2 (после фикса B1): bugfix→/fix-bug, refactoring-S/feature-S→/lite-task, research→/start-research, крупное→полный.

## Доказательство аддитивности
`git diff` файла — **чисто +50 строк, 0 удалений/изменений** существующего. Строка хэндоффа `Hand off to the tacticum-workflow sub-agent with:`, весь blockquote (ТЗ/Target directory/Phase 1/kb_discover/BRD, MOCKUPS, ADR, PIN, TESTS), frontmatter и Arguments — **байт-в-байт нетронуты**. Финальная фраза блока явно замыкает на прежний хэндофф: «Только если режим = ПОЛНЫЙ конвейер и пользователь подтвердил — продолжай хэндоффом ниже БЕЗ ИЗМЕНЕНИЙ (Фазы 1–2 конвейера остаются как есть)». Агент tacticum-workflow (Phase 1-2) НЕ тронут. Класс «полный конвейер» → тот же путь, что и сейчас.

## Mirror-проверка start-task
`_mirrors.yaml` пара `iva-analysis-base → iva-fr-analyst` содержит ингредиенты: api-contracts-discovery, design-system-discovery, fr-authoring, mockup-authoring, **start-feature**, update-feature. **start-task ОТСУТСТВУЕТ** в зеркалируемом наборе (там start-feature — другой ингредиент). → правил свободно, зеркало не задето.

## Тесты
`uv run pytest` (apps/backend), 3 файла: test_manifest_schemas.py + test_iva_role_presets.py + test_role_replacement_parity.py.
- ДО правки: **211 passed** (3.58s)
- ПОСЛЕ правки: **211 passed** (3.67s) — без регрессий (схемы валидируют JSON-манифесты, не тело markdown).

## ⚠️ Риски / для тимлида
1. **В worktree параллельно работает другой агент** (lite-mode): при старте дерево было clean, к концу появились НЕзакоммиченные правки в `templates/tacticum-lite-base/` (lite-task.md, SKILL.md, manifest.yaml). Я их НЕ трогал и НЕ коммитил — закоммичен строго один start-task.md. Тимлиду: свериться, что общий worktree — это ожидаемо (риск конфликта коммитов/стейта между воркерами).
2. **Точка семантики для owner-ревью:** proposal §2/§4 и Шаг 2 требуют подтверждения режима ДАЖЕ для полного конвейера («НИКОГДА не начинай Фазу 1 полного конвейера до подтверждения»). Это добавляет шаг подтверждения к текущему поведению /start-task (сейчас стартует сразу). Реализовал по букве ТЗ (гейт «никогда не молча»), пайплайн Фаз 1-2 не деградирован. Если owner хочет для однозначно-крупных задач авто-старт без подтверждения — это правка одной фразы, флажок для решения.
3. `/lite-task` и `/start-research` — режимы, которые гейт предлагает; их наличие в конкретной роли проверяется самим гейтом (блок «Состав роли»). Если лейнов нет — гейт не деградирует, а предлагает re-provision/передачу роли.

autonomy off — НЕ пушил, коммит локальный, без AI-подписей.