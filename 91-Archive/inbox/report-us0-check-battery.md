---
status: draft
role: lead-fr
topic: ТЗ#3 US#0 — батарея проверок перед push (fidelity + critic + git/controller)
repo: /Users/bubblemac/tacticum/tacticum-dev
worktree: /Users/bubblemac/tacticum/tacticum-dev-fr-contracts
branch: feat/fr-analyst-contracts @ 483dc0e
date: 2026-07-24
permalink: tacticum/00-board/report-us0-check-battery-1
archived-at: 2026-07-31 17:27
---

# US#0 — отчёт по батарее проверок (запрос президента через ГД)

Пуш НЕ выполнен — жду OK ГД. 3 блока по запросу.

## Блок 1 — FIDELITY к ТЗ Солонко

**US#0 vs ТЗ.** ТЗ Солонко описывает US#1–5 (доработку скиллов), но **не** содержит US#0 — это ENABLING-шаг, возникший из разведки (mirror-протокол) и апрувленный президентом (вариант Б). US#0 не отклоняется от интента ТЗ, а делает его физически исполнимым (ТЗ предполагает правки в `iva-fr-analyst`; mirror-байт-паритет это блокировал). Вариант Б реализован без отклонений: правки только в общих файлах + сторона зеркала, owner `iva-analysis-base` (Diaret) не тронут.

**Явный список отклонений от буквы (и причины):**
1. **Счётчик 61→59, не 61→58** (off-by-one фикс). Прежний ADR писал «61» при фактических **62** (проверено `check_mirror_sync.py`). 62−3=59. Доко-ошибка чинится внутри этой правки; в описании PR оговорено — чтобы ревьюер не спотыкнулся о «−2 вместо −3». Достоверность: число в ADR = вывод тула = grep по HEAD = 59.
2. **`update-feature` депрекирован СЕЙЧАС** (не отложен на US#5). Решение ГД 15:39: убрать все три сразу, чтобы не возвращаться к зеркалу; `update-feature` правит US#5 (тоже ТЗ#3), поэтому расхождение оправдано заранее.
3. **US#1/US#3 сужены реконсиляцией** (принцип президента: опираться на актуальный код). Уже реализовано и НЕ переделываем: двухчастная структура FR (0.1.5), разделы «Доменные модели»/«События EV-nn» в ТЗ-скелете (0.1.9), правило честности §2. Это верность ИНТЕНТУ ТЗ через факт-состояние кода, а не отступление от него.

**Покрытие ТЗ планом US** (ничего не выпало): US#1=ТЗ §2 (структура FR v2), US#2=ТЗ §3 (формат 3.1/CT-n), US#3=ТЗ §2.3+§3 (DM/EV), US#4=ТЗ §4 (конвейер, парная), US#5=ТЗ §5 (/update-feature). Couplings выделены: US#1↔US#2 (§1.6 + правила честности), US#1↔US#4 (переезд FT/UC ↔ чтение конвейером по маркеру). **Вывод блока 1: fidelity соблюдена, отклонения только перечисленные, каждое обосновано и задокументировано.**

## Блок 2 — CRITIC (по ADR-0059 + депрекация + US-разбивка) → вердикт `critic-us0-adr-depreciation`
**Вердикт critic: технически корректно и верно интенту ТЗ (осознанное расхождение, не депрекация; owner нетронут; цифра 59 верна), НО НЕ готово к push как есть — 3 пункта закрыть:**

1. **БЛОКЕР (дёшево):** frozen-owner исключение зафиксировано в CHANGELOG/ADR/PR, но НЕ в тексте НОРМЫ `_mirrors.yaml` L9-11 («объясни в CHANGELOG владельца»). Следующий агент при заморож. владельце либо переизобретёт исключение, либо полезет писать в CHANGELOG прода Diaret (запрещено). → внести оговорку в L9-11: «если владелец заморожен — запись в CHANGELOG зеркала». Единственный блокер по существу.
2. **РИСК (подтвердить чтением):** оставшиеся 3 зеркала (design-system-discovery/mockup-authoring/start-feature) могут потребовать правок под US#1 (ТЗ §1.9 UI, §2.4 единый контракт анализатора, TOC/переномерация) — тогда US#0-скоуп расширяется (их тоже выводить из зеркала). План пометил §1.9 «без изменений», но не подтверждено чтением тел навыков.
3. **ГИГИЕНА:** off-by-one оговорить в PR не в ADR (✅ уже так); реконсилировать план 58→59 (✅ поправлено); убрать стейл-строку US#5 про удаление update-feature (✅ поправлено).
+ низкоприоритетные: не названы couplings US#1↔US#3 (§2.4) и dual-read US#4; §2.5 жанр ТЗ не назначен — ✅ добавлено в план.

**Действия лида по вердикту:**
- Гигиена (п.3 + couplings) — ✅ **исправлено в плане** сразу (`plan-tz3-us-breakdown-detailed`).
- Риск (п.2) — ✅ **explorer подтвердил** (`explore-remaining-mirrors-us1-risk`): **design-system-discovery** (локатор `П.H`, SKILL.md:59) и **mockup-authoring** (`П.H у FR`, SKILL.md:60) ТРЕБУЮТ правок под US#1 (переезд UI в §1.9 + §2.4-контракт), объём малый; **start-feature** — НЕ требует (тонкая обёртка, остаётся зеркалом). → US#0-скоуп расширяется до **вывода 5 из 6** ингредиентов (добавить design-system-discovery + mockup-authoring), счётчик 62−5=**57**. ⚠️ Это меняет апрувленный президентом состав (было 3) → **флагнуто ГД, жду подтверждения 3→5 до амендмента**.
- Блокер (п.1) — ⏳ **implementer-амендмент** после подтверждения ГД (батчем: оговорка нормы L9-11 + вывод design-system-discovery/mockup-authoring) → повторный controller/тесты.

## Итог батареи
Блоки 1, 3 — PASS. Блок 2 (critic) — **условный PASS: 1 блокер + 1 риск закрыть до push.** US#0 в текущем виде **НЕ передаём президенту на push** — сначала закрываю блокер (норма L9-11) и разрешаю риск (3 зеркала). Гигиена уже закрыта. После фикса — повторный гейт → тогда ГД сверяет → президент.

## Блок 3 — GIT / CONTROLLER (переподтверждено свежим прогоном)

**Git-чистота:**
- `git status --short` → пусто (дерево чистое).
- `git log main..HEAD` → ровно 1 коммит `483dc0e feat(fr-analyst): decouple 3 ingredients from mirror pair (ТЗ#3 US#0)`.
- `git diff --stat main..HEAD` → 4 файла, +24/−5: `_mirrors.yaml` (−3), `docs/adr/0059-…` (+4/−1... §7), `iva-fr-analyst/CHANGELOG.md` (+20), `iva-fr-analyst/manifest.yaml` (версия).
- **AI-подписи:** в коммите — НЕТ; в диффе — НЕТ (grep по claude/generated/co-authored-by/anthropic пусто).
- **Секреты/мусор:** НЕТ (grep по .env/private key/password/secret/token/PAT/__pycache__/.DS_Store/.serena пусто).
- **Scope:** ровно заявленные 4 файла, ничего лишнего. owner `iva-analysis-base` — 0 файлов.

**Тесты (свежий прогон, uv-venv, не по отчёту):**
- `python scripts/check_mirror_sync.py` → «OK — 59 зеркальных ингредиентов в 6 парах синхронны.» (exit 0)
- `python scripts/check_profile_version_discipline.py --diff-against main` → «OK — 46 profile(s) clean.» (exit 0)
- `python -m pytest apps/backend/tests/catalog/test_role_replacement_parity.py --noconftest` → **79 passed** (3 тест-функции; `test_mirror_content_is_byte_identical` параметризован по 59 ингредиентам; блок replacement-parity зелёный).

**Controller-гейт** (ранее, независимый прогон): PASS по всем 6 пунктам → [[gate-us0-mirror-depreciation]]. **Вывод блока 3: git чист, 0 секретов/подписей, scope точный, все три энфорсера зелёные.**


## Связано
[[pr-us0-mirror-depreciation-ready]] · [[gate-us0-mirror-depreciation]] · [[impl-us0-mirror-depreciation]] · [[reconcile-tz3-vs-code-0110]]