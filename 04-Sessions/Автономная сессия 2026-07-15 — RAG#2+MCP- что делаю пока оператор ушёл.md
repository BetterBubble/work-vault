---
title: 'Автономная сессия 2026-07-15 — RAG#2+MCP: что делаю пока оператор ушёл'
type: note
permalink: tacticum/04-sessions/avtonomnaia-sessiia-2026-07-15-rag-2-mcp-chto-delaiu-poka-operator-ushiol
tags:
- autonomous
- rag2
- mcp
- rerank
- session
- helm
---

Оператор ушёл, работаю автономно как тимлид. Цель к возвращению: **готовый RAG#2 + analyst-MCP на проде, проверенный на ЧАСТИ данных** (данные доливаются параллельно) + **реранк подключён к RAG#1 и RAG#2**.

## Правило (оператор): коммить ВСЁ для синка
Любое изменение кода/конфига — **коммитить в ветку** (даже если на сервер тяну без пуша через bundle для скорости). Потом: push → PR → синк сервер↔локально↔github. Держать реестр изменений здесь.

## Текущее состояние (на момент ухода оператора)
- **Деплой готов:** `main c11a130` на helm (экстрактор+харнесс+pdf/docx+MCP), SEED=0, граф цел. Деплой через git-bundle (сервер не пуллит GitHub — deploy-key снят; НЕ добавляем ключ по решению оператора, тянем bundle).
- **Выгрузка идёт (detached):** `rag2-run-conf` (92 Confluence-спейса, продуктовые первыми → `iva_confluence__bge_m3_1024`), `rag2-run-jira` (14 проектов → `iva_jira__bge_m3_1024`). Порционно, resumable, per-контейнер валидация полноты, sleep 0.25с. E2E-смоук прошёл.
- **Туннель** `iva-sources-tunnel.service` (helm→adp_emb→10.22.0.10:443) поднят. Креды в `/opt/helm/_run/rag2.env` (Jira Basic monakhov-tech + Confluence token).
- **MCP** `/mcp/analyst` (8 тулов) задеплоен. C4/arch — в БД.
- **Реранк** `tacticum/rerank` (Qwen3-Reranker-4B) на Gateway работает, ключ control-tower имеет доступ.

## План автономно
1. **Реранк → RAG#1 + RAG#2:** helm-флаги `docs_rerank_enabled`/`assistant_rerank_enabled`/`rag2_rerank_enabled` (дефолт false) → включить (env HELM_*_RERANK_ENABLED=true), закоммитить изменение конфига в ветку, redeploy, проверить.
2. **Мониторю выгрузку** (счётчики коллекций растут, per-контейнер валидация, ловлю падения).
3. **Live-тест MCP на части данных** (analyst_search на залитых спейсах, arch_map/affected_systems на C4).
4. Всё меняющее — коммит в ветку(и) для последующего push+PR.

Связано: [[RAG#2 pipeline выгрузки+ингеста — канон (для реализации)]], [[RAG#2 analyst-MCP — канон дизайна (для реализации)]], [[Реранкер в LLM Gateway — сделано (DeepInfra Qwen3-Reranker-4B)]].
