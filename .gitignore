.DS_Store
.obsidian/workspace*
.obsidian/cache/
.basic-memory/
*.tmp

# MCP-серверы работы (project scope). В файле лежат токены доступа — в git он не идёт никогда:
# приватный репозиторий это не шифрование, и после ротации значение остаётся в истории.
.mcp.json
