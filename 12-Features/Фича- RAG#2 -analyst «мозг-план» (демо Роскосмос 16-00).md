---
title: 'Фича: RAG#2 /analyst «мозг-план» (демо Роскосмос 16:00)'
type: note
permalink: tacticum/12-features/ficha-rag-2-analyst-mozg-plan-demo-roskosmos-16-00
status: готово
created: 2026-07-22 12:05
updated: 2026-07-25 18:55
repo: helm
project: helm / RAG#2
autonomy: 'off'
deadline: 2026-07-22 16:00
branch: feat/rag2-analyst-sintez
tags:
- feature
- rag2
- demo
- roscosmos
- helm
- handoff
- analyst
---

# Фича: RAG#2 `/analyst` «мозг-план» — демо Роскосмос 16:00

**Статус:** TODO, ждёт лида (пользователь скажет лиду взять карточку с доски). **autonomy: off.**

## Зачем
Довести веб `/analyst` из цитируемого поиска до **синтезированного «мозг-плана»** (статус → сделано/в процессе/не начато → как сделать → кому задача → что уточнить) — продающий кейс для **Роскосмоса** (демо сегодня 16:00). Данные ИВА, tenant=iva.

## Что делать
Реализовать по **план-оф-рекорду**: [[plan-of-rekord-rag-2-analyst-mozg-plan-demo-roskosmos-16-00]] (там же техплан на реальном коде: [[tekhplan-rag-2-analyst-sintez-triva-na-api-rag2-answer]]).
- Ветка `feat/rag2-analyst-sintez` (от `main`).
- Новый `POST /api/rag2/answer` (надмножество `/context`; **`/context` НЕ трогать** — живой фолбэк).
- **Порядок A→UI→B→C→D. Cut:** A(синтез triva)+UI — обязательно; B(топология) — важно; C(Allure счётчики), D(docs-корпус `iva_docs__bge_m3_1024`) — stretch.
- Синтез — `TrivaLlm`/`GatewayClient.chat` (не-стрим); guardrail нет-hits→не зовём; **таймаут-guard `asyncio.wait_for ~25с`** (стекинг ретраев gateway); ошибка синтеза → `synthesis_failed`, фолбэк на context (HTTP 200).

## Гейты и границы
- **PR и деплой `/opt/helm` делает ПОЛЬЗОВАТЕЛЬ сам** — лид/воркеры до **controller-гейта**, не мержат/не деплоят.
- `/iva-docs` (RAG#1) не трогать.
- Тесты (C): только счётчики покрытия, **без «зелёных %»** (зелёные прогоны реально есть только у Звонков 40% и Почты 3/3).

## Воркеры (предложение лиду)
implementer (в git-worktree, ветка `feat/rag2-analyst-sintez`: A→UI→B→C→D) → verifier (прогон демо-вопросов на реальных данных `/analyst` + мини-нагрузка triva + acceptance) → controller-гейт перед показом пользователю.

## Критерии приёмки
См. план-оф-рекорд. Кратко: синтез с цитатами по 6 актам; топология в «как сделать»; тесты без ложных зелёных; отклонение (Акт 7) держит; triva стабильна (таймаут→фолбэк, не 5xx); tenant=iva проверен; `/iva-docs` жив.

## Демо-сценарий (реальные данные)
6 актов: SSO (killer: IVAONE-12438 «Заказчик РосКосмос AD+ALDpro») · Мессенджер/федерация · ВКС/MCU · Звонки/CS · Календарь+Диск · Почта+Terra · + живое отклонение. Детали и «чего избегать» — в план-оф-рекорде.

## Дальше
Пользователь докручивает сценарии Актов 3–6 с ГД параллельно исполнению.


---

## Ход работ (лид)

**2026-07-22 12:15 — СТАРТ (по слову пользователя).** autonomy off.
- Подготовлены два изолированных worktree от свежего `main 9571b1f`:
  - backend — `helm-wt-rag2-analyst-be`, ветка `feat/rag2-analyst-sintez`
  - frontend — `helm-wt-rag2-analyst-ui`, ветка `feat/rag2-analyst-sintez-ui`
- Распараллелено правильно: backend (`src/helm/**`) и frontend (`web/**`) — непересекающиеся файлы, контракт `/api/rag2/answer` зафиксирован заранее → в конце чистый мерж ветки UI в плановую.
- Запущены два implementer'а параллельно:
  - **backend**: A(синтез triva на новом `POST /api/rag2/answer`, `/context` не трогаем) → B(топология) → C(Allure счётчики) → D(docs-корпус). Cut-линия: A обязательна.
  - **frontend**: панель «мозг-плана» над цитатами + индикатор «Синтезирую…» + честный фолбэк на context (без регрессии).
- Дальше по плану: как оба вернут результат → мерж UI-ветки в плановую → **verifier** (прогон 6 демо-актов на реальных данных `/analyst` + мини-нагрузка triva + acceptance) → **controller-гейт** → сводка пользователю. PR+деплой `/opt/helm` — пользователь сам.

---

## Доработки v2 (2026-07-22 12:37) — ОБЯЗАТЕЛЬНО К ИСПОЛНЕНИЮ
Полный список — в плане: [[plan-of-rekord-rag-2-analyst-mozg-plan-demo-roskosmos-16-00]] §«Доработки v2». Ключевое:
- **Порядок demo-critical: A(синтез triva `/api/rag2/answer`) + UI → B(топология) → Allure(через тул `requirement_tests`).** ⚠️ Бэкенд A не подтверждён, UI уже в ветке `feat/rag2-analyst-sintez-ui` → **свести A+UI в одну ветку**.
- **Инженерного фолбэка НЕТ** (синтез ляжет → показываем RAG#1 `/iva-docs` руками). **R2/конфиденциальность — убрано** (не делаем).
- **«Кому задача»** — детерминированно подмешивать `owner_name` из `arch_map`/`affected_systems` (иначе «уточнить»).
- **`as_of`** — показывать свежесть (данные актуальны на 22.07, дельта-ингест ежедневно), не «10.07».
- **D (docs-корпус)** — флаг вкл/выкл + замер качества ретрива, **ПОСЛЕ** того как A+UI+B работают (не демо-блокер).
- **ГАРДРЕЙЛ: не трогать другие направления/сервисы.** RAG#2/`/analyst` деплоим свободно (никто не держит).
- **Проверка на РЕАЛЬНОМ ПРОДЕ по нашему КАНОНУ — 6 актов демо** (демо-сценарий в плане + точные вопросы и «чего избегать»: [[demo-hero-features-iva]], [[demo-hero-features-iva-part2]]). Гонять на живом `/analyst`, сверять, что синтез выходит «красиво» — это критерий приёмки.
- Числа-честность: свести к консистентным (Звонки ≈41% = 26/63, не «40% из 72»).

Ранбук демо-дня и wow-моменты — в плане §«Доработки v2».


**2026-07-22 ~12:45 — билд готов, гейт GO (условный).**
- Backend (commit `4b3a7f3`+`0bb085d`): новый `POST /api/rag2/answer` — синтез «мозг-плана» triva над retrieval-контекстом (A), топология C4 в промпт+поле (B), счётчики покрытия Allure без «зелёных %» (C). D (docs-корпус) осознанно отрезан — риск сломать живой ретрив при минимуме пользы для демо. `/context` не тронут (живой фолбэк). Guardrail-first + таймаут-guard 25с + любая ошибка синтеза → HTTP 200 с фолбэком.
- Frontend (commit `3805187`): панель синтеза над цитатами, индикатор «Синтезирую план…», честный фолбэк на цитируемый поиск без регрессии.
- Мерж UI-ветки в плановую `feat/rag2-analyst-sintez` — чисто (файлы `web/` и `src/helm/` непересекающиеся). Объединённая ветка: 9 файлов, +712/−12.
- **Тесты: 13 passed** (7 новых: успех-синтез, guardrail-no-hits, ошибка/таймаут→failed+200, llm=None→как /context, 503/422) + 80 смежных. ruff/mypy clean.
- **Controller-гейт GO (условный):** гит-чистота / скоуп / достоверность-безопасность / целостность кода / frontend-фолбэк / память — все PASS. Отчёт [[gate-rag2-analyst-demo]].
- **Остаток (блокер ДЛЯ ДЕМО, не для деплоя):** live-acceptance на реальной triva не прогнан (юнит-тесты мокают triva — покрывают плумбинг/безопасность, не качество синтеза на живых данных). Гонится ПОСЛЕ деплоя пользователем: 6 демо-актов на tenant=iva + triva-нагрузка + `/context` не сломан. Бриф — [[verifier-brief-rag2-analyst-demo]].
- **Ждём: пользователь деплоит `/opt/helm`** (autonomy off) → затем verifier/лид прогоняет live.


**2026-07-22 ~12:50 — задеплоено на прод + live-прогон (реальные данные).**
- Деплой: `git pull` ff → `9d41fdb`, `SEED=0 bash scripts/deploy.sh` (образ пересобран, миграции no-op, сид пропущен — данные целы). Бэкап `/root/backup_helm_pre_rag2analyst_2026-07-22.sql` (81M). Нюанс инфры: docker-часть деплоя идёт под root (у `tacticum-deploy` нет доступа к docker-сокету; git — под `tacticum-deploy`). Verify: `/answer` 401 (жив), `/context` 401 (регресс ок), `/analyst` 200, логи чистые.
- **Синтез работает и качественный** (акт SSO через phk-токен): рубрики на месте, 5 цитат `[n]`, строго по контексту, анти-галлюцинации держат.
- 🔴 **БЛОКЕР ЛАТЕНТНОСТИ (live-числа):** SSO без топологии = 24.8с (впритык к guard 25с); Звонки с топологией = **58.5с → `synthesis_failed=true`** (пустой ответ). Топология B добавляет ~30с (тяжёлые arch_map/affected_systems без кэша, последовательно) → синтез не укладывается в `wait_for(25с)`.
- 🟡 `as_of=2026-07-10` (данные реально от 10.07, не 22.07 как в v2); `tests=null` для Звонков (ожидали ~40% — `requirement_tests` не сматчил).
- **Действие:** запущен implementer на ветке `feat/rag2-analyst-latency` (worktree `helm-wt-rag2-latency`): ускорить топологию <5с (кэш C4 + ограничение + под-таймаут 8с fail-soft), параллелить retrieval‖topology, `max_tokens` 900→550, `wait_for` 25→40с, диагностика tests/as_of. Цель — стабильно <25с без `synthesis_failed`. Затем редеплой + перемер на проде.


**2026-07-22 ~13:00 — уточнение причины латентности (по скрину UI + воспроизведению).** UI-вопрос «Что по SSO / IVA ID сделано…» → 32.4с, `synthesis_failed=true`, но `topology_len=158` (КОРОТКАЯ). Вывод: падение НЕ из-за топологии — виновата сама генерация triva на грани 25с + объёмный входной контекст промпта (полный render с 5 развёрнутыми цитатами). Фронт-фолбэк отработал корректно (показал поиск + бейдж «синтез недоступен», не 5xx). Рычаги (переданы implementer'у): **урезать вход промпта синтеза** (top-≤5 hits, тела ≤600-800 симв, суммарно ≤~4-5k) + `max_tokens 550` + **кэш 6 актов** (главный) + топология-саммари короткой + wait_for умеренно. Приоритет — кэш+прогрев (scripted-акты мгновенно, живая triva только Q7).


**2026-07-22 ~14:15 — демо-ГОТОВО (ядро), приёмка на реальном проде пройдена.**

**Крупный поворот дня:** vLLM triva (`10.0.196.12:8004`) ЛЁГ в контуре ИВА (Connection refused, проверено с adp_emb — не туннель, не наш код). Синтез невозможен через triva → **обход:** синтез RAG#2 переведён на gateway-модель. `smart`/`chat`/`flash` оказались reasoning (жрут токены на reasoning → обрубают ответ); рабочая — **`tacticum/cheap`** (не reasoning, полный синтез ~10с). Изоляция конфига (`HELM_RAG2_SYNTH_LLM_*`, фолбэк на `iva_llm_*`) — RAG#1 не затронут. Env временный, снять когда triva поднимут.

**Задеплоено на прод (main, обход в .env):**
- Обход `cheap` — синтез полный на всех актах.
- Латентность-оптимизация (кэш+прогрев+компактный вход+короткая топология+параллель+max_tokens).
- Чипы-пресеты — 10 ЭТАЛОННЫХ вопросов run-of-show (Диск/Календарь разведены).
- tests-матч фикс (снят `IVAONE-%` фильтр в allure-джобе) + ре-ингест снапшота → Звонки-тесты подтянулись (96 ТК).

**Приёмка 10 актов на живом `/analyst` (реальные данные, tenant=iva):**
- Синтез `failed=false` на ВСЕХ 10; топология на всех; цитаты 5 везде.
- Тесты 6/10 (SSO/Диск — ТК нет по эталону = норма; Почта — снапшот `by_feature=6` не покрыл, нюанс данных).
- Killer SSO ✅ («Андрияшин, заказчик РосКосмос AD+ALDpro»); Диск-архитектура ✅ (One Disk/DS/DLP); федерация (Медведев/160ТК) ✅.
- Регресс чист: `/context` 200, `/iva-docs` (RAG#1) 200, `/analyst` 200.
- Латентность без кэша: большинство ~10-13с, Звонки 28с, Почта 19с → закроется финальным прогревом.

**В работе (параллельно, отдельные ветки):** история диалога RAG#2 (`rag2_conversation_turn`, по паттерну RAG#1) + design-режим синтеза (вопросы «как добавить X», акт 1b ЕСИА, +`nearest_spec`/`related_tasks`).

**Осталось до финала:** свести conversation+design в main → финальный деплой (с миграцией conversation) → прогрев 10 актов (уберёт латентность) → финальная сверка по эталону. **Демо-ядро уже показываемо.**

**Открытый риск:** triva down — если поднимут, снять env-обход (вернуть на приватную модель контура); пока синтез идёт через gateway (тот же контур, что ретрив-эмбеддинги/реранк — конфиденциальность не меняется, осознанно с пользователем).


**2026-07-22 ~15:30 — DEMO-ФИНАЛ ЗАКРЫТ (всё проверено на живом проде).**
Задеплоен `feat/rag2-integration` (commit `778d2a7`, PR отправлен). Полный набор demo-фич работает:
- **Синтез** на всех 10 актах через обход `cheap` (triva down), кэш прогрет.
- **Design-режим** (проектные вопросы «как добавить X»): детектор `is_design_query` + `SYNTH_PROMPT_DESIGN` + `build_design_context` (`related_tasks`/`nearest_spec`/`api_registry_check`). ЕСИА (акт 1b) — design-рубрики + честное «API-ручки нет» ✅.
- **Память диалога**: стор `rag2_conversation_turn` (миграция накачена), первый запрос авто-генерит `conversation_id`, follow-up понимает контекст (проверено: «кто отвечает?» → про SSO) ✅. Кэш пресетов не затронут.
- **Frontend**: лента вопрос-ответ + follow-up + кнопка «Новый диалог» + кэш-обход пресетов.
- **Tests Звонков** (96 ТК), **чипы** 10 эталонных, регресс чист.

**Готово отдельными ветками (не в demo-деплое):**
- `feat/rag2-docs-corpus` (`da667ba`) — блок D (docs-корпус в ретрив), флаг `HELM_RAG2_DOCS_CORPUS_ENABLED` дефолт OFF. Замер по слову пользователя (риск дублей docs↔confluence/RRF; при ON подмешивается и в /context).
- `feat/rag2-synth-fallback` (`0ea9fde`) — цепочка primary→fallback (fast-fail на refused). Для возврата triva: primary `iva_llm_*`=triva, fallback `HELM_RAG2_SYNTH_FALLBACK_LLM_*`=cheap.

**Открытый риск:** triva down (env-обход в `.env`, снять при подъёме + активировать fallback). Перенос демо на контур Роскосмоса — оценить когда дадут URL (зависимости: Qdrant-корпус, embeddings/rerank, LLM; закрытый контур → нужна их LLM/перенос).


**2026-07-22 ~16:30 — triva ВЕРНУЛАСЬ, целевая архитектура активна и принята.**
Смержено в main (`feat/rag2-final2`): demo-финал + docs-корпус (D) + synth fallback-цепочка. Задеплоено, env перенастроен.
- **Синтез: primary=triva → fallback=cheap.** `iva_llm_*`=triva (обход-override `HELM_RAG2_SYNTH_LLM_*` закомментирован), `HELM_RAG2_SYNTH_FALLBACK_LLM_*`=gateway cheap. `HELM_RAG2_SYNTH_PRIMARY_TIMEOUT_S=45` (triva синтезирует ~20-23с — 12с было мало, уходило на fallback).
- **Приёмка на triva (10 актов):** все `failed=false`, ~20-23с, **primary(triva)=11, fallback(cheap)=0** — triva стабильна, fallback ни разу не понадобился. Решение пользователя: 20-40с на triva ок, качество важнее (triva технически конкретнее — keytab/SPN/nginx).
- **История диалога на triva:** авто conversation_id + follow-up понимает контекст (проверено). ✅
- **Design на triva (ЕСИА 1b):** design-рубрики (Куда встраивать/Что затронет/Прецедент/Кому/Что уточнить). ✅
- **Docs-корпус (D): ON, оставлен** (решение пользователя — не портит). Ретрив бьёт `iva_docs__bge_m3_1024` (8272 чанка, green); docs с весом 0.6+дедуп не топит confluence/jira — безопасно расширяет покрытие.
- Fallback-путь проверен: при `primary_timeout` мал/недоступности triva → авто-переход на cheap (лог «отработал fallback LLM»).

**ИТОГ: RAG#2 /analyst полностью готов на целевой конфигурации.** Все demo-фичи + история + design + D + triva-primary/cheap-fallback работают на живом проде. triva down больше не риск (fallback подстрахует автоматически).


**TODO ПОСЛЕ ДЕМО (git-гигиена, отложено по решению пользователя — не трогать прогретый кэш до показа):**
1. Выровнять прод `/opt/helm` на `main` (сейчас на `feat/rag2-final2` = тот же код, что main `b7dff4a`; git checkout main + pull + redeploy → сбросит прогретый демо-кэш, поэтому после демо + перепрогрев).
2. Почистить 14 временных rag2-worktrees (`helm-wt-rag2-*`) + их локальные ветки — вся работа влита в main (PR #90/#92/#93/#94), безопасно удалить.
3. Опц.: удалить запушенные feature-ветки на origin (`feat/rag2-*`) после подтверждения мержа.
Прод-состояние на момент демо: ветка `feat/rag2-final2` (`d5b4160`), кэш 10 актов прогрет на triva, обход-env снят (triva primary/cheap fallback), D ON.


**2026-07-23 — triva-канал ПОЧИНЕН (Лебедев) + primary переведён на gateway.**
Прямой vLLM `10.0.196.12:8004` умер (адрес от Сакевич). Лебедев (на `tei_service`, бэкапы `*.bak.20260722`): перекинул туннель `cifragen-triva-tunnel-1` на живой `10.0.196.14:9034`, в `litellm/config.yml` модель `openai/triva`, `.env` токен .14; пересоздал litellm+туннель; дал helm-ключу (control-tower) грант на `tacticum/triva`.
- **Итог:** triva доступна через gateway `llm.cifragen.ru/v1` model=`tacticum/triva` (проверено: 200, 0.44с, «Да»). БЕЗ стриминга (обёртка .14 не отдаёт SSE) — но нашему `/answer` (синтез целиком, не-стрим) это НЕ мешает.
- **Перенастроен RAG#2 synth на проде:** primary = gateway `tacticum/triva` (`HELM_RAG2_SYNTH_LLM_*`, раскомментировано, модель triva), fallback = gateway `tacticum/cheap`. Оба через gateway (надёжнее хрупкого прямого туннеля 8790). Синтез SSO: 17.5с, failed=false, лог «отработал primary LLM».
- **Открыто (к Сакевич, завтра):** постоянный ли адрес (.12:8004 vs .14:9034) + стриминг для потокового бота (не для /analyst). Стрим на стороне ИВА.


**2026-07-23 — режим после демо:** демо прошло. Полный прогрев кэша (10 актов) БОЛЬШЕ НЕ НУЖЕН. Достаточно лёгкой проверки работоспособности: `/answer` отвечает (1-2 запроса), лог primary(triva)/fallback(cheap), health `/analyst`+`/context`. Прогрев был только ради мгновенных сценарных актов на демо.


**2026-07-23 — ВЕСЬ helm переведён на Gateway (по решению пользователя).**
`iva_llm_*` (общий синтез: RAG#1 /docs, RAG#3 /assistant и т.д.) переключён с мёртвого прямого туннеля `8790→.12:8004` на Gateway `tacticum/triva` (→ .14:9034). Бэкап `/opt/helm/.env.bak.20260723-gwswitch` (хранит старые iva_llm значения для возврата на .12).
- **Итог:** весь синтез helm через Gateway `.14`. RAG#1 `/docs/ask` ОЖИЛ (был 502 на мёртвой triva → теперь 200, 7с). RAG#2 не сломан (primary triva, 13.8с). Прямой туннель 8790 больше не используется (systemd-юнит висит, upstream .12 мёртв — можно отключить позже).
- **Стриминг:** RAG#1-бот теперь получает ответ целиком (Gateway .14 не отдаёт SSE). Вернуть стрим — когда Сакевич поднимет .12:8004 (тогда RAG#1 → прямой канал из бэкапа, RAG#2 оставить на Gateway).


**2026-07-23 — новые креды triva от Сакевич (Лена) проверены — ОБА сценария работают.**
Дала прямой vLLM эндпоинт (не gateway): `BASE_URL=http://10.0.196.14:8004/v1`, `MODEL=/models/triva_llm_instruct`, свой ключ. Просьба: протестировать обычный запрос + стриминг, отписать. Фолбеки между `.12` и `.14` делать НЕ надо.
- Доступ: `.14:8004` открыт с **adp_emb** (с gateway закрыт). Тесты гоняли curl'ом с adp_emb.
- `GET /models` → 200, 0.08с. Отдаёт две модели: `/models/triva_llm_instruct` и `/models/triva_llm_instruct-backup`.
- **Обычный запрос** (`chat/completions`, non-stream) → **200, 0.21с**, ответ корректный («Москва»). vLLM 0.23.0. ✅
- **Стриминг** (`stream:true`) → **работает**: 20 SSE-чанков `data:`, инкрементальные `delta.content`, финальный `finish_reason:stop` + `data: [DONE]`. Текст собрался корректно. **Это первый triva-эндпоинт со стримингом** (gateway `.14:9034` и старый прямой `.12:8004` SSE не отдавали). ✅
- Нюанс: при стриминге ответ приходит от `/models/triva_llm_instruct-backup` (эндпоинт роутит на backup-реплику) — на работу не влияет, но отметить.
- **Не меняли конфиг helm** — это проверка кредов по просьбе Сакевич, не переключение прода. RAG#2/RAG#1 остаются на Gateway `tacticum/triva` (→.14:9034). Если решим переводить потоковый бот на стриминг — вот рабочий прямой канал `.14:8004`.


**2026-07-23 (день) — Gateway triva переведён на новый живой адрес `.14:8004` (по слову пользователя). СДЕЛАНО + verify.**
Старый gateway-таргет triva `.14:9034` УМЕР (`Connection error`, у triva не было фолбэка → RAG#1 `/docs`/RAG#3 деградировали; RAG#2 спасался helm-side fallback на cheap). Перевели на утренние живые креды Сакевич (`10.0.196.14:8004`, model `/models/triva_llm_instruct`, ключ `sk-vXA…`).
- **Правки в `/opt/cifragen` (gateway-сервер), 3 файла (бэкапы `*.bak.20260723-triva14`):**
  1. `docker-compose.yml` — sidecar-туннель `triva-tunnel`: форвард `127.0.0.1:8790 → 10.0.196.14:9034` → **`:8004`**.
  2. `litellm/config.yml` — маршрут `tacticum/triva`: `model: "openai/triva"` → **`openai//models/triva_llm_instruct"`** (новый прямой vLLM строго проверяет id).
  3. `.env` — `IVA_TRIVA_API_KEY` → новый ключ. (api_base 8790 и env-ref ключа не меняли.)
- **Блокер, который вскрылся и пофикшен:** туннель поднимался, но джамп `194.36.208.242` (adp_emb) давал `administratively prohibited` — в `~/.ssh/authorized_keys` на adp_emb ключ туннеля `gateway->adp_emb triva tunnel` ограничен `permitopen` (было `.14:9034` + `.12:8004`, нового `.14:8004` НЕ было). **Добавили `permitopen="10.0.196.14:8004"`** (старые не трогали; бэкап `authorized_keys.bak.20260723-triva14`), рестарт туннель-контейнера — форвард встал.
- **Verify через gateway (`llm.cifragen.ru`, master key):** обычный ✅ («Париж»), **tool-calling ✅** (`finish=tool_calls`, `get_weather`), **СТРИМИНГ ✅** (13 SSE-чанков + `[DONE]`). helm-путь ✅ (helm-креды → `tacticum/triva` → 200). **Регресс:** `tacticum/cheap` (gemini) жив — другие тиры/ЗУ не задеты (пересоздавали только litellm+tunnel).
- **Бонус:** через Gateway triva теперь умеет и **function tools**, и **стриминг** (у мёртвого `.14:9034` и старого `.12:8004` этого не было). Прямой `.14:8004` тоже проверен на tools — `finish_reason: tool_calls` ✅.
- **Команда agents:** их тиры на Gateway (cheap/chat/smart/code/flash — gemini+deepseek) отвечают и **отдают tool_calls штатно** — для их проверок всё рабочее работает.
- ⚠️ **ОСТАЛОСЬ (не делали — ждёт решения пользователя):** у `tacticum/triva` в Gateway ВСЁ ЕЩЁ НЕТ фолбэка (`No fallback model group for tacticum/triva` — движок это прямо показал). Рекомендация: добавить в `router_settings.fallbacks` `{ "tacticum/triva": ["tacticum/cheap"] }` (не triva↔triva, запрет пользователя соблюдён). Запрет фолбэка между `.12`↔`.14` соблюдён — второй triva-адрес в запас не ставили.


**2026-07-23 (день) — Gateway: добавлен ФОЛБЭК `tacticum/triva → tacticum/cheap` (решение президента). СДЕЛАНО + доказано.**
Закрыт открытый пункт (у triva не было фолбэка). Теперь при падении triva ВСЕ её потребители (весь helm: RAG#1/#2/#3) прозрачно переключаются на gemini `cheap` и продолжают работать.
- Правка: `/opt/cifragen/litellm/config.yml` → `router_settings.fallbacks` добавлена `{ "tacticum/triva": ["tacticum/cheap"] }` (бэкап `config.yml.bak.20260723-trivafb`, YAML валиден). litellm пересоздан.
- **Доказано контролируемым тестом на проде:** остановил туннель triva → запрос `tacticum/triva` вернул **HTTP 200, ответ от cheap** (fp пустой, gemini) вместо прежнего `500 Connection error`; вернул туннель → triva снова сама (`fp=vllm-0.23.0`). ✅
- **Операционный нюанс (важно на будущее):** после `docker compose up -d --force-recreate litellm` туннель `triva-tunnel` надо **пересоздавать** (`up --force-recreate triva-tunnel`), НЕ `docker restart` — он делит netns litellm через `network_mode: service:litellm`, при простом restart порт 8790 не поднимается (`connect_ex 111`) и всё уходит на фолбэк. Правильно встало: `connect_ex 0`.
- Запрет фолбэка `.12`↔`.14` соблюдён — фолбэк на `cheap`, не на второй triva-адрес.
