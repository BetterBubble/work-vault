---
title: ctrl-816-final-2026-08-06
type: note
permalink: tacticum/00-board/ctrl-816-final-2026-08-06
---

# Финальный гейт: Taiga #816 — CI-комментарий со ссылкой на коммит

Дерево `/Users/bubblemac/tacticum-worktrees/bl-816` · ветка `ci/816-taiga-commit-link` · контролёр `ctrl-816`, 2026-08-06.
Ничего не правил, ветку не переключал, не пушил, не мержил. В живую Taiga не отправлено ни одного комментария — только чтение.

> **HEAD, на котором вынесен вердикт, — `d5aa59f4` (9 коммитов).** Лид просил гейт на `0fb705c7`; пока шла проверка, ветка ушла вперёд ещё на один коммит. Прогон — на изолированном срезе `git archive HEAD`. Числа лида (`2124`, 126 тестов) верны для `0fb705c7`; на фактическом HEAD они другие, и это не расхождение, а движение ветки.
>
> **Дифф — от точки ветвления (`origin/main...HEAD`, три точки).** `origin/main` ушёл вперёд на 20 коммитов после ветвления `78a7031a`, поэтому двухточечный дифф показал бы 119 файлов и 8287 удалений — это изменения main, а не ветки.

## 1. Скоуп и чистота — прошло

`git diff --name-only origin/main...HEAD` — **5 файлов, +2117 строк, ни одного удаления**:
`.github/workflows/backend-ci.yml` · `.github/workflows/taiga-commit-link.yml` · `.gitignore` · `apps/backend/tests/test_taiga_commit_link.py` · `scripts/taiga_commit_link.py`

- **Голдены — 0 файлов** в диффе ветки (в каталоге 47).
- **Чужих файлов #750 нет ни в одном из 9 коммитов** — `skill_spec.py`, `test_manifest_schemas.py`, `ingredient.v1.schema.json` в дифф ветки не входят. **Условие отдачи подтверждаю явно.**
- **Дерево чистое** — `git status --porcelain` пуст; чужие файлы на диске откачены и совпадают с HEAD. Инцидент с двумя воркерами в одном дереве закрыт: `git -C <путь> checkout -- <файлы>` сработал там, где спотыкалась составная команда с `cd`. Это не обход классификатора, а другая форма вызова.
- Секретов нет: `phk_`, `ghp_`, `github_pat_`, `sk-`, `xox`, `AKIA`, `BEGIN` — **0** в диффе и в истории ветки. AI-подписей (строгая проверка футеров) — **0**.

**Чистоту дерева проверил и в конце прогона, а не только на старте** — своё же правило из прошлого отчёта. На старте чисто, в конце чисто.

## 2. Числа — перемерены на срезе `d5aa59f4`

| | Значение |
|---|---|
| полный сьют (команда ровно как в CI) | **2131 / failures 0 / errors 0 / skipped 43**, exit 0 |
| тестов в `test_taiga_commit_link.py` | **133**, падений 0 |
| база без файла задачи | **1998** (1998 + 133 = 2131) |
| `ruff check .` | All checks passed! |
| `ruff check` по скрипту | All checks passed! |
| `ruff format --check` по скрипту | 1 file already formatted |
| `ruff format --check .` | **516 files already formatted** |
| `mypy src` | **Success: no issues found in 222 source files** |
| `mypy` по скрипту | Success: no issues found in 1 source file |
| `uv lock --check` | Resolved 112 packages |

Заявленные лидом `2124` и 126 тестов сходятся с той же базой 1998 для `0fb705c7`; девятый коммит добавил ещё 7 тестов. **Все семь гейтовых команд прогнаны дословно в том виде, в каком стоят в `backend-ci.yml`, и все проходят** — то есть условие Президента «в конце все гейты должны пройти» выполнено, и проверено исполнением, а не чтением конфига.

Фильтр `paths` у `backend-ci` теперь включает `scripts/**` и для `pull_request`, и для `push`; фильтр стоит на уровне workflow, поэтому правка одного скрипта поднимает **все три джоба**, включая `tests` со 133 тестами. Обоснование поимённого списка вместо каталога подтверждаю замером: `ruff check ../../scripts/` — **27 ошибок**, `mypy ../../scripts/` — **23 ошибки в 7 файлах** из 8.

## 3. Показательный случай — печатается по-настоящему

Прогнал не тест, а сам скрипт на `cd0ddfc7~1..cd0ddfc7`:

```
  cd0ddfc7 → [817, 818, 819] · fix(dogfood-followup): корневые фиксы по ревью PR #226 (#810/#811/#770/#769) (#229)
  пропущено (не Taiga-форма): ревью PR #226 в cd0ddfc7
::notice::пропущено (не Taiga-форма): ревью PR #226 в cd0ddfc7
::notice::пропущено (не Taiga-форма): ревью #226 в cd0ddfc7
::warning title=Taiga::TAIGA_TOKEN не задан — 3 задач(и) остались без ссылки на коммит
```

Уведомление реально печатается, ссылки при этом не теряются (817/818/819 берутся из тела). Новый словарь подсказок проверен: `ревью PR #226`, `review #250`, `эпик E-TASKTEL #782` (вставка между словом и номером), `эпик #762`, `task #12` — ловятся; `tracked in #609`, `под #443/#444` — нет, как и заявлено. Форма `fix(#816):` даёт ref 816.

## 4. Парсер и три блокера — подтверждены ранее, в силе

Проверено на срезе `627d63da`, девятый коммит парсер ссылок не менял (тронут только словарь подсказок — диагностика, не отправка):

- **Ложных срабатываний ноль — подтверждаю независимым разбором.** 149 уникальных ref по всей истории; 136 разрешаются в живые issue/US проекта 12; 13 не разрешаются — и все 13 порождены канонической явной формой (`US #69`, `Taiga #234`, `Epic #476, US #492`), задач просто нет в трекере. Парсер не выдумал ни одного номера.
- **20 номеров, приобретённых формой `issue #N`** — список совпал посимвольно, **все 20 живые issues проекта 12**.
- **B1 (неотличимый провал)** — проверен собственным стендом: клиент, отвечающий на `add_comment` формой успеха при пустой истории, получает отказ «ответ принят, но записи в истории нет». Лучший узел ветки.
- **B2 (предковость)** — механизм верен; масштаб в отчёте занижен и приписан не той ветке: `chore/wave-a-sync-main` даёт 18–25 коммитов, а реальный худший случай по репозиторию — **562 коммита / 105 задач**.
- **B3 (потолок 25)** — прогон по всей истории: exit 1, `::error`, отправок 0.

## 5. Находки

**5.1. В теле коммита `627d63da` — карта личного окружения. Единственное, что стоит поправить ДО пуша.**

> `(streamable-http), не /mcp/sse из ~/tacticum/.mcp.json; рабочий ключ — у сервера`
> `iva-write в ~/.claude.json, ключ из старого .mcp.json отдаёт 401.`

и отдельной строкой — `«(независимая находка обоих критиков)»`.

Значений секретов нет. Но по правилу рабочего контура в репозиторий заказчика не уходят карты доступов и следы агентской работы, а здесь именно они: где лежит рабочий ключ, под каким именем сервера, какой файл отдаёт 401. Уедет на GitHub с первым пушем; **после мержа тело коммита уже не переписать**. До пуша правится дёшево.

**5.2. Тест на канон ловит удаление строки, но не разворот смысла.**
`test_canon_line_still_says_github_issues_are_unused` проверяет наличие двух подстрок — `"GitHub Issues"` и `"не используются"` — **где угодно в файле**. Проверил все три состояния: удалил формулировку → **упал** (`AssertionError`, строка 1215); вернул → **прошёл**; развернул канон на «GitHub Issues … ТЕПЕРЬ ИСПОЛЬЗУЮТСЯ», оставив «не используются» в другой строке файла, → **прошёл**. От самого вероятного способа смены канона защиты нет. Лечится сверкой предложения целиком.

**5.3. Аннотации дедуплицируются по фразе, а не по номеру.** Один номер, упомянутый в разных формулировках, даёт несколько `::notice::`. На показательном коммите — два уведомления про #226; на синтетическом примере (`по ревью PR #226` / `ревью #226` / `PR #226`) — **три на один номер**. Это тот же участок «громкости», который ветка старательно чинила: шум снижает сигнал. Правится дедупликацией по номеру с выбором самой длинной фразы.

**5.4. Замер в докстринге не воспроизводится.** Заявлено «187 неразобранных номеров, из них 61 — хвост `(#241)` и 114 — проза». Прогнал четыре разумных определения; ближайшее (всё тело, уникальные пары коммит-номер, корпус `origin/main`) даёт **207 / 53 / 109**. Порядок величин и сам вывод — «гнаться за этим хвостом словарём бессмысленно» — **подтверждаются**; точные три числа в докстринге — нет. Дефект документации, не поведения.

**5.5. Ветка отстала от `origin/main` на 20 коммитов.** Конфликтов по пяти файлам не ожидается (четыре новых), но CI на PR пойдёт по слитому состоянию, а не по тому, что я мерил.

**5.6. Ветка правилась во время гейта — четвёртый раз подряд.** Просили гейт на `0fb705c7`, фактический HEAD `d5aa59f4`. На этот раз всё своё, дерево чистое, чужого нет.

## 6. Ответ на вопрос Президента

**«Готова ли ветка технически к тому, чтобы после добавления секрета `TAIGA_TOKEN` всё заработало».**

**Да.** Контракт MCP подтверждён живьём: имена и схемы аргументов всех шести инструментов сверены со схемами сервера, резолв ref→id работает в обоих пространствах, `resource="userstory"` живой. Разбор не даёт ложных срабатываний на 913 коммитах. Все гейты качества проходят и реально запускаются на правку скрипта. Аномальный диапазон упирается в потолок до всякой сети. Ложный «успех» отправки невозможен: запись подтверждается чтением истории.

Не проверено по построению одно — **боевой `add_comment`**: в трекер до мержа не пишем. Остаток минимизирован правильно: при сомнении B1 ошибается в строгую сторону (аннотация «не доставлен»), а не в тихую.

Перед пушем стоит убрать §5.1 из тела коммита — это единственное, что после мержа уже не исправить.

---

ВЕРДИКТ: частично
Проверено: на срезе `d5aa59f4` — скоуп от точки ветвления, голдены, отсутствие файлов #750 во всех 9 коммитах, чистота дерева на старте И в конце; секреты и AI-подписи; полный сьют и все семь гейтовых команд дословно из `backend-ci.yml`; фактический запуск гейтов на правку скрипта и обоснование поимённого списка; печать `::notice::` реальным прогоном скрипта, а не утверждением теста; новый словарь подсказок на семи формах; замер из докстринга по четырём определениям. На срезе `627d63da` (парсер ссылок с тех пор не менялся) — разбор всей истории со сверкой каждого номера по живому трекеру, список 20 приобретённых номеров, B1 собственным стендом, B2 замером по всем веткам репозитория, B3 прогоном до отказа, тест на канон в трёх состояниях.
Данные: дифф 5 файлов / +2117 строк / 0 удалений · голденов в диффе 0 из 47 · чужих файлов #750 в коммитах 0, дерево чистое · секретов 0 · AI-подписей 0 · полный сьют **2131 / 0 / 0 / 43**, exit 0 · тестов в файле **133**, падений 0 · база 1998 · ruff периметр passed, ruff скрипт passed, format 516, mypy src 222, mypy скрипт 1, uv lock 112 пакетов · долг каталога 27 ruff / 23 mypy в 7 файлах · разбор истории 913 коммитов / 183 со ссылками / 149 задач · из 149 ref разрешаются 136, не разрешаются 13 — **все 13 из канонических форм, выдуманных парсером ноль** · 20 приобретённых ref все живые · B2 худший случай 562 коммита / 105 задач · B3 exit 1, отправок 0 · дедуп подсказок: до 3 аннотаций на один номер · замер докстринга 187/61/114 → перемерено 207/53/109 · ветка отстала от main на 20 коммитов.
Подтверждение: `gate-check.sh` → «НАРУШЕНИЙ НЕТ» · `git archive HEAD` → срез `d5aa59f4`, на нём `pytest tests -q -p no:randomly --ignore=tests/e2e_install --junitxml` → exit 0, `tests=2131 failures=0 errors=0 skipped=43` · `ruff check .` → «All checks passed!» · `ruff format --check .` → «516 files already formatted» · `mypy src` → «Success: no issues found in 222 source files» · `ruff check --config pyproject.toml ../../scripts/taiga_commit_link.py` → «All checks passed!» · `mypy ../../scripts/taiga_commit_link.py` → «Success: no issues found in 1 source file» · `ruff check ../../scripts/` → «Found 27 errors», `mypy ../../scripts/` → «Found 23 errors in 7 files» · прогон скрипта на `cd0ddfc7~1..cd0ddfc7` → «::notice::пропущено (не Taiga-форма): ревью PR #226 в cd0ddfc7» · сверка 149 ref через `list_issues`/`list_user_stories`/`list_epics` проекта 12 → 136 разрешаются · стенд B1 → «ответ принят, но записи в истории нет — считаем отправку несостоявшейся» · сухой прогон без `--max-comments` → EXIT=1, «::error title=Taiga::задач к комментированию 149 при потолке 25 … не отправлено ничего» · изменение строки канона → тест упал (`test_taiga_commit_link.py:1215`), возврат → прошёл, разворот смысла → **прошёл** · `git status --porcelain` в конце прогона → пусто. Дата 2026-08-06.
НЕ проверено: (1) **боевой `add_comment`** — по построению, в трекер до мержа не пишем; остаток минимизирован B1 (ошибка в строгую сторону, а не в тихую). (2) **Запуск workflow на раннере GitHub** — ветка не пушена, секрета `TAIGA_TOKEN` в репозитории нет; шелл-логика проверена локальным прогоном, а не Actions. (3) **Поведение после слияния с ушедшим вперёд `origin/main`** — мерил на срезе ветки, CI на PR пойдёт по слитому состоянию (§5.5). (4) **Разбор ссылок на срезе `d5aa59f4`** — переносил результат с `627d63da`, убедившись диффом, что девятый коммит тронул только словарь подсказок (диагностику), а не `extract_refs`; полного повторного разбора истории на новом срезе не делал. (5) **Состояние ветки после 19:35** — правится параллельно с гейтом (§5.6). (6) Наличие секрета в настройках репозитория на GitHub — доступа нет.