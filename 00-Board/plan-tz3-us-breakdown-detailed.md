---
status: draft
role: lead-fr
topic: ТЗ#3 — детальный план US#0–3 по фактическому дельта-скоупу
repo: /Users/bubblemac/tacticum/tacticum-dev
date: 2026-07-24
permalink: tacticum/00-board/plan-tz3-us-breakdown-detailed
---

# ТЗ#3 — детальный план US (по факт. коду 0.1.10, вариант Б апрувлен)

База: реконсиляция [[reconcile-tz3-vs-code-0110]] + механика [[explore-mirror-depreciation-mechanism]] + ТЗ Солонко. Ветка: `feat/fr-analyst-contracts`. autonomy off (PR/мерж — президент). Правки только в `iva-fr-analyst`, owner `iva-analysis-base` (Diaret) НЕ трогаем.

## US#0 — Разъединение mirror-пары (enabling, делается ПЕРВЫМ)
**Правки (точные):** *(ГД 15:39 — 3 ингредиента; расширено до 5 по вердикту critic + пред-одобрению ГД 16:25 — риск подтверждён explorer)*
1. `templates/_mirrors.yaml` первая пара — удалить **5** строк: `api-contracts-discovery`, `fr-authoring`, `update-feature` (правят US#1/#2/#5) + `design-system-discovery`, `mockup-authoring` (правят US#1 — переезд UI П.H→§1.9, подтверждено `explore-remaining-mirrors-us1-risk`). Остаётся под зеркалом РОВНО `start-feature` (тонкая обёртка, правок не требует). Счётчик 62−5=**57**.
2. `docs/adr/0059-...md` §7 (L65) — счётчик → **57** (⚠️ фактический baseline по `check_mirror_sync.py` = **62**, не 61: в старом ADR был off-by-one; 62−5=57, правка заодно чинит доко-ошибку — оговорить в описании PR, НЕ в теле ADR); абзац о первом осознанном расхождении ингредиентов пары iva-analysis-base↔iva-fr-analyst (5 ингредиентов), причина — **расхождение аудиторий** (base = обучение аналитиков as-is-честности §2 без ослаблений; fr-analyst = design-capable эволюция с релаксацией §2 в Части 1).
4. ⭐ **`templates/_mirrors.yaml` шапка L9-11 (замечание critic — БЛОКЕР):** внести в текст НОРМЫ оговорку «если владелец заморожен — запись в CHANGELOG зеркала» (иначе протокольная дыра у источника, который читают агенты).
3. CHANGELOG: шапка `_mirrors.yaml` L9-11 велит «объясни в CHANGELOG владельца», НО owner заморожен. **Решение лида:** пишем в CHANGELOG `iva-fr-analyst` (расходящаяся сторона) + причину в ADR; в PR явно отметить frozen-owner исключение. (Флаг ГД: подтвердить, что не пишем в CHANGELOG замороженного owner.)
**Приёмка:** `python scripts/check_mirror_sync.py` → green; `pytest test_role_replacement_parity.py` → green (блок A больше не байт-чекает 2 ингредиента; блок B не затронут). Прецедента нет — это первое применение правила шапки, PR документирует.
**Форма доставки:** предлагаю **отдельным малым PR** (протокол-правка ADR-0059 — чистый ревью президента), до кода US#1-3.

## US#1 — Структура FR v2 + оркестрация (`fr-authoring`) — СУЖЕНО
Не «создаём две части» (есть с 0.1.5) — переархитектурируем семантику.
1. **Релаксация §2 (ядро):** ввести в Часть 1 зону, где проектные wire-имена РАЗРЕШЕНЫ под 3 предохранителями (основание в Ч.2 + плашка «требует утверждения dev+CTO»/Q-n + валидатор). §2 в Части 2 остаётся строгим. Переписать §2/§12 «Правил честности» под двухзонную модель.
2. **Переезд FT-n/UC-n** из П.A/П.B (Приложение) в Часть 1 как формальные to-be разделы 1.4/1.5. Упразднить П.A/П.B/П.H (контент наверх).
3. **Механический валидатор границы:** детерминированная проверка на шаге «Интеграция» (ТЗ §2.4 п.3): в Ч.2 нет проектных имён; каждое wire-имя Ч.1 трассируется к основанию (П.F/П.E) либо в проектном разделе; нарушение = дефект, публикация стоп. Форма (чек-алгоритм в скилле vs вспом. скрипт) — на проработку implementer, предложить.
4. **`fr_skeleton: 2`** — маркер версии скелета в шапке страницы (сейчас версионирования нет).
5. **TOC-макрос** Confluence в начале Части 1. Переномерация разделов под ТЗ §2.3 (1.1-1.11).
**Приёмка:** FR нового скелета генерится; валидатор ловит проектное имя в Ч.2 и нетрассируемое имя в Ч.1; fr_skeleton:2 в шапке.

## US#2 — Контрактный раздел (`api-contracts-discovery`) — НОВОЕ
Сейчас — алгоритм разведки (5 шагов), формата/серии нет. Ввести:
1. **Формат 3.1:** реестр изменений (Контур|Контракт|Изменение|Назначение|Совместимость|**Потребители**) + по-операционные секции (таблицы параметров запроса/ответа, примеры JSON, коды ответа с семантикой).
2. **Серия CT-n**, трассировка каждой операции на CT-n/FT-n + пробу П.F.
3. Дополнения к образцу: права доступа/`403`, лимиты/`429`, асинхронность/`202`+статус-ресурс.
4. **JUMP-контур:** те же таблицы (имя команды вместо метода+пути, payload вместо body, Контур=JUMP), конвенции distrohost с цитатой.
5. **Чеклист полноты 3.2** (пункт неприменим → явно «не применимо»).
6. Порядок: контракт проектируется только после CT-n + проб П.F; альтернативы фиксируются в П.F.
**Приёмка:** контрактный раздел по формату 3.1 генерится для REST и JUMP; CT-n трассируются; чеклист отрабатывает.

## US#3 — Навыки DM-n / EV-n — СУЖЕНО (+ parity-развилка)
1. Создать 2 новых skill_spec: анализатор модели данных (DM-n) + событий (EV-n).
2. **Консолидация нумерации:** разделы ТЗ-скелета 7 «Доменные модели»/9 «События EV-nn» рефакторятся так, что их порождают новые навыки; серия ЕДИНАЯ (EV-n = EV-nn, унифицировать ID-формат). Не плодить параллельные серии.
3. Внести DM/EV в жанр FR как разделы §1.7/§1.8 Части 1.
4. ⚠️ **PARITY-РАЗВИЛКА (сигналю ГД перед правкой — зона test-matrix):** новые skill_spec в `iva-fr-analyst`, которых нет в owner, уронят `test_role_covers_replaced_profile` (пара `iva-role-analyst ← iva-fr-analyst`, allowlist `{}`). Чинится НЕ через `_mirrors.yaml`, а либо (а) строкой в `REPLACEMENTS` allowlist с причиной, либо (б) покрытием в лейне роли (owner заморожен → (б) невозможно). → вероятно (а); согласую с ГД до правки теста.
**Приёмка:** DM/EV навыки генерят разделы; EV-нумерация единая с ТЗ-скелетом; parity-тест зелёный (через согласованный allowlist).

## US#4 — Конвейер (dev-профили) — ОТДЕЛЬНАЯ ПАРНАЯ US, через ГД
К-1…К-5 на `iva-web-brownfield`/`iva-kmp-brownfield`/`iva-brownfield-mail` (сами зеркала). Пересечение с lead-ds по `iva-web-brownfield` — окно правки согласуем через ГД. Пока — не трогаю. Оформлю отдельной парной US, когда US#1-3 будут в форме.

## US#5 — /update-feature пер-раздельно + D-n — позже
Надстройка над существующей командой: перегенерация только затронутого раздела + фиксация утверждений проектных разделов (dev+CTO) как D-n. (`update-feature` уже выведен из mirror-пары в US#0 — возврата к зеркалу не требуется.)

## Couplings (полный список, вкл. замечания critic)
- **US#1↔US#2** — US#1 создаёт §1.6 + валидатор границы, US#2 наполняет §1.6 форматом 3.1; правила честности §2 обоих скиллов держать непротиворечивыми (critic на формулировки).
- **US#1↔US#3** *(добавлено по critic)* — US#1 создаёт слоты §1.7/§1.8 + вводит **единый контракт навыка-анализатора (ТЗ §2.4)** ОДИН РАЗ; US#2/US#3 его ПОТРЕБЛЯЮТ. Инвариант §2.4 определяется в US#1.
- **US#1↔US#4** — переезд FT/UC ломает чтение конвейером; по ТЗ §4 конвейер релизится **синхронно** со структурой. Coupling ОБЯЗАН гарантировать переходный **dual-read по `fr_skeleton`** (К-1/К-5), а не просто «конвейер читает новую структуру». Окно `iva-web-brownfield` с lead-ds — через ГД.
- **Жанр ТЗ (ТЗ §2.5)** *(уточнить по critic)* — применяется ли новая двухзонная модель к жанру ТЗ в US#1 или отдельной US? Унаследованная неоднозначность из ТЗ §5 (FR-центричного). Решить при старте US#1.

## Порядок исполнения
US#0 (enabling, малый PR) → US#1 + US#2 (ядро, параллельно разными implementer в одном worktree/ветке) → US#3 (после parity-согласования с ГД) → US#5 → US#4 (парная, координация lead-ds через ГД).

## Связано
[[План ТЗ-3 Аналитик — генерация требований к контрактам-модели данных — lead-fr]] · [[reconcile-tz3-vs-code-0110]] · [[explore-mirror-depreciation-mechanism]] · [[explore-fr-analyst-baseline]]
