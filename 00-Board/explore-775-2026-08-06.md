---
title: explore-775-2026-08-06
type: note
permalink: tacticum/00-board/explore-775-2026-08-06
status: draft
tags: [explore, tacticum-dev, us-775, lanes, ci]
---

# Разведка ветки `fix/775-dependents-repin-check` (US #775)

Репозиторий `/Users/bubblemac/tacticum/tacticum-dev`, ветка `origin/fix/775-dependents-repin-check`,
один коммит `6a66faf5` «Check dependents repin after base bumps» (автор Diaret, 05.08.2026).
Разведка велась в собственном worktree `probe-775` (detached HEAD), после работы снят;
ветка не изменялась, в Taiga ничего не писалось.

## 1. Что коммит реально делает

`git show --stat 6a66faf5`:

| Файл | Строк |
|---|---|
| `scripts/check_profile_version_discipline.py` | +132 |
| `apps/backend/tests/catalog/test_dependents_repin_check.py` | +127 (новый) |
| **итого** | **2 файла, +259, −0** |

Скрипт вырос с 320 до 452 строк. Добавлены:

- `find_unrepinned_dependents(bumped, manifests, exemptions)` — чистое ядро без git, юнит-тестируемо;
- `check_dependents_repinned(base_ref)` — git-обвязка (собирает манифесты HEAD, считает `bumped`
  из `_git_diff_files`/`_git_show`, вызывает ядро);
- `_repin_exemptions(base_id, head_version)` + `_changelog_entry_text` + регэксп `_REPIN_EXEMPT` —
  escape hatch: маркер `repin-exempt: dep-a, dep-b` или `repin-exempt: all` в CHANGELOG-записи
  ИМЕННО новой версии лейна (release-scoped, следующий бамп снова строгий);
- строка в `main()`: `diff_violations += check_dependents_repinned(args.diff_against)`.

**Тесты:** есть, 7 штук в `apps/backend/tests/catalog/test_dependents_repin_check.py` —
`test_pr213_reconstruction_is_caught`, `test_silent_when_dependents_bumped_together`,
`test_deprecated_dependent_is_skipped`, `test_repin_exempt_single_dependent`,
`test_repin_exempt_all_for_staged_rollout`, `test_new_profile_in_diff_does_not_violate`,
`test_repin_exempt_parsed_from_changelog_entry`.

**Подключение в CI:** отдельного шага не добавлено и он **не нужен** —
`.github/workflows/profile-version-discipline.yml` уже гоняет
`python scripts/check_profile_version_discipline.py --diff-against …` на `pull_request`
(база — `origin/<base_ref>`) и на `push` в `main` (база — `github.event.before`). Чек 4 висит
внутри той же ветки `if args.diff_against`, значит включается автоматически. Фильтр `paths:`
у workflow содержит `templates/**`, а бамп лейна всегда трогает `templates/**` → триггер сработает.
Проверено чтением workflow, не предположено.

## 2. Решает ли он постановку

Постановка прочитана дословно из Taiga (проект 12, US id `1575`, ref `775`, статус
`Ready for test`, исполнитель Дмитрий Лебедев, эпик #705 `[E-LANES]`). Сверка по критериям приёмки:

| Критерий из постановки | Статус | Чем подтверждено |
|---|---|---|
| Падает на реконструкции PR #213 (лейн забампан, роль нет), RED обязателен | **закрыт** | `test_pr213_reconstruction_is_caught` + живой прогон, п.5 |
| Молчит, когда роли забампаны вместе с лейном (состояние после PR #216) | **закрыт** | `test_silent_when_dependents_bumped_together` + прогон D, п.5 |
| Escape hatch работает и покрыт тестом | **закрыт** | 3 теста + сквозная проба через git, п.5 |
| `deprecated: true` потребителей пропускать | **закрыт** | `test_deprecated_dependent_is_skipped`; в каталоге 6 deprecated из 64 |
| Глубина 1 (ADR-0056), без рекурсии | **закрыт** | `find_unrepinned_dependents` строит `reverse` одним проходом, рекурсии нет |
| `tests/catalog`, `tests/e2e_install` — 0 failed, прогон по каталогу чист | **закрыт** | п.3 и п.5 |

Постановка предлагала маркер «в CHANGELOG-записи лейна **или** ключ в манифесте» — выбран
CHANGELOG-вариант, это внутри разрешённого. Реализация совпадает с псевдокодом постановки
буквально, включая место (`рядом с check_bump_needed_for_content_changes`) и отказ от отдельного
шага в CI. **Постановка закрыта полностью, отклонений нет.**

## 3. Работает ли он вообще (гейты на ветке как есть)

Ветка как есть, `6a66faf5`:

```
cd apps/backend
uv run --python 3.12 --extra dev python -m pytest tests -q -p no:randomly \
    --ignore=tests/e2e_install --junitxml=…
```
→ `tests="1898" failures="0" errors="0" skipped="43"` (**1855 passed**), код возврата **0**.

```
uv run --python 3.12 --extra dev ruff check .     → Found 318 errors
uv run --python 3.12 --extra dev mypy src         → Found 49 errors in 19 files (222 файла)
```

**Это не долг ветки, и в CI этих гейтов нет вообще.** Проверено:

- `mypy src` — коммит в `src/` не лезет ни одной строкой, значит все 49 преждесуществующие;
- `ruff` по двум изменённым файлам даёт ровно 4 ошибки (`S603`/`S607`, строки 107/108/132/133
  скрипта — `subprocess` в `_git_show`/`_git_diff_files`). Тот же файл из `origin/main`, прогнанный
  отдельно, даёт **те же 4 ошибки на тех же строках**. Новый тестовый файл — **0 ошибок**.
  Итого ветка добавляет **0 новых замечаний ruff**;
- в `.github/workflows/` три workflow, `ruff`/`mypy` не запускает ни один. `install-e2e.yml`
  гоняет `pytest tests/catalog` и `pytest tests/e2e_install`, `profile-version-discipline.yml` —
  скрипты. Числа 318/49 — общий долг репозитория, к мержу отношения не имеют.

Сам скрипт: `python scripts/check_profile_version_discipline.py` системным python падает на
`ModuleNotFoundError: No module named 'yaml'` — это про локальное окружение, не про код
(в CI ставится `pip install pyyaml`). Через `uv run` работает.

## 4. Насколько протух за отставание от main

Ветка отстаёт от `origin/main` на **99 коммитов** (не 71 — main уехал дальше), опережает на 1.

`git merge-tree --write-tree origin/main HEAD` → один OID дерева, код возврата 0.
Реальный `git merge --no-edit origin/main` в своём worktree: **0 конфликтов**,
`git diff --diff-filter=U` пуст. Причина понятна: коммит трогает два файла, один из которых
новый, а второй за 99 коммитов в `main` не менялся.

Гейты **на слитом состоянии** (`main` + ветка):

```
pytest tests --ignore=tests/e2e_install → tests="2005" failures="0" errors="0" skipped="43"
                                           (1962 passed), exit 0
pytest tests/e2e_install               → tests="217" failures="0" errors="0" skipped="0", exit 0
```

Все 7 новых тестов присутствуют в прогоне и зелёные (проверено по `--junitxml` поимённо).
Строка «N passed» не печаталась — числа взяты из junit-XML и кодов возврата.

**Утверждение в комментарии Taiga, что `tests/catalog` падает на Windows-EOL артефактах (#737),
на слитом состоянии не воспроизводится: 0 failed.** За 99 коммитов это, судя по всему, починили.

## 5. Ловит ли чек реальную проблему

### 5а. Ложных срабатываний на чистом каталоге нет

На слитом состоянии, чистое дерево:

```
python scripts/check_profile_version_discipline.py --diff-against origin/main
→ OK — 64 profile(s) clean.   exit 0
```

Пустой `bumped` на реальных 64 манифестах → 0 нарушений.

### 5б. Искусственная ситуация «лейн забампан, зависящая роль не перезаморожена» — ловится

В своём worktree сделан коммит: `tacticum-analysis-core` 0.4.1 → 0.4.2 (манифест + запись
в CHANGELOG), 11 зависимых ролей НЕ тронуты.

```
python scripts/check_profile_version_discipline.py --diff-against <base>
→ Profile version discipline violations (11):  exit 1
```

Ровно 11 нарушений по 11 живым зависимым: `firebird-role-web`, `iva-role-analyst`,
`iva-role-architect`, `iva-role-connect-ios`, `iva-role-go`, `iva-role-ios`, `iva-role-ivcs`,
`iva-role-java`, `iva-role-kmp`, `iva-role-one-mail-desktop`, `iva-role-web`. Текст нарушения
называет старую версию зависимого, механику («frozen `depends_on` edge … change never reaches
installations») и оба пути исправления.

### 5в. Escape hatch снимает нарушение — сквозная проба через git

Вторым коммитом в запись `## [0.4.2]` того же CHANGELOG добавлена строка `- repin-exempt: all`:

```
python scripts/check_profile_version_discipline.py --diff-against <base>
→ OK — 64 profile(s) clean.   exit 0
```

### 5г. Реконструкция боевого случая PR #213 на РЕАЛЬНОМ каталоге

Ядро `find_unrepinned_dependents` вызвано напрямую на 64 реальных манифестах:

| Сценарий | Нарушений |
|---|---|
| ничего не забампано | 0 |
| `development-core` + `analysis-core` 0.2.0→0.3.0, роли не тронуты (случай PR #213) | **20** (11 + 9) |
| то же + бамп всех 11 зависимых | 0 |
| то же + `repin-exempt: all` на `development-core` | 11 (сняты 9) |

Радиус поражения по каталогу (бамп каждого профиля поодиночке): нарушения даёт **32 профиля
из 64**. Крупнейшие: `tacticum-core-base` — 18 зависимых, `tacticum-analysis-core` /
`tacticum-bugfix-base` / `iva-write-base` — по 11, `tacticum-development-core` /
`tacticum-research-base` — по 9, `tacticum-lite-base` / `tacticum-ui-base` — по 8.

### 5д. Прогон по реальной истории — сколько уже смерженного чек бы завалил

Проигрыш чека по 60 merge-коммитам первой линии `origin/main` (база — `sha^1`, exemptions
пустые, так как маркеров в старых CHANGELOG нет):

**завалил 21, прошёл 23, манифесты не бампались 16.** Среди заваленных — PR #213
(20 нарушений), PR #169 (26), PR #202 (18), PR #198 (17), PR #190 (16), PR #185 (14).

Читать это надо не как «чек шумный», а как «класс дефекта массовый»: примерно половина
PR, трогавших версии лейнов, уезжала в каталог без перезаморозки зависимых. Это ровно то,
о чём стори, и подтверждает P0.

### 5е. Что покраснеет сразу после мержа

Проверены **все 176 открытых веток `origin`** (кроме `main`), каждая против своей merge-base.
Чек завалит **5 из 176**:

| Ветка | Бампов | Нарушений |
|---|---|---|
| `origin/chore/wave-a-sync-main` | 6 | 18 |
| `origin/feat/run-review-wave-a` | 6 | 18 |
| `origin/fix/review-opt-in` | 6 | 18 |
| `origin/fix/kb-navigation-installation-id` | 7 | 5 |
| `origin/feat/run-review-wave-b` | 2 | 1 |

Веток `bl-750` и `bl-816`, где сейчас работают другие агенты, в этом списке нет.

## 6. Чего не хватает до мержа

| Пункт | Есть? | Комментарий |
|---|---|---|
| Тесты | **да** | 7 юнит-тестов, включая RED-реконструкцию PR #213 |
| Подключение в CI | **да** | автоматом через `main()`; отдельный шаг не нужен, `paths:` фильтр покрывает |
| Прогон гейтов | **да** | 1962 + 217 passed на слитом состоянии, 0 failed |
| Конфликты с `main` | **нет** | мерж чистый |
| Корневой CHANGELOG | **не применимо** | в репозитории нет ни `CHANGELOG.md`, ни `docs/CHANGELOG.md` |
| Документация escape hatch | **НЕТ** | `repin-exempt` не упоминается **ни в одном `.md` репозитория**. Автор лейна узнает о нём только из текста нарушения (там путь описан). Место для строки — `.claude/skills/profile-authoring/SKILL.md` и `.agents/skills/profile-authoring/SKILL.md` (§ про фазу 3, где уже стоит `check_profile_version_discipline.py --diff-against github/main clean`) |
| Докстринг скрипта | **устарел** | `scripts/check_profile_version_discipline.py:4` — «Catches **three** failure modes», перечислены 1–3, четвёртой нет; блок `Usage` тоже про неё молчит |
| Шапка workflow | **устарела** | `.github/workflows/profile-version-discipline.yml:4` — «Catches the **three** failure modes» |
| `.serena/memories/task_completion_checklist.md` §4a | **не обновлён** | пункт учит «поменял контент → бампни версию + CHANGELOG», про «забампал лейн → перезаморозь зависимых» ни слова, хотя нарушение ссылается именно на §4a |
| Taiga | стори в `Ready for test` | закрыть после мержа |

Все четыре недостающих пункта — **текстовые, суммарно строк десять**, ни один не меняет поведение
кода и ни один не блокирует работу проверки.

## Прямой ответ на вопрос Президента

**Домерживается после вот такой доработки** — доработка чисто текстовая и её можно сделать
как отдельным коммитом в ту же ветку, так и следом за мержем:

1. Докстринг `scripts/check_profile_version_discipline.py`: «three» → «four», добавить пункт 4.
2. Шапка `.github/workflows/profile-version-discipline.yml`: то же.
3. Одна строка про `repin-exempt` в `profile-authoring/SKILL.md` (обе копии — `.claude/` и `.agents/`).
4. Абзац в `.serena/memories/task_completion_checklist.md` §4a про перезаморозку зависимых.

Код, тесты и проводка в CI доработки не требуют: конфликтов с `main` ноль, гейты на слитом
состоянии зелёные, чек ловит свой случай и не даёт ложных срабатываний на всех 64 профилях.

---

## ВЕРДИКТ

**Ветка технически готова к мержу как есть; до мержа не хватает только четырёх текстовых правок
документации (~10 строк), кода они не касаются.**

**Проверено**

- Состав коммита: 2 файла, +259/−0, скрипт 320→452 строки.
- Постановка US #775 прочитана дословно из Taiga (id 1575), все 6 критериев приёмки закрыты.
- Мерж `origin/main` в свой worktree: 0 конфликтов (`merge-tree` и реальный `merge`).
- Гейты на слитом состоянии: pytest 2005 тестов / 0 failed / 0 errors / 43 skipped;
  `tests/e2e_install` 217 / 0 failed. Оба exit 0.
- Ветка добавляет 0 новых замечаний `ruff`; `mypy src` коммитом не затронут.
- `ruff`/`mypy` в CI не запускаются вообще — числа 318/49 это преждесуществующий долг репозитория.
- Чек ловит искусственный дефект: бамп `analysis-core` без ролей → 11 нарушений, exit 1.
- Escape hatch снимает нарушение сквозным путём через git: → `OK — 64 profile(s) clean`, exit 0.
- Ложных срабатываний на чистом каталоге нет: 64 профиля, 0 нарушений.
- `repin-exempt` не документирован ни в одном `.md` репозитория.

**Данные**

- Коммит `6a66faf5`, отставание от `origin/main` — 99 коммитов, опережение — 1.
- 64 профиля в каталоге, 6 из них `deprecated`.
- Радиус: 32 профиля из 64 имеют зависимых; максимум — `tacticum-core-base`, 18 зависимых.
- История: чек завалил бы 21 из 44 смерженных PR, трогавших версии манифестов (по 60 merge-коммитам).
- Открытые ветки: покраснеют 5 из 176; `bl-750` и `bl-816` не затронуты.

**Подтверждение**

```
git worktree add /Users/bubblemac/tacticum-worktrees/probe-775 --detach origin/fix/775-dependents-repin-check
git merge-tree --write-tree origin/main HEAD          # exit 0, один OID
git merge --no-edit origin/main                       # 0 конфликтов
cd apps/backend
uv run --python 3.12 --extra dev python -m pytest tests -q -p no:randomly \
    --ignore=tests/e2e_install --junitxml=merged-pytest.xml    # 2005/0/0/43, exit 0
uv run --python 3.12 --extra dev python -m pytest tests/e2e_install -q -p no:randomly \
    --junitxml=merged-e2e.xml                                  # 217/0/0/0, exit 0
uv run --python 3.12 --extra dev ruff check --output-format=concise \
    ../../scripts/check_profile_version_discipline.py \
    tests/catalog/test_dependents_repin_check.py               # 4 (те же, что в origin/main)
uv run --python 3.12 python ../../scripts/check_profile_version_discipline.py \
    --diff-against origin/main                                 # OK — 64 profile(s) clean
# искусственный дефект: бамп analysis-core 0.4.1→0.4.2 без ролей, коммит, затем
uv run --python 3.12 python ../../scripts/check_profile_version_discipline.py \
    --diff-against <base>                                      # 11 нарушений, exit 1
# + строка `- repin-exempt: all` в запись [0.4.2] CHANGELOG, коммит, затем то же
                                                               # OK — 64 profile(s) clean, exit 0
git worktree remove --force /Users/bubblemac/tacticum-worktrees/probe-775
```

Ветка `origin/fix/775-dependents-repin-check` после разведки — на том же `6a66faf5`, не изменена
и не запушена. В Taiga только чтение (`get_user_story`, `get_history`, `search_project`).

**НЕ проверено**

- Поведение в настоящем GitHub Actions — прогоны локальные; вывод о срабатывании чека в CI сделан
  чтением workflow и кода `main()`, не запуском пайплайна.
- Поведение на проде: доезжает ли контент после правильного бампа — это проверяется сидом
  и запросом к `profile_version_dependencies`, к серверу я не ходил.
- Полный список из 21 исторически заваленного PR не разбирался поштучно на предмет «а был ли там
  реальный дефект или сознательный staged rollout» — считалась только частота срабатывания.
- Прогон по 60 merge-коммитам сделан с пустыми exemptions: в старых CHANGELOG маркеров нет
  по построению, поэтому эта цифра — верхняя граница.
- Ветка не ребейзилась на `main` — проверялся мерж; для линейной истории ребейз одного коммита
  поверх бесконфликтного мержа риска не добавляет, но отдельно это не прогонялось.
