---
status: draft
role: explorer
task: ТЗ#3 / US#4 E-brd
for: lead-fr → ГД (ack перед правкой diverged brd)
repo: tacticum-dev @ origin/main 5884bcd
generated: 2026-07-24
permalink: tacticum/00-board/map-us4-ebrd-kmp-divergence
---

# Карта E-brd: канон brd-authoring ↔ kmp brd-authoring (расхождение)

**Цель:** охарактеризовать дельту, которую надо внести в KMP-brd (К-1/К-2), НЕ затерев axis-2 (#145). Это карта для ГД (ack), НЕ правка.

## Файлы

| | Путь | Строк | md5 |
|--|--|--|--|
| КАНОН | `templates/tacticum-dev-base/ingredients/skills/brd-authoring/SKILL.md` | 85 | `604e1f94…97` |
| KMP | `templates/iva-kmp-brownfield/ingredients/skills/brd-authoring/SKILL.md` | 62 | `18d93be7…f5` |

Оба в `origin/main` (fetch свежий). md5 различаются — подтверждено.

## Что ОБЩЕЕ (идентично, не трогаем)

- frontmatter (обе 1-7) — идентичен;
- `# BRD Authoring` (обе 9);
- `## Document structure` — канон 54-64 == KMP 35-45 (включая NFR-пункт 6 из `kb_get_nfr` — строка канон 64 / KMP 45), идентично;
- Rules-хвост: канон 71-77 == KMP 50-56;
- Anti-patterns-хвост: канон 81-83 == KMP 60-62.

## Расхождения — две оси (раздельно)

### (а) axis-2 / KMP-специфика — НЕ ЗАТИРАТЬ

KMP несёт СВОЙ блок FR-контракта вместо канонического «Input: reading the FR»:

- **KMP 11** — терзой intro `Generate a BRD from ТЗ using knowledge base.` (канон 11-15 — расширенный, про FR/версию). Косметика.
- **KMP 13-33** — `## FR на входе — наследование идентификаторов (сквозная трассировка)`. Это KMP-flavored контракт чтения FR. Внутри — реально axis-2/brownfield-специфика, которую НЕЛЬЗЯ терять:
  - **15-16**: вход через `fr.md` из `/start-task <Jira-key>` (cross-repo / Jira-точка входа brownfield);
  - **20-21**: перенос «Зафиксированных решений» `D-n` из FR как принятых — канон этого НЕ содержит;
  - **22-23**: собственные добавления BRD нумеруются продолжением серии FR с пометкой `(BRD)`;
  - **24-25**: несколько FR → префикс серии `FR2-UC-3`, наследовать как есть;
  - **26-29**: расхождения FR↔KB не молча — таблица `| # | FR говорит | KB/код говорит | предложение |`, критичные → комментарий в **Jira issue, label `needs-info`**, владелец правки FR — аналитик (cross-repo reconciliation, чисто axis-2);
  - **30**: секции BRD и порядок не меняются;
  - **32-33**: self-check `grep "UC-"` по fr.md и brd.md == одно множество номеров.

  → Строки **15-16, 20-33** — axis-2-контент, в каноне отсутствует, MUST-KEEP.
  → Строки **18-19** (`UC-n`/`FT-n` наследуются дословно, не renumber, GIVEN/WHEN/THEN) — СЕМАНТИЧЕСКИ пересекаются с канон-К-1, но БЕЗ указания локации (§1.4/§1.5) и БЕЗ версии. Это точка сшивки (см. план).

### (б) Отсутствие К-1/К-2 — ДЕЛЬТА, которую вносим

KMP-brd не несёт (всё присутствует в каноне, отсутствует в KMP):

1. **Детекция маркера `fr_skeleton`** — канон 17-33 (`## Input: reading the FR` + version-detection + v2/v1 layout). KMP версии FR вообще не различает — читает FR «плоско».
2. **К-1 (откуда FT/UC)** — канон 35-39: в v2 `FT-n`←§1.4, `UC-n`←§1.5 (Часть 1); в v1 ←П.A/П.B. KMP (18-19) читает `UC-n`/`FT-n` без локации.
3. **К-2 (регистрация серий CT/DM/EV, v2 only)** — канон 41-52: `CT-n`(§1.6)/`DM-n`(§1.7)/`EV-n`(§1.8) передать вниз в pin-authoring. В KMP — НЕТ ничего.
4. **Rules-дельта** — канон 69-70 (две строки: читать FT/UC по маркеру; ссылаться на CT/DM/EV в v2). В KMP отсутствуют.
5. **Anti-patterns-дельта** — канон 84-85 (не читать FT/UC из Приложения на v2; не renumber FT/UC/CT/DM/EV). В KMP отсутствуют.

## План аккуратного merge (что вносим, куда, что НЕ трогаем)

**Вносим 5 частей:**

1. **Version-detection преамбула** (из канона 17-33) → в НАЧАЛО KMP-блока `## FR на входе`, перед/вокруг строки 16 (после `не перенумеровывает:` или отдельным абзацем над буллетами). Даёт: маркер `fr_skeleton`; v2 → §1.4/§1.5 (+§1.6-1.8); marker absent → v1/П.A-П.B; «never guess — treat malformed as v1». Это добавляет ЛОКАЦИЮ поверх существующего KMP-правила verbatim-наследования.

2. **К-1 (локация FT/UC)** (канон 35-39) → сшить с KMP 18-19: НЕ дублировать «verbatim/не renumber» (уже в 18-19), а ДОБАВИТЬ «в v2 FT←§1.4, UC←§1.5; в v1 ←П.A/П.B». Точка сшивки — строка 18-19.

3. **К-2 (серии CT/DM/EV)** (канон 41-52) → НОВЫЙ подблок ПОСЛЕ KMP-блока FR (после строки 33) или новым буллетом. Конфликта нет — в KMP пусто.

4. **Rules-дельта** (канон 69-70) → в KMP `## Rules`, после строки 49 (перед `Each UC has unique ID`).

5. **Anti-patterns-дельта** (канон 84-85) → в KMP `## Anti-patterns`, после строки 62.

**НЕ ТРОГАЕМ (риск клоббера):**
- KMP 15-16 (`/start-task <Jira-key>`, fr.md-вход);
- KMP 20-21 (перенос `D-n`);
- KMP 22-25 (продолжение серии `(BRD)`, префикс `FR2-UC-3`);
- KMP 26-29 (таблица FR↔KB + Jira `needs-info` + аналитик-owner);
- KMP 30 (порядок секций);
- KMP 32-33 (grep self-check);
- KMP 35-45 (Document structure + NFR) — идентично канону;
- KMP 50-56, 60-62 (общие Rules/Anti-patterns хвосты).

## Backward-safe

После внесения: KMP-brd читает **v2 по маркеру** `fr_skeleton: 2` (§1.4/§1.5 + CT/DM/EV), а при **отсутствии маркера → v1** — как раньше (плоское чтение / П.A-П.B). Существующее поведение KMP (наследование UC-n/FT-n, D-n, grep-check) = ветка v1/legacy, сохраняется. Прод-safe, обе версии поддержаны.

## ОТКРЫТЫЙ ВОПРОС для ГД (реконсиляция, не выдумываю)

KMP-блок (18-19) читает `UC-n`/`FT-n` из `fr.md` **без локации** (ни §1.4/§1.5, ни П.A/П.B). Канон v1 явно указывает П.A/П.B. Нужно решение ГД: KMP-v1-FR использует layout П.A/П.B (тогда К-1-v1 ложится ровно) ИЛИ плоский FR (тогда v1-ветка К-1 в KMP формулируется мягче — «из FR как есть», без П.A/П.B). Не найдено в файле однозначного ответа — решает роль-владелец канона.

## Оценка объёма

- Чистой канонической дельты в каноне: ~17 строк (Input/marker 17-33) + 5 (К-1) + 12 (К-2) + 2 (Rules) + 2 (Anti-patterns) ≈ **~38 строк источника**.
- НЕТТО в KMP (с учётом что verbatim-наследование уже есть в 18-19 — не дублируем): реально добавляем **~25-35 строк**.
- Переплетение с axis-2: **умеренное, ЛОКАЛИЗОВАНО** в одной точке — строки KMP 18-19 (К-1 сшивается с существующим verbatim-правилом). Остальные 4 части (маркер-преамбула, К-2, Rules, Anti-patterns) вносятся в НЕконфликтующие места. axis-2-блоки (15-16, 20-33) не пересекаются с дельтой.