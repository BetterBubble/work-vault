---
title: merge-qa-mobile-kit-2026-08-06 — сведение ветки QA с origin/main
type: report
status: current
created: 2026-08-06 14:30
updated: 2026-08-06 14:30
permalink: tacticum/00-board/merge-qa-mobile-kit-2026-08-06
repo: tacticum-dev
branch: feat/qa-mobile-kit-adoption
project: tacticum-dev / профили / QA
lead: lead-qa
tags:
- board
- qa
- merge
---

# Ветка сведена с `origin/main` — 17 конфликтов разрешены

Мерж-коммит **`f0c4fe87`** поверх `efcee066`, вторым родителем `cd0ddfc7` (`origin/main`).
Страховка на прежнюю вершину: тег `pre-merge-qa-2026-08-06` → `efcee066`.

## Что сохранено из каталога (то, что «прилетело новое»)

1. **Лейн `iva-write-base`** у `iva-role-qa` и `iva-role-qa-web` — канал записи в
   Jira/Confluence ИВА (PR #227/#230). Остался последней строкой `depends_on` вместе с
   комментарием-инструкцией. Это была главная опасность мержа: обе роли уже сидены с каналом
   на прод, и на эти версии **запинены установки** — потеря прошла бы молча.
2. **Снятие ручных `[mcp_servers.*]`** из codex-шаблонов всех четырёх ролей (US #769). У
   мобильной роли это был реальный конфликт: ветка правила комментарий (`Selenium → Appium`)
   **внутри блока, который каталог удалил целиком** — правка ушла вместе с блоком и стала
   беспредметной, потерей это не является.
3. Всё остальное из 26 коммитов `main` — автослиянием, без вмешательства.

## Что сохранено из ветки

Перенятие кита заказчика · предупреждение о перезаписи чужого кита во всех четырёх ролях ·
область применимости правила подчинения конвенции проекта · отзыв ложного утверждения про
`names.py` у десктопной роли · процесс-лейн `tacticum-autotest-core` 0.7.1. Ни одной правки
ветки не отброшено.

## Версии

Подняты выше **обеих** сторон, потому что содержимое отличается от каждой:

| Роль | ветка | каталог | стало |
|---|---|---|---|
| `iva-role-qa` | 0.11.0 | 0.8.1 | **0.12.0** |
| `iva-role-qa-web` | 0.7.0 | 0.4.1 | **0.8.0** |
| `iva-role-qa-mobile` | 0.6.0 | 0.1.1 | **0.6.1** |
| `iva-role-qa-desktop` | 0.5.0 | 0.1.1 | **0.5.1** |

## Найденная по ходу коллизия номеров

**Номер `0.8.0` у `iva-role-qa` и `0.4.0` у `-web` заняты дважды** — веткой (04.08, своё
содержимое) и каталогом (05.08, канал записи). В каталоге и в проде живёт версия каталога, на
неё же запинены установки. В CHANGELOG обе истории сведены по номерам: каталожная запись идёт
первой, у записи ветки **в теле** стоит пометка, что номер занят и в проде лежит другое.
Заголовки `## [X.Y.Z]` не тронуты намеренно — их разбирает
`check_profile_version_discipline.py`, и правка формата сломала бы гейт.

## Golden

Перегенерированы (`E2E_INSTALL_REGEN_GOLDEN=1`). Проверено, что регенерация ничего не замазала:

- **состав установки QA-ролей не изменился ни на один файл** — 88/87, 82/81, 80/79, 80/79
  ключей, ноль лишних, ноль потерянных относительно ветки;
- изменились хеши ровно там, где ожидалось: `CLAUDE.md`/`AGENTS.md` (секция канала записи),
  `.mcp.json`/`config.toml` (MCP), `tacticum-context/SKILL.md` (бамп базового лейна);
- **все прочие golden совпадают с `origin/main` до байта** — от main приехали 97 golden,
  отличаются от него ровно 8, и все восемь — QA-шные.

## Числа

| | до мержа (ветка) | база `origin/main` | **после мержа** |
|---|---|---|---|
| `pytest tests/catalog tests/e2e_install` | 1298 passed · 42 skipped · 0 failed | 1268 passed · 42 skipped · 0 failed | **1319 passed · 42 skipped · 0 failed · 0 error** |
| версии профилей | OK 63 | — | **OK 64** (прибавился `iva-write-base`) |
| зеркала | OK 75 в 7 парах | — | **OK 75 в 7 парах** |
| ссылки установки | 116 битых | 116 битых | **116 битых** — все в `iva-role-go`, унаследовано, не регресс |

`gate-check.sh`: **нарушений нет** — дерево чистое, секретов и мусора нет, AI-подписей нет,
63 непустых коммита, 87 файлов в диффе против `origin/main`.

## Канал записи доказан мутацией, а не отчётом

Снял строку `- iva-write-base` из манифеста `iva-role-qa` → **упали оба e2e**
(`test_install_flow_roles_generic[codex-iva-role-qa]` и `[claude-code-iva-role-qa]`). Вернул →
8 passed. То есть канал под живым сторожем `_IVA_WRITE_ROLES`, и молча он теперь не пропадёт.

## Что осталось за границей

- **Не пушено и не мержено** на момент записи; PR создаёт и мержит Президент.
- Живого прогона автотестов роли на стенде по-прежнему нет — блокер прежний (нет `pytest`,
  Appium, `adb`, Squish, VPN).
- **`main` оставил висячую фразу** «блоки ниже даны как ориентир…» в codex-шаблонах всех
  четырёх ролей после удаления самих блоков (#769). Дефект единообразный, чужой зоны не
  касается; трогать в мерже не стал — это была бы правка поверх решения каталога, а не
  сведение. Стоит починить отдельно.
- 116 битых ссылок в `iva-role-go` — чужая роль, отдельная работа.

## Связано

[[verify-qa-mobile-kit-merge-2026-08-06]] · [[report-qa-mobile-kit-adoption]] ·
[[Направление- Профили → QA-профиль (iva-role-qa) + AQA-toolkit ИВА]]
