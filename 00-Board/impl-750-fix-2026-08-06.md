---
title: "impl #750: assets — запрет в схеме, терпимость в модели"
type: note
permalink: tacticum/00-board/impl-750-fix-2026-08-06
---

# impl #750 — правка под находку verifier'а

Ветка `fix/750-skill-assets-schema`, worktree `/Users/bubblemac/tacticum-worktrees/bl-750`.
Один новый коммит поверх прежнего состояния ветки: `c227f069`. Не пушил, не мержил, прод не трогал.

## Что было не так

Схема авторинга и Pydantic-модель рендера — два разных контракта, и в прежнем состоянии ветки они
были склеены: `assets` сняли и там, и там. Схема права: манифест не должен объявлять доставку,
которой нет. Модель — нет: она читает то, что **уже лежит в БД**, а там живёт
`tacticum-lite-base` 0.1.3 (status=active, ингредиент `lite-task-workflow`) с `metadata.assets`.
`extra="forbid"` превращал эту строку в `ValidationError` в `renderer._orm_row_to_pydantic`,
то есть в падение установки на codex/copilot-пути.

## Что сделано

1. **Схема не тронута** — `assets` в `templates/_schema/ingredient.v1.schema.json` остаётся удалённым,
   `additionalProperties: false` закрывает обход через незаявленное поле. Авторы профилей положиться
   на него не могут.
2. **`SkillMetadata.assets` возвращён как legacy-only** — принимается и игнорируется. В докстринге
   класса названо прямо: поле удалено из схемы авторинга по #750, в модели оставлено только ради
   исторических строк, живой пример — `tacticum-lite-base` 0.1.3, рендером не используется ни в одном
   CLI. Механики доставки как не было, так и нет — поле не выдаётся за механику.
3. **Инвариант зеркала переписан под асимметрию.** Введён именной список
   `_SCHEMA_DROPPED_LEGACY_FIELDS = {"skill_spec": {"assets"}}`. Расхождение «в модели есть, в схеме
   нет» разрешено только для полей из него; «в схеме есть, в модели нет» запрещено по-прежнему — это
   и есть баг, ронявший рендер. Третьей проверкой список защищён от протухания: перечисленное поле
   обязано существовать в модели.
4. **Тесты приведены в порядок:**
   - `test_skill_spec_assets_rejected_by_schema_accepted_by_model` (переименован из
     `..._rejected_by_schema_and_model`) — схема отвергает, модель принимает и отдаёт значение.
   - `test_prod_legacy_assets_row_renders_for_all_clis` (новый) — прод-подобная ORM-строка проходит
     `render_for_claude_code` / `render_for_codex` / `render_for_copilot`. Copilot прогоняется на
     строке с `copilot` в `supports`: `_render_via_canonical` фильтрует ингредиент по `supports`, а в
     проде у этой строки заявлены только claude-code и codex.
   - `test_skill_spec_rejects_unknown_metadata_key` оставлен как есть.

Правил символьными операциями Serena: 3 (`replace_symbol_body` ×2, `insert_after_symbol`,
`insert_before_symbol` ×2 — итого 5 вызовов) плюс 2 текстовые правки импортов/докстринга
(`replace_content`) и 1 `Edit`: `replace_symbol_body` потерял декоратор
`@pytest.mark.parametrize` над тестом зеркала, вернул его текстом.

## Главное доказательство: прод-подобная строка

Скрипт собирает ровно прод-строку (`lite-task-workflow`, skill_spec, supports={claude-code,codex},
`metadata.assets=["references/work-order-template.md"]`) и гонит её через модель рендера и три
публичных рендерера.

**ДО** (модель без `assets` — состояние ветки, найденное verifier'ом):

    модель рендера (TypeAdapter[Ingredient]): FAIL ValidationError
       skill_spec.metadata.assets  Extra inputs are not permitted [type=extra_forbidden, ...]
    claude-code: OK, actions=1, path=.claude/skills/lite-task-workflow.md
    codex: FAIL ValidationError
    copilot: FAIL ValidationError
    ИТОГ: ПАДАЕТ

**ПОСЛЕ** (`c227f069`):

    модель рендера (TypeAdapter[Ingredient]): OK
    claude-code: OK, actions=1, path=.claude/skills/lite-task-workflow.md
    codex: OK, actions=1, path=.agents/skills/lite-task-workflow/SKILL.md
    copilot: OK, actions=1, path=.github/skills/lite-task-workflow/SKILL.md
    ИТОГ: ПРОХОДИТ

`claude-code` зелёный в обоих прогонах — он идёт по сырым ORM-строкам мимо модели, поэтому и не
ловил расхождение. Падало ровно там, где сказал verifier.

## Гейты (числа)

Итоговая строка `N passed` в этом окружении не печатается — числа взяты из `--junitxml`.

| Гейт | Команда | Было (verifier) | Стало |
|---|---|---|---|
| unit | `pytest tests -q -p no:randomly --ignore=tests/e2e_install` | 1957 passed / 43 skipped / 0 failed | **1958 passed / 43 skipped / 0 failed / 0 errors** (tests=2001) |
| e2e | `pytest tests/e2e_install -q -p no:randomly` | 217 passed | **217 passed / 0 failed / 0 skipped** |
| ruff | `ruff check .` | 0 | **All checks passed!** |
| format | `ruff format --check .` | — | **515 files already formatted** |
| mypy | `mypy src` (как в CI) | 0 в 222 файлах | **Success: no issues found in 222 source files** |

Прирост unit +1 — это новый `test_prod_legacy_assets_row_renders_for_all_clis` (второй тест
переименован, а не добавлен).

**Расхождение по mypy, о котором надо знать.** В брифе стояла команда `mypy .`, но CI
(`.github/workflows/backend-ci.yml:97`) гоняет `mypy src`. `mypy .` на этой ветке даёт
**1195 ошибок в 154 файлах** — это весь `tests/`, где строгие аннотации никогда не ставились
(в `test_manifest_schemas.py` 46 ошибок вида «missing return type annotation», включая строки,
которых я не касался). Цифра «0 в 222 файлах» из брифа — это именно `mypy src`, и он зелёный.
Ошибки `mypy .` не мои и не новые, но и «0» по этой команде получить нельзя.

## Что НЕ трогал

`templates/**` кроме уже изменённого `_schema/ingredient.v1.schema.json` · голдены
`tests/e2e_install/golden/**` (не перегенерировал) · прод — ни SQL, ни ретракта версий, решение по
прод-строке за человеком · поле `scripts` (тот же дефект, отдельная задача) · остальные 8 kind-ов в
схеме — `additionalProperties` им не добавлял · `scripts/check_install_links.py` (правка verifier'а,
оставил как есть) · `main`, пуш, мерж, деплой, Taiga.

Проверил заодно: в `apps/**` и `templates/_schema/**` больше нет ни одной живой ссылки на
`metadata.assets` кроме моделей/тестов #750 — zod-схемы админки поля не знают.

## ВЕРДИКТ

**Готово.** Контракты разведены: схема запрещает, модель терпит.

- **Проверено:** прод-подобная строка с `metadata.assets` проходит модель рендера и все три
  публичных рендерера; схема эту же строку отвергает; инвариант зеркала держит опасную сторону
  расхождения и разрешает безопасную только по именному списку; все гейты в числах выше.
- **Данные (было → стало):** codex/copilot на прод-строке `ValidationError` → OK (actions=1);
  unit 1957 → 1958 passed при 0 failed; e2e 217 → 217; ruff 0 → 0; `mypy src` 0/222 → 0/222.
- **Подтверждение:** `uv run --python 3.12 --extra dev python <скрипт прод-строки>` — вывод «ДО/ПОСЛЕ»
  приведён выше дословно; junit-счётчики `tests="2001" skipped="43" failures="0" errors="0"` и
  `tests="217" failures="0"`; `mypy src` → `Success: no issues found in 222 source files`.
- **НЕ проверено:** поведение на реальной прод-БД (строку воспроизводил локально по описанию
  verifier'а, к проду не ходил) · нужен ли ретракт версии 0.1.3 — это решение человека, я его не
  готовил · `mypy .` до нуля не доведён и в этой задаче не трогался · поле `scripts` осталось с тем
  же дефектом, что и было, — вне скоупа #750.

---

# Доработка по критикам (коммит `9b67d784`)

Оба ревизора — `[[critic-750-a-2026-08-06]]` и `[[critic-750-b-2026-08-06]]` — сошлись в одном:
диффом ничего не сломано, но **текст обещает запрет, которого в системе нет**. Ниже — ровно семь
пунктов задания, каждый со ссылкой на то, чем подтверждён.

## Что я перепроверил сам, прежде чем править тексты

| Утверждение критиков | Как проверил | Итог |
|---|---|---|
| Схема в рантайме не читается | `grep -rn jsonschema src/ scripts/` | пусто — ни одного импорта |
| `ingredient.v1` прикладывают только тесты профилей | grep по `tests/` | 5 профильных файлов + `test_migration_0008_data` (мёртвый резолвер) |
| Главный путь авторинга валидирует моделью | `catalog/interface/admin/catalog.py:33,529` | `_INGREDIENT_DOMAIN_ADAPTER = TypeAdapter(Ingredient)` — модель, не схема |
| Сид впрыскивает `codex_body`/`codex_target_path` в `metadata` | `scripts/seed_community.py:74-91` | подтверждено, впрыск идёт ПОСЛЕ сборки payload |

## 1-2. Ложь убрана из трёх мест

- **`skill_spec.py`.** Абзац «Схема закрыта `additionalProperties: false`, поэтому новый манифест
  объявить `assets` не может» заменён на честный: схема в рантайме не применяется (`jsonschema` в
  `src` не импортируется, сид кладёт `metadata` как есть), фактическая защита — тесты пяти профилей,
  а `POST .../ingredients` схему не проходит вовсе и валидирует этой же моделью, которая `assets`
  принимает по построению → **автор в админке получит 201**. Итоговая формулировка: «правка снимает
  вектор объявления в манифесте, а не закрывает поле на входе в БД».
- **Докстринг модуля `test_manifest_schemas.py`.** Строка «backend `seed_profile.py` (server-side
  validation на seed)» — механизма нет и не было. Заменена перечнем реальных потребителей: эти
  тесты + тесты пяти профилей (единственный гейт), zod-схемы админки (ручное зеркало), admin API
  (валидирует моделью, не схемой). Прямо сказано, что прежняя редакция обещала несуществующее.
- **Про `scripts`.** Было «под него заведена отдельная задача» — задачи нет, Президент отложил её
  06.08, на доске лежит только черновик с пометкой «не заводить». Стало: «находка зафиксирована на
  доске, задача под неё пока не заведена».

Тело коммита `c227f069` не переписывал (история); в теле `9b67d784` сказано прямо, что именно было
ложью и чем заменено.

## 3. Два сторожа — и оба проверены негативным прогоном

Добавлены в `test_metadata_fields_mirror_between_schema_and_model`, срабатывают только для kind с
непустым legacy-списком. Мало поставить сторожа — надо показать, что он кусает:

**Сторож A** (временно снял `additionalProperties: false` у `skill_spec`):

    E AssertionError: skill_spec: вырез в зеркале (['assets']) выдан под обещание, что объявить
      поле в манифесте нельзя, а ветка схемы открыта — без additionalProperties: false удаление
      поля из properties не запрещает его
    1 failed, 8 passed in 0.27s

**Сторож B** (временно вернул `assets` в `properties` схемы):

    E AssertionError: skill_spec: ['assets'] снова объявлено в схеме — запись в
      _SCHEMA_DROPPED_LEGACY_FIELDS пережила свою причину, удалить
    1 failed, 8 passed in 0.20s

Схема после каждого прогона восстановлена из копии; итоговый `git diff` схемы — ровно одна
добавленная строка `description` (см. п. 5).

## 4. Условие снятия выреза записано

У самого `_SCHEMA_DROPPED_LEGACY_FIELDS`, отдельным абзацем с заглавным «УСЛОВИЕ СНЯТИЯ»: в каталоге
уже `tacticum-lite-base` 0.1.4 без поля (CHANGELOG от 28.07); как только 0.1.3 уйдёт из `active` —
пересидом или ретрактом, — запись удалить вместе с полем в модели, иначе она молча останется
мусором. Номер задачи не выдумывал: задачи на снятие нет, и обещать её было бы третьей ложью.

## 5. Схема объясняет себя сама

В ветку `skill_spec.metadata` добавлен `description`: это контракт авторинга, а не содержимого БД;
единственная ветка с `additionalProperties: false` из девяти; в рантайме схема не применяется;
ключи `codex_body`/`codex_target_path`/`copilot_body`/`copilot_target_path` впрыскивает
`seed_community.py` **после** валидации — в БД они есть, в схеме их нет и быть не должно.
Находка критика A: таких строк в проде 31, у 8 ингредиентов каталога. Ломаться нечему, но следующий
не будет разбираться заново.

## 6. Тест переименован и очищен

`test_prod_legacy_assets_row_renders_for_all_clis` →
**`test_prod_legacy_assets_row_survives_model_backed_renderers`**.

- `render_for_claude_code` **убран из цикла**: он идёт по сырым ORM-строкам мимо модели и в целевом
  сценарии зелен при любом исходе — то есть доказывал не то. Импорт из файла тоже убран, чтобы не
  осталось мёртвой ссылки. Почему исключён — сказано в докстринге.
- В цикле остались `codex` и `copilot` — единственные пути, где утверждение проверяемо.
- copilot-строка помечена **синтетической** прямо в коде (`# синтетика: copilot дописан в supports`)
  и в докстринге: в проде copilot у этой строки не заявлен.
- «all CLIs» из имени убрано: живой путь установки знает три CLI, `opencode` и `gemini` к установке
  не подключены и здесь не проверяются — это тоже записано в докстринге.

## 7. Факт про прод-строку — один дом

Было четыре копии (`skill_spec.py`, комментарий к списку, два докстринга теста). Стало: единственный
дом — комментарий у `_SCHEMA_DROPPED_LEGACY_FIELDS`, где лежат версия, ингредиент, разведочное
«1 из 4140» с датой разведки и условие снятия. Остальные три места ссылаются на него текстом
(«какая строка, сколько их и когда исключение снимать — в комментарии к …»). Проверено grep-ом:
`0.1.3` и `4140` встречаются только в этом комментарии.

## Чего НЕ делал (по прямому запрету лида)

- `additionalProperties: false` остальным 8 kind-ам — **не добавлял**. Предложение критика B, но он
  не знал находки критика A: в БД 804 строки несут ключи, которых нет ни в схеме, ни в модели.
- `patch_ingredient` — **не трогал**. Модель `assets` принимает по построению, дефект это не
  закроет, а поведение API изменит.
- `scripts`, прод, `main`, голдены, `templates/**` кроме схемы, пуш, мерж, Taiga — не трогал.

## Гейты после доработки

| Гейт | Результат |
|---|---|
| unit (`pytest tests --ignore=tests/e2e_install`) | `tests="2001" skipped="43" failures="0" errors="0"` → **1958 passed**, exit 0 |
| e2e (`pytest tests/e2e_install`) | `tests="217" failures="0" skipped="0"` → **217 passed**, exit 0 |
| `ruff check .` | **All checks passed!**, exit 0 |
| `ruff format --check .` | **515 files already formatted**, exit 0 |
| `mypy src` (как в CI) | **Success: no issues found in 222 source files**, exit 0 |
| схема как JSON | `json.load` проходит |

Числа те же, что до доработки: тестов по-прежнему 1958 (сторожи добавлены внутрь существующего
параметризованного теста, новых кейсов не появилось).

Прод-подобная строка после доработки прогнана заново — модель OK, claude-code/codex/copilot OK по
одному действию каждый.

## ВЕРДИКТ по доработке

**Готово, все семь пунктов.**

- **Проверено:** каждое фактическое утверждение критиков перепроверено в дереве самостоятельно
  (таблица выше); оба новых сторожа доказаны негативным прогоном с приведённым текстом падения;
  тексты сужены до правды в трёх местах; факт про прод-строку сведён к одному дому; все гейты
  зелёные в числах.
- **Данные (было → стало):** утверждений о запрете, которых система не обеспечивает: 3 → 0;
  сторожей над вырезом: 0 → 2 (оба падают при снятии причины); копий прод-факта: 4 → 1;
  CLI в цикле теста: 3, из них доказательных 2 → 2 доказательных, тавтологичный убран;
  тесты 1958 → 1958, e2e 217 → 217, ruff 0 → 0, `mypy src` 0/222 → 0/222.
- **Подтверждение:** негативные прогоны сторожей (`1 failed, 8 passed` дважды, тексты ассертов
  выше); junit `tests="2001" skipped="43" failures="0" errors="0"` и `tests="217" failures="0"`;
  `mypy src` → `Success: no issues found in 222 source files`; `grep` по `jsonschema` в `src/` —
  пусто, `catalog.py:33,529` — `TypeAdapter(Ingredient)`.
- **НЕ проверено / осталось открытым:** дыра админского API (`POST` пропускает `assets`, `PATCH` не
  валидирует metadata вовсе) — не закрывал по прямому запрету, она описана в тексте, но не
  устранена; прод-число «1 из 4140» держится на однократной разведке 06.08, к БД я не ходил;
  8 kind-ов остаются открытыми, и исходная связка «схемно-валидный манифест роняет codex-рендер»
  для них жива; симптом задачи (навык ссылается на несуществующий файл) по-прежнему не измеряется
  этой веткой — по данным критика B в каталоге 128 известных находок. **#750 мержить можно,
  закрывать как «дефект устранён» — нельзя.**

---

# Приёмка #750 (коммит `c523fc45`)

Плюс перенос одного докстринга, потерянного из-за общего Serena-сервера (ниже отдельно).

## Сначала — про потерянную правку

Лид разобрался в причине: Serena это один MCP-сервер на всех агентов с ОДНИМ активным проектом.
Пока я работал, активным успел стать `bl-816`, и часть моих символьных правок ушла в чужое дерево;
встречно у `impl-816` его операции падали с «points outside the project root .../bl-750».

Проверил каждый пункт доработки в `bl-750` grep-ом по дереву — на месте оказалось всё, кроме
**докстринга модуля** `test_manifest_schemas.py` (там до правки висела ровно та ложь про
«server-side validation на seed», которую велел убрать критик B). Перенёс его текстовым
редактированием. Чужое дерево `bl-816` не трогал ни разу.

Дальше правлю эти файлы только `Edit`/`Write`, без Serena: файлов два, выигрыш от символьных
операций нулевой, риск реальный.

## Что добавлено — тест, который и есть приёмка

`test_all_real_ingredients_validate_against_ingredient_v1_schema` — глоб по
`templates/*/manifest.yaml`, валидирует **каждый ингредиент каждого манифеста** против
`ingredient.v1`. Симметричен соседнему `test_all_real_manifests_validate_against_v2_schema`,
но тот дыру не закрывал: у `manifest.v2` ключа `ingredients` нет вовсе.

В докстринге теста прямо сказано, что он и есть механизм запрета: автор, объявивший `assets` в
манифесте, получает красный PR. Формулировка «фактическая защита — тесты пяти профилей» снята из
докстринга `SkillMetadata` как устаревшая.

**Ошибки печатаются адресно.** Не `v.validate(...)` на первой же ошибке без контекста, а
`iter_errors` с именем профиля и ингредиента — иначе на 812 ингредиентах падение не найти:

    AssertionError: tacticum-lite-base / lite-task-workflow: ['metadata']:
    Additional properties are not allowed ('assets' was unexpected)

## Числа по каталогу — совпали с разведкой

Прогон схемы по всему каталогу до написания теста:

    манифестов: 64  ингредиентов: 812  невалидных: 0

Ровно те числа, что дала разведка. Опасение контролёра «прогон всех 64 вскроет находки» не
подтвердилось: тест зелёный сразу, чинить в каталоге нечего.

## Тест краснеет — проверено впрыском

Временно добавил `metadata.assets: ["references/work-order-template.md"]` ингредиенту
`lite-task-workflow` в `templates/tacticum-lite-base/manifest.yaml`:

    E AssertionError: tacticum-lite-base / lite-task-workflow: ['metadata']:
      Additional properties are not allowed ('assets' was unexpected)
    1 failed in 0.67s

Манифест восстановлен из копии, `git status --short templates/` — пусто.

## Тест реально запустится на PR — проверено по workflow

Это главное условие: зелёный тест, который не запускается, бесполезен.

- `.github/workflows/install-e2e.yml:18-27` — триггеры `pull_request` **и** `push: main` с
  `paths: templates/**` (плюс `apps/backend/**` и сам workflow).
- Строка 47 — первый pytest-шаг: `pytest tests/catalog -q -p no:randomly`. Новый тест лежит именно
  там.
- `backend-ci.yml:21-28` для этого не годится — его `paths` только `apps/backend/**` и свой файл.
  PR, трогающий один лишь манифест, его не запустит.

То есть PR с `assets` в манифесте краснеет по `install-e2e`, даже если бэкенд в нём не тронут.

## Чего НЕ делал (по прямому запрету)

- `seed_community.py` — не трогал. Валидация на сиде возможна и дёшева, но это боевой путь, решение
  за Президентом. В PR молча не тащу.
- Админский API (`add_ingredient`/`patch_ingredient`) — не трогал. По разведке: `add_ingredient`
  33 события за один день 14 мая, `patch_ingredient` — 0 за всю историю при 500 активных версиях.
  Авторы ходят манифестом и PR.
- `manifest.v2.schema.json`, `scripts`, прод, `main`, голдены, пуш, мерж, Taiga — не трогал.

## Гейты (числа)

| Гейт | Результат |
|---|---|
| unit | `tests="2002" skipped="43" failures="0" errors="0"` → **1959 passed**, exit 0 |
| e2e_install | `tests="217" failures="0" skipped="0"` → **217 passed**, exit 0 |
| `ruff check .` | **All checks passed!**, exit 0 |
| `ruff format --check .` | **515 files already formatted**, exit 0 |
| `mypy src` | **Success: no issues found in 222 source files**, exit 0 |

Прирост 2001 → 2002 — это ровно новый тест приёмки.

## ВЕРДИКТ по приёмке

**Приёмка выполнена.** Фраза постановки «запретить валидацией, чтобы авторы профилей не смогли на
него положиться» теперь обеспечена механизмом, а не обещанием.

- **Проверено:** схема прикладывается ко всем 812 ингредиентам всех 64 манифестов, а не к пяти
  профилям; тест краснеет на реальном манифесте с `assets` с адресным сообщением; тест запускается
  на PR, трогающем `templates/**` (`install-e2e.yml`, `paths` + строка 47), а не только при правке
  бэкенда; все гейты зелёные в числах.
- **Данные (было → стало):** профилей под гейтом схемы 5 из 64 → **64 из 64**; ингредиентов под
  гейтом ~единицы → **812**; невалидных в каталоге 0 → 0 (находок нет); тестов 2001 → 2002;
  e2e 217 → 217; ruff 0 → 0; `mypy src` 0/222 → 0/222.
- **Подтверждение:** прогон по каталогу «манифестов: 64 ингредиентов: 812 невалидных: 0»; красный
  прогон с впрыском (`1 failed`, текст ассерта выше); `install-e2e.yml:18-27` и `:47`;
  junit `tests="2002" skipped="43" failures="0" errors="0"`, `tests="217" failures="0"`.
- **НЕ проверено / остаётся открытым:** админский путь (`add_ingredient` валидирует моделью и
  `assets` пропускает, `patch_ingredient` не валидирует metadata вовсе) — закрывать запрещено,
  отдельной задачей; валидация на сиде не заводилась — решение Президента; 8 kind-ов схемы
  по-прежнему открыты (`additionalProperties` им не добавлял); симптом «навык ссылается на
  несуществующий файл» этой веткой не измеряется — по данным критика B в каталоге 128 известных
  находок битых ссылок, это отдельный долг.

Три коммита ветки: `c227f069` (контракты разведены) → `9b67d784` (тексты сужены до правды,
сторожи) → `c523fc45` (приёмка). Не пушил, не мержил.
