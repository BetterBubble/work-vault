---
type: report
status: draft
date: 2026-08-07
author: verifier
tags:
- triage
- taiga
- board
permalink: tacticum/00-board/triage-1-2026-08-07
---

# Триаж кандидатов на закрытие — блок 1 (#230, #305, #304, #303, #300, #428, #429)

Проверка по существу: дифф коммита против текста задачи, плюс состояние `origin/main`
на сегодня (HEAD `1bc7f93c`), плюс — для #428/#429 — прогон тестов в отдельном worktree и
чтение боевой БД каталога. Рабочее дерево `tacticum-dev` не трогалось; два временных
worktree созданы и сняты.

Все четыре коммита действительно в `origin/main` (`git merge-base --is-ancestor` = 0 для
`11e6b01a`, `570ae010`, `d8d44dcd`, `510eae3a`).

---

## #230 — split `platform/admin/router.py` per bounded context (ADR-0012)

**Просили:** механический перенос существующих `/admin/seed`, `/admin/retract`,
`/admin/tokens`, `/admin/audit` из `platform/admin/router.py` в `interface/admin/` своих
контекстов; приёмка №1 — «`platform/admin/router.py` пустой (или удалён)».

**Что в коммите `11e6b01a`:** добавлены **новые** эндпоинты Organization CRUD в
`identity/interface/admin/orgs.py` (+475 строк, 6 файлов, ни одного удаления). Тело коммита
прямым текстом говорит обратное закрытию: *«existing /admin/seed-/retract-/tokens-/audit
stay in platform/admin/router.py until tech-debt issue Taiga #230 is picked up»*. Плюс сам
заголовок — «commit 1/5», то есть часть незаконченной серии по другой задаче (ADR-0018).

**Состояние сегодня:** в `origin/main` файл `apps/backend/src/backend/platform/admin/router.py`
жив и по-прежнему содержит `@router.post("/seed")` (стр. 114), `/retract` (158), `/tokens` (209),
`/audit` (363) — и сверх того `/usage` (387). Приёмка №1 не выполнена спустя 89 дней.

**Вердикт: НЕ ЗАКРЫВАТЬ.** Ссылка на задачу в коммите — это отметка «долг остаётся», а не «долг закрыт».

---

## #305 — friendly-сообщения вместо сырой ошибки pydantic

**Просили:** бэкенд превращает `ValidationError` в структурный `{kind, field, message}`;
фронт рендерит по короткой фразе на поле; в снапшоте Playwright нет `pydantic.dev`.

**Что в коммите `570ae010`:** расширены `IngredientCreateRequest`/`PatchRequest`
(`supports`/`install_scope`/`target_path_template`) и `GENERIC_KIND_HINTS`; роутер редакторов
переключён на generic. Про обработку ошибок — ничего. Тело коммита называет #305 в списке
**отложенного**: *«Specialized per-kind editors stay in the file but are not wired — to be
revived in a proper sweep epic (Taiga #303 / #304 / #305 / #306)»*, и прямо фиксирует, что
пользователь и дальше видит сырое: *«Pydantic returns 422 with the canonical shape needed»*.

**Состояние сегодня:** `catalog/interface/admin/catalog.py:541-544` в `origin/main` —
`except Exception … detail=f"metadata invalid for kind={request.kind}: {exc}"`, то есть ровно
та строка «metadata invalid for kind=…», с которой заведена задача.

**Вердикт: НЕ ЗАКРЫВАТЬ.**

---

## #304 — стабильные селекторы (`name=` / `data-testid`) в 9-kind редакторе

**Просили:** `document.querySelector('textarea[name="metadata"]')` возвращает элемент; то же
для `body`, `ingredient_id`, `tier` во всех kind-модалках; плюс Playwright-смоук на селекторы.

**Что в коммите `570ae010`:** только маршрутизация редакторов и подсказки; ни одного атрибута
не добавлено. Задача, как и #305, названа в коммите отложенной.

**Состояние сегодня:** во всём `apps/admin_web/src/components/profile-editor/editors.tsx`
(978 строк в `origin/main`) поиск `name="` и `data-testid` даёт **0 совпадений**. У инпутов
`ingredient_id`, `tier`, `body`, `metadata` в `GenericMetadataEditor` атрибутов нет.

**Вердикт: НЕ ЗАКРЫВАТЬ.**

---

## #303 — пред-заполненный per-kind шаблон metadata вместо `{}`

**Просили (опция 1):** при открытии модалки textarea уже содержит kind-специфичный скелет
JSON; статический абзац-подсказка **удалён** (информация переехала в поле).

**Что в коммите `570ae010`:** сделано ровно обратное направление — `GENERIC_KIND_HINTS`
расширен ещё на 5 kinds, то есть усилен именно **абзац**, который задача просит убрать.

**Состояние сегодня:** в `GenericMetadataEditor` (`origin/main`) поле инициализируется как
`useState(initial?.metadata ? JSON.stringify(...) : "{}")` — пусто для новой записи; блок
`<div className="field-help">` с `GENERIC_KIND_HINTS[kind]` на месте. Оба пункта приёмки
не выполнены.

**Вердикт: НЕ ЗАКРЫВАТЬ.** Коммит — соседняя работа по тому же экрану, не эта задача.

---

## #300 — плейсхолдеры `${REPO_ROOT}` и др. в `mcp_server_spec`

**Просили:** `render_for_claude_code` раскрывает `${REPO_ROOT}`, `${HOME}`, `${OS}`,
`${PROFILE_ID}` в `args`/`command`/`env` и в `repo_config.target_path_template`; новый тест в
`tests/catalog/domain/test_renderer.py` на все 4 плейсхолдера; блок документации в исходнике.

**Что в коммите `d8d44dcd`:** это фикс **другой** задачи — Taiga #310, `mcp_server_spec` теперь
эмитит `merge_json` в `.mcp.json`. Про #300 в теле коммита сказано буквально: *«${REPO_ROOT}
expansion in args remains tracked separately by Taiga #300»* — то есть упоминание-отсылка,
а не решение.

**Состояние сегодня:** `git grep 'REPO_ROOT'` по `apps/backend/src` в `origin/main` — **пусто**;
`${OS}` / `${PROFILE_ID}` не встречаются нигде в репозитории. В `renderer.py` нет ни функции
подстановки, ни блока документации по плейсхолдерам. (Совпадения `REPO_ROOT` в `apps/backend/tests`
и `scripts/` — это питоновская константа `REPO_ROOT = Path(...)`, к рендереру отношения не имеет.)

**Вердикт: НЕ ЗАКРЫВАТЬ.**

---

## #428 — split `/run-implementation` на phase-команды с ack-гейтами (P1)

**Просили:** 3 новых `command_spec` (`run-coder`/`run-tester`/`run-test-runner`, tier=full,
supports=[claude-code, codex]); каждая делегирует ровно одному сабагенту и печатает свой
маркер; `/run-implementation` переписан как комбо с гейтами; `tacticum-workflow` ×3 CLI
эмитит маркеры; версия 0.4.0 + CHANGELOG; тесты + регрессия на 4 маркера; зелёный
`check_profile_version_discipline.py`; засеяно на VPS как active.

**Что в коммите `510eae3a`:** 20 файлов, +496/−29. В `manifest.yaml` три новых `command_spec`,
tier=full, supports=[claude-code, codex]; тела `run-coder.md` / `run-tester.md` /
`run-test-runner.md` делегируют по одному сабагенту и завершаются обязательным маркером
(`PHASE_3_CODER_COMPLETE` / `PHASE_3_TESTER_COMPLETE` / `PHASE_4_RUNNER_COMPLETE`);
`run-implementation.md` содержит таблицу фаз со всеми 4 маркерами и формулировку «Missing any
marker = workflow FAILURE — surface it to the user; do not silently exit»; версия поднята до
`0.4.0`, CHANGELOG-запись на 41 строку.

**Чем подтверждается (прогоны сегодня, 2026-08-07):**
- worktree на `510eae3a`: `pytest tests/catalog/test_iva_brownfield_mail_profile.py` → **15 passed**
  (было 10 тестов до коммита, +5 новых регрессий, включая
  `test_run_implementation_has_ack_gate_markers` и `test_agent_bodies_emit_their_ack_markers`);
- `python scripts/check_profile_version_discipline.py` → `OK — 3 profile(s) clean.`, exit 0;
- worktree на `origin/main` (`1bc7f93c`, профиль уже 0.8.2): те же 15 тестов **passed** —
  функциональность не откатывалась;
- БД каталога на `tacticum_prod`: `iva-brownfield-mail @ 0.4.0`, `status=active`,
  `seeded_at 2026-05-28 10:05:16 UTC`; в этой версии лежат `run-coder`, `run-tester`,
  `run-test-runner` (`command_spec`, tier=full); в засеянном теле `run-implementation`
  присутствуют все **4/4** маркера.

Все пункты приёмки, включая последний («seeded on VPS, active в `profile_versions`»), закрыты.

**Вердикт: ЗАКРЫТЬ.**

---

## #429 — скилл `ui-mockup-match` (P2)

**Просили:** `skill_spec` `ui-mockup-match` (tier=full, supports=3 CLI); SKILL.md с триггерами,
контрактом входов, алгоритмом цикла, форматом вывода и анти-паттернами; опора на уже имеющийся
`playwright` MCP без новых MCP-серверов; заметка в теле `coder` ×3 CLI про
`mockup-match-report.md`; 0.4.0 + CHANGELOG; тесты + регрессия. Плюс 4 open questions, снятые
комментарием admin 2026-05-28 (семантика+DOM без pixel SSIM, 3 итерации, «write report and stop»,
только HTML-макеты) — реализация обязана следовать этим дефолтам.

**Что в коммите:** `SKILL.md` на 149 строк с разделами «Inputs (contract)», «Tools (depends on)»,
«Loop (max 3 iterations)» из 5 шагов, форматом `mockup-match-report.md` и разделом
«Anti-patterns». Все четыре разрешённых вопроса отражены буквально: «Diff (semantic + DOM only,
NEVER pixel SSIM)», кап 3 итерации, «No fail-loud» — при исчерпании пишется отчёт и стоп,
«HTML mockups only for this version». Новых MCP-серверов в диффе манифеста нет — добавлены
только 1 `skill_spec` и 3 `command_spec`; скилл ссылается на playwright «in this profile since
v0.3.1». Тела `coder` во всех трёх CLI-вариантах получили ссылку на `mockup-match-report.md`.

**Чем подтверждается:** те же прогоны, что и по #428 — 15/15 passed на `510eae3a` и на
`origin/main`, включая `test_ui_mockup_match_skill_present` (проверяет kind/tier/supports,
упоминание playwright, кап итераций и анти-паттерн по пикселям) и
`test_coder_body_references_mockup_match_report` (×3 CLI). В БД `tacticum_prod` в версии
`0.4.0` (active) присутствует ингредиент `ui-mockup-match`, `kind=skill_spec`, `tier=full`.

**Вердикт: ЗАКРЫТЬ.**

---

## Сводный блок

```
#230 · НЕ ЗАКРЫВАТЬ · коммит 1/5 по соседней задаче (ADR-0018) и прямо пишет «existing endpoints stay until #230 is picked up»; в origin/main seed/retract/tokens/audit до сих пор в platform/admin/router.py
#305 · НЕ ЗАКРЫВАТЬ · коммит откладывает #305 явным списком, обработки ValidationError нет; в origin/main жив сырой дамп catalog.py:543 detail=f"metadata invalid for kind={kind}: {exc}"
#304 · НЕ ЗАКРЫВАТЬ · в editors.tsx на origin/main ноль вхождений name=" и data-testid; коммит трогал только маршрутизацию редакторов
#303 · НЕ ЗАКРЫВАТЬ · коммит расширил статический абзац-подсказку, который задача просит убрать; textarea metadata по-прежнему инициализируется "{}"
#300 · НЕ ЗАКРЫВАТЬ · коммит про Taiga #310 (merge_json в .mcp.json) и сам пишет «${REPO_ROOT} expansion remains tracked separately by #300»; в apps/backend/src подстановки плейсхолдеров нет
#428 · ЗАКРЫТЬ · 3 phase-команды и 4 ack-маркера в манифесте и телах; pytest test_iva_brownfield_mail_profile.py 15 passed на 510eae3a и на origin/main, check_profile_version_discipline OK; в БД tacticum_prod 0.4.0 active (seeded 2026-05-28 10:05 UTC), 4/4 маркера в засеянном run-implementation
#429 · ЗАКРЫТЬ · skill_spec ui-mockup-match (tier=full, 3 CLI) + SKILL.md 149 строк по всем 4 снятым open questions, coder ×3 CLI ссылается на mockup-match-report.md; те же 15 passed, в БД tacticum_prod 0.4.0 ингредиент ui-mockup-match присутствует
```

---

ВЕРДИКТ: частично (2 из 7 — к закрытию, 5 — оставить открытыми; спорных нет)
Проверено: 7 задач Taiga (#230, #305, #304, #303, #300, #428, #429) против 4 коммитов в `origin/main` (`11e6b01a`, `570ae010`, `d8d44dcd`, `510eae3a`) — принадлежность коммита ветке, соответствие диффа тексту задачи, пункты приёмки, состояние `origin/main` на сегодня, и для #428/#429 — прогон тестов и факт засева в боевой БД каталога
Данные: 4/4 коммита — предки `origin/main`; #428/#429 — pytest `tests/catalog/test_iva_brownfield_mail_profile.py`: 10 тестов до коммита → **15 passed** после, и те же 15 passed на текущем `origin/main` (`1bc7f93c`); `check_profile_version_discipline.py` exit 0, «OK — 3 profile(s) clean»; в БД `tacticum_catalog` версия 0.4.0 `active`, seeded 2026-05-28 10:05:16 UTC, 5 из 5 ожидаемых ингредиентов на месте, 4/4 ack-маркера в засеянном теле `run-implementation`; #230/#305/#304/#303/#300 — 0 совпадений искомого в `origin/main` (`name="`/`data-testid` в editors.tsx — 0 из 978 строк; `REPO_ROOT` в `apps/backend/src` — 0; сырой `detail=f"metadata invalid…"` — жив, `catalog.py:543`; `/seed`,`/retract`,`/tokens`,`/audit` — живы в `platform/admin/router.py`)
Подтверждение: `git merge-base --is-ancestor <sha> origin/main` (exit 0 ×4) · `git show <sha>` / `git show origin/main:<file>` / `git grep origin/main` · worktree `verify-triage1-510eae3a` и `verify-triage1-main` (созданы и сняты, общее дерево не тронуто) · `pytest -q tests/catalog/test_iva_brownfield_mail_profile.py` → `15 passed in 0.17s` · `python scripts/check_profile_version_discipline.py` → `OK — 3 profile(s) clean.` · `docker exec tacticum-postgres-1 psql -U catalog -d tacticum_catalog` на `tacticum_prod` (SELECT по `profile_versions` и `ingredients`) · дата прогона 2026-08-07
НЕ проверено: (1) поведение в рантайме — что Codex/Claude действительно перестал обрывать цепочку после первого сабагента и что скилл `ui-mockup-match` реально сходится за ≤3 итерации: тесты проверяют присутствие маркеров и текста в телах, а не исполнение (полевой прогон профиля не делал); (2) на каком именно хосте имелся в виду «dev VPS» из приёмки — мерил на `tacticum_prod` (описан как «Прод tacticum-dev, каталог профилей»), другого каталожного стенда в списке серверов нет; (3) полный бэкенд-сьют не гонял — только файл `test_iva_brownfield_mail_profile.py`, поэтому «ничего не сломано в целом» утверждать не могу; (4) admin_web не открывал в браузере — по #303/#304/#305 вывод сделан по исходникам `origin/main`, Playwright-снапшот не снимал; (5) не искал, не закрыты ли #230/#305/#304/#303/#300 коммитами **без** ссылки на номер — искал по конечному состоянию кода, что сильнее, но по #230 не проверял, не переехали ли эндпоинты частично куда-то ещё сверх найденного файла.