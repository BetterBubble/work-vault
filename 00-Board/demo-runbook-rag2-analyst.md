---
title: demo-runbook-rag2-analyst
type: note
permalink: tacticum/00-board/demo-runbook-rag2-analyst
status: draft
role: lead
tags:
- runbook
- rag2
- demo
- roscosmos
- analyst
---

# Демо-ранбук RAG#2 /analyst (Роскосмос 16:00)

## КАНОН 6 демо-вопросов (ТОЧНЫЕ строки — единый источник для прогрев-кэша И чипов-пресетов)
Обновлено 2026-07-22 после приёмки: перешли на ЭТАЛОННЫЕ формулировки из run-of-show (дают лучший retrieval; смешанный «Календарь и Диск» ронял Диск). 10 вопросов (В1+ключевые В2). Прогрев-скрипт и чипы-пресеты ОБЯЗАНЫ использовать эти строки дословно.

1. `Что по SSO / IVA ID сделано, что в работе, что не начато?`
2. `Мульти-провайдерный SSO — статус, владелец и для какого заказчика?`  (killer, IVAONE-12438)
3. `Статус федеративных чатов, каналов и тредов в мессенджере?`
4. `Что по ВКС сделано — транскрибация, демонстрация документов?`
5. `Что по звонкам сделано, что в работе?`
6. `Как звонки покрыты тестами?`  (честный тест-слайд, 26/63≈41%)
7. `Расскажи архитектуру Диска — One Disk и DS, что с DLP?`  (Диск отдельно — сильнейший акт)
8. `Статус календаря — множественные календари, планировщик, FreeBusy?`
9. `Статус почты и миграции XIMSS→JUMP?`
10. `Что по Terra — суммаризация, перевод? Что заблокировано?`

## Q7 — живое отклонение (единственный живой triva на демо, приемлемо «Синтезирую…»)
Пул проверенных:
- `Сравни риски SSO и федерации мессенджера к сроку`
- `Дай план доведения любого partial-требования по звонкам`

## Killer-акценты (порядок показа)
SSO первым (Роскосмос на экране: `IVAONE-12438` Готово, «Заказчик РосКосмос AD+ALDpro»). Сильнейшие: Диск (архитектура) + Terra (AI) + SSO. Честный тест-слайд — Звонки (26/63 ≈ 41%).

## Чего ИЗБЕГАТЬ вживую (даст пустой/плохой ответ)
Запись/скачивание ВКС (Rejected+403), настройка SIP-trunk, ссылка переводчика Terra (absent), `who_to_involve(system_id)` (пусто — владельцев берём из текста требования).

## Чеклист дня (перед 16:00)
- [ ] Финальный деплой (latency+cache ветка) на прод, verify.
- [ ] Прогрев кэша: `rag2_warm_demo.py` (6 актов + triva warm-up от cold-start).
- [ ] Перемер живьём: 6 актов из кэша мгновенно, Q7 живой < ~20с, без `synthesis_failed`.
- [ ] triva-туннель жив (SPOF), вход tenant=iva у презентующего.
- [ ] `/context` и `/iva-docs` живы (регресс).

## Открытые числа
- `as_of=10.07` — данные реально от 10.07 (проверяем на ретриве); не выдавать 22.07.
- Звонки: свести к 26/63 ≈ 41% (консистентный знаменатель).

## Тех-статус 22.07
**Обновлено ~14:15.** vLLM **triva ЛЁГ** в контуре (Connection refused) → синтез /analyst переведён на gateway **`tacticum/cheap`** (не-reasoning; smart/chat/flash обрубались reasoning-токенами). Изоляция конфига `HELM_RAG2_SYNTH_LLM_*`, RAG#1 не тронут. Задеплоено на прод (main + env-обход).

**Приёмка 10 актов (живой прод):** синтез `failed=false` на всех; топология+5 цитат везде; регресс чист. Тесты 6/10 (SSO/Диск — норма; Почта — снапшот by_feature=6 не покрыл). Латентность без кэша ~10-13с (Звонки 28с, Почта 19с) → закрывается прогревом. Звонки-тесты подтянулись (96 ТК) после ре-ингеста снапшота.

**Осталось:** свести историю диалога + design-режим (ЕСИА 1b) → финал-деплой (миграция conversation) → прогрев 10 актов → финальная сверка.

**Токен приёмки:** `HELM_PROJECTHUB_TOKEN` (tenant iva) на `/api/rag2/answer`. Прогрев вручную = 10 канон-вопросов curl'ом (наполняет in-process кэш; сбрасывается при каждом деплое → прогрев ФИНАЛЬНЫМ шагом).
