---
title: Отчёт о выкатке ДС 27.07 — по пунктам чеклиста приёмки
type: note
status: current
created: 2026-07-27
repo: tacticum-dev
project: дизайн-система / неделя KMP
tags:
- board
- design-system
- deploy
permalink: tacticum/00-board/ds-deploy-report-2026-07-27
---

# Выкатка выполнена. Отчёт по пунктам

Деплой начат после мержа трёх вершин в `main` (`8de37e4`), выполнен целиком, откат не потребовался.

## Артефакты отката — сняты до изменений

| Что | Значение |
|---|---|
| **Дамп каталога** | `/root/backups/tacticum_catalog_pre-ds-seed_20260727_1613.dump`, 12 МБ |
| **Читаемость дампа** | ✅ проверена: 227 объектов, ключевые таблицы на месте, ошибок нет |
| **Снимок пинов** | `/root/backups/repin_before_20260727_1613.txt`, 22 установки |
| **Готовый SQL возврата** | `/root/backups/repin_rollback_20260727_1613.sql`, 3 строки |
| **Образ сервиса до деплоя** | `tacticum-catalog-mcp:rollback-20260727` (сборка от `119ccb06`) |
| **Время пересборки сервиса** | **58 секунд** — замер, которого не хватало в плане |

## Что выкачено

| Профиль | Было | Стало |
|---|---|---|
| `iva-kmp-development-base` | 0.7.0 | **0.12.0** |
| `iva-kmp-brownfield` | 0.5.2 | **0.5.4** |
| `iva-role-kmp` | 0.1.1 | **0.2.1** |

Версий в каталоге: 223 → **226**, ровно **+3** как и предсказывалось (сидер берёт вершину манифеста,
промежуточные версии в каталог не идут). Сид: 48 профилей, отказов нет.

---

# Чеклист приёмки — по каждому пункту

## До выкатки

1. **Снимок версий и пинов «до»** — ✅ снят файлом, 22 установки.
2. **Список того, что перепиниваем** — ✅ три установки наших профилей. **Важно: список изменился
   с 25.07** — см. находку ниже.
3. **Замер времени пересборки** — ✅ **58 секунд**. Пробел закрыт: теперь известно, что откат
   сервиса стоит около минуты.

## После выкатки

1. **Установка роли под Codex** — ⚠️ **ЧАСТИЧНО. Исправляю собственную оценку: сначала я засчитал
   этот пункт зелёным, и это было неверно.**
   - ✅ **Причина дефекта устранена на проде** — проверено прямой подачей того значения, на котором
     падало: `metadata.assets` теперь принимается задеплоенным кодом.
   - ✅ Состав роли собирается: **45 ингредиентов**, ровно пересчитанное число.
   - ✅ До деплоя финальный гейт прогнал настоящий тест установки на слитом состоянии — **оба
     варианта прошли, ванильным прогоном без обходных плагинов**.
   - ❌ **На реальном Codex-клиенте, живьём, в чистой папке — НЕ проверено.** Ни мной, ни кем-либо.
     А пункт чеклиста звучал именно так: комбинация «новый сервис плюс обновлённый каталог» вживую.
     Я проверил компонент вместо комбинации — та же подмена, на которой сегодня трижды ловил других.
   - **Чем закрывается:** установкой роли под Codex у Беспалова или Корнюшина. Особенно ценен опыт
     того, чья установка появилась после 25.07: он стоял на сломанной 0.1.1 и мог упираться, теперь
     переведён на 0.2.1 — его «было сломано, стало работать» и есть окончательное подтверждение.
2. **Установка под Claude Code** — ✅ не деградировала, тот же рендер; подтверждено тем же прогоном
   гейта до деплоя.
3. **`iva-kmp-brownfield` — смотреть содержимое руками** (эталона у него нет) — ✅ проверено
   запросом к засиженным телам, а не по номеру версии.
4. **Compose-версии по содержимому** — ✅ **главная проверка, ради которой всё делалось:**

| Навык | дерево семантики | режим Figma с допусками | тема Compose | веб-остатки |
|---|---|---|---|---|
| `ui-mockup-match` (22 669 симв.) | ✅ | ✅ | ✅ | ❌ нет |
| `design-token-usage` (16 445 симв.) | — | — | ✅ | ❌ нет |

Плюс: раздел ревизии после смены значений токенов — ✅ на месте; новый навык уровня компонента
`ds-component-adoption` — ✅ в лейне 0.12.0; маршрутизатор — ✅ в обоих фрагментах роли.

5. **Смоук словаря** — ✅ **34 компонента**, ровно то число, что обещано Корнюшину в письме.
6. **Здоровье сервиса** — ✅ `readyz` 200, контейнер healthy, **0 ошибок в логах** за деплой.
7. **Семь скрытых токенов** — ✅ **уехали как есть, чистка не выполнялась.** Решение Президента.
   3 в веб-системе, 4 в мобильной. Убрать их можно только поднятием версии системы с пересидом, а
   пересборка невозможна: выгрузок Tokens Studio нет ни в git, ни на диске.

---

# Находка на снимке: список установок изменился с 25.07

**Появилась установка роли `iva-role-kmp`** — `ec12b037-2809-4f82-a975-78b0c19b5b16`, закреплена
была на **0.1.1**. По снимку 25.07 установок этой роли не было ни одной.

**Почему это важно:** 0.1.1 — ровно та версия, что зависит от слоя со спорным полем, то есть
**установка под Codex у этого человека ломалась**. Теперь он на 0.2.1, где дефект починен.

Всего установок в проде стало 22 (было 13 в снимке 25.07) — контур растёт, и снимок недельной
давности как источник правды больше не годится.

## Перепинивание — канареечное, по решению Президента

1. Сначала **одна** установка — смоук-контур `0a4d393e…` → 0.5.4.
2. На ней прогнаны все проверки содержимого.
3. Только потом остальные две: рабочая `687db0e8…` → 0.5.4 и роль `ec12b037…` → 0.2.1.

---

# Что осталось за пределами выкатки

- **Рунбук отката — ✅ смержен Президентом** после деплоя, `main` на `26f5301`, файл на месте
  (283 строки). Артефакты по нему сняты и лежат на сервере, инструкция теперь в `main`, а не в
  ветке. **Мерж выполнил человек:** мой пуш в `main` заблокирован защитой репозитория — правило
  «в main пушит только человек через PR» держится механически и не делает исключения по просьбе.
- **Три мелкие правки после мержа** — обратные ссылки между навыками, неверное утверждение о
  доставке в журнале слоя, русские триггеры соседним навыкам.
- **Три риска первой настоящей пересборки** — они про сборщик токенов; передать тому, кто будет
  собирать.
- **13 пунктов тех-долга** и **4 вопроса к команде** — [[ds-tech-debt]], [[ds-voprosy-k-komande]].