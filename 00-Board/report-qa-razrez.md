---
title: report-qa-razrez — состояние работы lead-qa
type: report
status: current
created: 2026-07-29 16:42
updated: 2026-07-29 22:43
repo: tacticum-dev
project: tacticum-dev / профили / QA
tags:
- board
- qa
- report
permalink: tacticum/00-board/report-qa-razrez
---

# Состояние lead-qa

## Где стоим

**Работа закончена, обе ветки готовы, наружу не отправлено.** Worktree
`~/tacticum/tacticum-dev-qa-web`, ветка `feat/qa-role-web`, HEAD `e8c815f`, дерево чистое.

1. **`refactor/qa-lane-split`** — разрез монолита на 4 лейна, роль `iva-role-qa` 0.6.0. 14 коммитов.
2. **`feat/qa-role-web`** (**от первой**) — роль `iva-role-qa-web` под OneWeb + расцепление
   процессного лейна со стеком. 15 коммитов.

**Мержить строго по порядку.** Гейт **GO** (4 захода: NO-GO → закрыто → GO → NO-GO → закрыто → GO).

**Числа (сверены лидом):** `tests=1054 failures=0 errors=0 skipped=0` · ссылки обеих ролей чисто
(4 пары) · `paths --all-profiles` чисто (102) · дисциплина версий 57 при обеих базах · golden
`iva-role-qa` 80→80 путей, `iva-role-qa-web` 74→74, пропаж ноль.

**Живой прогон установки не автором — доставка целая.** Обе роли × оба CLI, изолированная БД:
пустых файлов 0, неподставленных `{ingredient_id}` 0, склейки нет, 186 ссылок без битых.

## Следующий шаг — ждёт Президента

1. **Пуш + PR** обеих веток по порядку (тексты готовы, версии сверены).
2. **Приёмка web-роли** — живой прогон возможен только у Евгения (нужен стенд `one-web` + учётки).
3. **Внутренний IP `10.6.0.219`** в selenium-лейне — решение, если репозиторий публичный.

## Что заблокировано

Роли **mobile** и **desktop** — до ответов на открытые вопросы: компоненты KMP в одном
репозитории? · «различия на уровне кода» — про адресацию элементов тоже? Пока открыты, число
ролей не определить. Десктоп вообще не pytest — отдельная работа.

## Что отсеяно (не делать)

Ничего не удаляем (решение Президента) · `craft-stack` не переименовываем · монолит
`iva-qa-autotest-base` 0.5.1 заморожен и не правится · `:62` в `jira-issue-autotest` (аппрув перед
push) не трогаем — это граница, а не трение · golden-дубль контракта `{stack}` в роль не пишем —
он в лейне.

## Ключевые факты

- **Класс дефекта дня: утверждение верно для одного стека и подано как общее.** Шесть штук
  (`--env`, браузерные флаги, `--headed`, `-p no:randomly`, «Браузер один», Block 3). Пока стек
  был один — **неотличимы от истинных**. Вторая роль и была той проверкой.
- **Два блокера, которых зелёные проверки не видели:** `--env` в OneWeb обязателен (без дефолта →
  `config[None]` → `KeyError`; фикстура **сессионная**, падает весь прогон — так выглядел отказ
  28.07) · на **codex** у web-роли не было драйвера разведки вообще (`install --skills` наполняет
  только `.claude/skills/`, microsoft/playwright#39317).
- **Зелёное ≠ проверено, трижды за два дня.** Правило: новая проверка не считается работающей,
  пока её не сломали мутацией.
- **Утверждение живёт в копиях, правится в одной** — копии ищутся по смыслу, не грепом.
- **Порядок сида обязателен: лейны → роль.** `depends_on` замерзает на сиде; проверено фактом.
- Прод не задет: там роль **0.5.5 на монолите**.
- **Чужой платформенный дефект (отсигнален ГД):** рендерер codex пишет `~/.codex/config.toml`
  невалидным TOML (`env` как JSON), до 12 профилей, включая роль аналитика на проде.

## Ссылки

[[qa-тексты-PR-2026-07-29]] — тексты PR · [[explore-qa-gaps-2026-07-29]] — ревизия ·
[[verify-qa-install-2026-07-29]] — живой прогон · [[gate-qa-web-2026-07-29]] — гейт ·
[[требования Евгения (ИВА) к QA-профилю — дословно]] — канон ·
[[QA — карта поверхностей, стеков и ролей]] · [[plan-qa-razrez-2026-07-29]] (не трогать)
