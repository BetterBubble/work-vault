---
title: gate-qa-final-2026-07-28
type: note
status: current
created: 2026-07-28
tags:
- board
- qa
- gate
permalink: tacticum/00-board/gate-qa-final-2026-07-28
---

# Финальный гейт: вся работа по QA-профилю (две ветки, ничего не пушено)

Проверял controller, только чтение. Ветки: `feat/qa-canon-delivery` (`~/tacticum/tacticum-dev-qa-canon`,
HEAD **`d792969`**, 25 коммитов) и `chore/qa-acceptance-tooling` (`~/tacticum/tacticum-dev-qa-gate`,
HEAD `ed63b6a`, 4 коммита). База — `main` `9be499d`.

**HEAD двигался во время проверки трижды** — `fc09c2f` → `a44ae76` → `625a4fb` → `d792969`;
ветка 2 тоже выросла (`30d123e` → `ed63b6a`). Ниже разобраны все
сдвиги; всё, что не оговорено отдельно, снято на `d792969` / `ed63b6a`.

### Сдвиг 3: `625a4fb` → `d792969` — инструмент приёмки был сломан, числа пересняты

Разбор — в разделе «Финальная дельта» ниже. **Главное: мой собственный прежний вывод про
`PROGRESS.md` («рантайм-артефакт, ложное срабатывание») был неверен**, и я его снимаю.

### Сдвиг 2: `a44ae76` → `625a4fb` — блокер 1 закрыт

**Коммитов три, а не два** (тимлид назвал два): `9d7da52` (исполняемые selenoid-команды +
`__pycache__` в gitignore), `8d3b87c` (паки роли, роль 0.5.4), `625a4fb` (остаток `tools/testops`
— 10). Разбор — в разделе «Закрытие блокера 1» ниже.

### Сдвиг 1: `fc09c2f` → `a44ae76` Проверил сам, а не со слов:
`git diff --name-status fc09c2f..a44ae76` = **один файл, `templates/iva-qa-autotest-base/CHANGELOG.md`**,
9 вставок / 5 удалений — уточнение счёта вызовов (16 → 18 с раскладкой) и формулировка про
`run-tests`. CHANGELOG **ингредиентом не является и в дерево установки не попадает** (проверено:
в `manifest.yaml` его нет ни в одном `body_path`, в собранном дереве файла нет). Пересобрал дерево
доставки на `a44ae76` и сравнил с деревом `fc09c2f` — **расхождений 0**. Все факты ниже сняты
на `a44ae76`; прогоны ссылок, рендера и тестов повторены на нём, а затем ещё раз на `625a4fb`.
Предыдущий гейт (половина работы): [[gate-qa-canon-2026-07-28]]. План: [[plan-qa-rabota-2026-07-28]].
Первоисточник: [[требования Евгения (ИВА) к QA-профилю — дословно]].

## ВЕРДИКТ

- **GO — на показ Президенту.** Безоговорочно. Все три блокера, найденные гейтом за день, закрыты
  и перепроверены мной на собранном дереве, а не по отчётам.
- **«Приёмка пройдена» — теперь это можно говорить.** Критерий плана «битых ссылок по дереву
  установки обоих CLI = 0» **выполнен**: 0, перепроверено исправленным инструментом на трёх точках
  истории. Весь день это было единственное невыполнимое число.
- **Приёмка стала гейтом.** Golden перегенерирован, два теста `e2e_install` позеленели, проверка
  ссылок подключена в CI двумя шагами. Шаг 0 плана, открытый с утра, закрыт.
- **Остаётся один пункт с последствиями, и он процедурный:** порядок сида (лейн 0.5.0 → потом
  роль 0.5.4). В коде он не живёт и проверке до сида не поддаётся — место ему в runbook, а не
  в ветке. Остальное открытое (недоказанная универсальность, красный `e2e_install` теми же
  тестами, что на `main`) — это границы знания, а не недоделки.

## Механический гейт (детерминированный, приложен дословно)

`feat/qa-canon-delivery`, `--scope templates/iva-qa-autotest-base/** + templates/iva-role-qa/**`,
`--metrics` из критериев приёмки плана:

```
── детерминированный гейт ──
  ✔ ветка не main/master  — ветка: feat/qa-canon-delivery
  ✔ есть база для сравнения  — база: origin/main, файлов в диффе: 71
  ✔ рабочее дерево чистое  — незакоммиченного: 0
  ✔ нет файлов-секретов в диффе  — чисто
  ✔ нет мусора в диффе  — чисто
  ✔ нет похожего на секрет в добавленных строках  — чисто
  ✔ нет AI-подписей в коммитах  — чисто
  ✔ коммиты не пустые  — коммитов: 25
  ✔ изменения в пределах скоупа плана  — 71 файлов, все в скоупе
  ✔ метрика битых ссылок по дереву установки (оба CLI)  — 0 против порога 0
  ✔ метрика дефектов путей доставки (режим paths)  — 0 против порога 0
  ✔ метрика дублей target_file  — 0 против порога 0
  ✔ метрика плейсхолдеров в путях доставки  — 0 против порога 0
  ✔ метрика at-test-* стендов в дереве доставки  — 0 против порога 0
  ✔ метрика $HOME/Projects в дереве доставки  — 0 против порога 0
  ✔ метрика живых вызовов python -m tools.testops  — 0 против порога 0
  ✔ метрика исполняемых команд на selenoid  — 0 против порога 0
  ✔ метрика Selenium/one-web в паках роли  — 0 против порога 0
  ✔ метрика ссылок на .kit/schema.md  — 0 против порога 0
  ✗ метрика one-web без -kmp в дереве доставки (T10)  — 6 против порога 0
  ✔ метрика плейсхолдеров в ключах golden  — 0 против порога 0
  ✔ метрика чужих golden тронуто  — 0 против порога 0
  ✔ метрика новых падений e2e_install против базы  — 0 против порога 0
  ✔ метрика tests/catalog passed  — 710 против порога 710
  ✔ метрика agent_spec сохранено  — 3 против порога 3
  ✔ метрика расхождение sha256 .agent-kit между CLI  — 0 против порога 0

ЕСТЬ НАРУШЕНИЯ
```

`chore/qa-acceptance-tooling` — **9 из 9, нарушений нет**, 2 файла в скоупе, коммитов 4.

Единственное ✗ — метрика `one-web` без `-kmp` = 6. Все шесть разобраны ниже и все шесть законны:
это «референс-инсталляция one-web» в вербатим-клиенте TestOps, то есть утверждение о том,
на какой инсталляции Allure подтверждены эндпоинты. Факт, а не сращённость с репо.
**Порог 0 здесь мой, а не плановый** — план требовал ноль «в живых телах», и в живых телах ноль.

**Все метрики приёмки плана — зелёные, включая ту, что была красной весь день:** битых ссылок
**0** при пороге 0. Красными в прошлых редакциях этого отчёта были семь метрик; закрыты все. Значения метрик я снял сам,
своим прогоном; порог — из раздела «Приёмка» плана дословно.

## 1. Блокеры прошлого гейта — оба закрыты, проверено своим прогоном

### (а) Метрика в поставляемом CHANGELOG — теперь честная

Прогнал доставленный `check_install_links.py` по трём точкам, каждый раз на настоящем дереве
шаблонов соответствующего коммита:

| Точка | Битых ссылок | Что заявлено в CHANGELOG |
|---|---|---|
| база `9be499d` | **88** | «88 → 26» в секции `[0.4.0]` |
| `87ade2d` (конец 0.4.0) | **26** | — |
| HEAD `fc09c2f` | **20** | «битые ссылки: 26 → 20» в секции `[0.5.0]` |

**Все три числа воспроизведены. Ложного «194 → 15» в файле больше нет**, конфликтующего второго
«до» — тоже. Блокер закрыт по существу, не косметически.

### (б) Роль бампнута — да; ребро «переморожено» — только при верном порядке сида

- Роль `iva-role-qa` `0.5.1` → **`0.5.3`**; лейн `0.5.0`. CHANGELOG роли описывает оба бампа
  (0.5.2 и 0.5.3) и честно говорит, что ингредиенты роли не менялись.
- **Но `depends_on` в манифесте роли версии НЕ несёт** — это плоский список `profile_id`
  (`templates/iva-role-qa/manifest.yaml:29-32`). Версию выбирает сид:
  `seed_profile.py` берёт `ProfileVersion.status == "active"` с `order_by(seeded_at.desc()).limit(1)`
  и замораживает конкретный `base_version_id`.

**Отсюда важное, чего нет ни в одном отчёте:** «ребро ведёт на 0.5.0» — это **не свойство ветки**,
проверить его в git нельзя. Это свойство **процедуры доставки**. Если роль `0.5.3` засидят
раньше лейна `0.5.0`, ребро свяжется с `0.4.0`, и весь шаг 1б (клиент TestOps, `tms-read`,
`run-tests`) до инженера не доедет — при полностью зелёном гейте и зелёном бампе.
**Порядок обязателен: сначала сид лейна 0.5.0, потом сид роли 0.5.3.** Записать в runbook доставки.

## 2. Клиент TestOps реально работает в дереве установки — проверено живьём

Собрал дерево установки роли настоящим рендерером каталога (`render_for_claude_code` /
`render_for_codex`, композиция по `depends_on`), материализовал на диск, запустил.

```
claude-code: действий=81  write_file=77  уникальных путей=77  дублей=0  плейсхолдеров=0
codex:       действий=78  write_file=78  уникальных путей=78  дублей=0  плейсхолдеров=0
```

- **`.agent-kit/` — 55 файлов, sha256 совпадают между клиентами до последнего: расхождений 0.**
  Один комплект канона на оба CLI, как и задумано.
- **Шим стартует без `.kit`** — в дереве установки каталога `.kit` нет вовсе:

```
$ python ".agent-kit/tms/scripts/testops_cli.py" --help
usage: testops [-h] {get,cfv,search,cases,list,launch,launches,flake} ...
Read-only REST к Allure TestOps
```

  То же самое из дерева codex, включая подкоманды (`launch --help` отдаёт свои опции).
  `params.load()` без `.kit/answers.toml` возвращает `DEFAULTS` — проверено импортом.
- **Verbatim: 6 файлов из 7 побайтно совпадают с `kit-new/kit-main/tms/scripts/`.**
  `params.py` — **побайтно идентичен**, как и требовалось отдельно.

**Одна оговорка, которую надо сказать вслух:** клиент требует `requests` и `pyyaml`
в python проекта — без них шим падает `ModuleNotFoundError` ещё до `--help`. Это не дефект:
`post_install_notes` называет требование прямо (`manifest.yaml:900`), а в `one-web-kmp`
`requirements.txt` обе библиотеки есть (`PyYAML==6.0.2`, `requests`). Прогон выше сделан
на venv, собранном ровно из этих двух пакетов, — то есть на среде инженера он заведётся.

## 3. `run-tests` очищен — по существу да, по буквальному grep нет (проверка на `fc09c2f`, в силе)

В доставленном теле (идентично на обоих CLI, 15616 байт):

| Что искали | Найдено |
|---|---|
| `browser_win_*` / `browser_lin_*` / `browser_mac_*` | **0** |
| `selenoid` | **0** |
| `at-test-1-one` / `at-test-3-one` / `at-test-` | **0** |
| резолв стенда по имени ветки | **0**, и прямо запрещён |
| `browser_` / `--browser-tag` / `--local` | **8 вхождений — все запретительные** |

Восемь вхождений разобраны поимённо, все четыре места:

- `:25` «Браузерных маркеров (`-m browser_*`), тегов версий, grid- и local-режимов в нём **не**…»
- `:102` таблица: «`-m browser_*`, `--browser-tag`, `--local` | таких маркеров и опций в проекте
  нет; со `--strict-markers` это ошибка прогона»
- `:209-210` «❌ **Не передавать** маркеры и опции, которых нет в проекте»
- `:143` `browser_url` — это ключ `config.yaml` самого `one-web-kmp`, легитимное имя

**Вывод: тело чище, чем требовал критерий.** Критерий приёмки («grep = 0») написан наивно — он
не отличает употребление от запрета, а запрет здесь полезнее молчания: он объясняет агенту,
почему нельзя. Ставлю ✔ по существу и фиксирую, что **докладывать «grep = 0» было бы ложью**.

Отдельно: `:211` «❌ Не зашивать имена стендов в команду и не резолвить стенд по имени ветки»,
`:60-66` «`--env` не передаём», дефолт из conftest.

**Маркеры сверены с целевым репо.** Все, что тело называет —
`suite_smoke`, `suite_regress_simple`, `suite_regress_advanced`, `suite_regress`,
`parallel_unsafe` — **есть в `pytest.ini` `one-web-kmp`**. Маркеров `browser_*` там нет вообще,
и стоит `--strict-markers` — то есть прежнее тело падало гарантированно, а новое не упадёт
по этой причине.

## 4. Субагенты и делегирование — целы, потери нет

- `agent_spec`: **3 на базе, 3 на HEAD**, те же `codebase-analyst` / `dom-explorer` / `code-writer`.
- `ingredients/agents-codex/*.toml`: все три на месте; дифф **только путевой**
  (`.claude/skills/craft-stack/` → `.agent-kit/craft/`), предметное содержание не тронуто.
- Счётчики `spawn_agent|wait_agent|close_agent` пофайлово против `9be499d`:

```
OK  skills-codex/batch-autotest/SKILL.md     HEAD=15 BASE=15
OK  skills-codex/fix-failed-test/SKILL.md    HEAD=18 BASE=18
OK  skills-codex/write-autotest/SKILL.md     HEAD=12 BASE=12
OK  manifest.yaml                            HEAD=14 BASE=14
OK  README.md                                HEAD=10 BASE=10
ДИФФ CHANGELOG.md                            HEAD=11 BASE=8   (проза новой записи)
```

Единственный прирост — в CHANGELOG. **Механизм делегирования не потерян нигде.**
Оба клиента получают `.claude/agents/*.md` и `.codex/agents/*.toml` — проверено на дереве.

## 5. Чеклист Т1-Т10 по первоисточнику — состояние на `fc09c2f`

> ⚠️ **Снимок промежуточного состояния, оставлен для истории проверки.** Актуальная сверка Т1-Т10
> на финальном `d792969` — в разделе «ЧТО ЗАКРЫТО И ЧТО ОСТАЛОСЬ ПО ТРЕБОВАНИЯМ ИНЖЕНЕРА» ниже.

| № | Состояние | Чем закрыто / что осталось |
|---|---|---|
| **Т1** клиент TestOps в поставке | **ЗАКРЫТ** | пакет 6 файлов + шим едут в `.agent-kit/tms/scripts/`, стартуют из дерева установки без `.kit` — проверено запуском |
| **Т2** ничего не требовать от репо | **закрыт в своей части** | `tools/testops` из живых тел ушёл полностью: было 48 вхождений, осталось 10 — **все в CHANGELOG и двух поясняющих комментариях манифеста**, ни одного вызова. README и `post_install_notes` переписаны: «Клиент Allure TestOps от репозитория проекта НЕ требуется». **Остаётся требуемым:** venv с `autocore`, внешние CLI (`pytest`/`playwright-cli`/`allurectl`/`glab`), конвенция `.tasks/`, PostToolUse-хук — это среда, а не контент репо, и названо честно |
| **Т3** reference-файлы доезжают | **ЗАКРЫТ** | 55 файлов в `.agent-kit/`, один комплект на оба CLI |
| **Т4** `.kit` не требовать | **закрыт по обязательности, остаток описательный** | императивов «заведи `.kit`» больше нет; `post_install_notes` прямо: «Не ведёт — заводить `.kit` не нужно». Осталось 25 вхождений в 17 доставляемых файлах, из них два проблемных — см. ниже |
| **Т5** `AUT_OVERVIEW.md` не требовать | **ЗАКРЫТ** | 3 вхождения, все с «опциональна / если проект её ведёт»; из предпосылок манифеста убран |
| **Т6** локальный запуск | **ЗАКРЫТ в `run-tests`** | сетевые стенды, грид, браузерная матрица убраны; `--env` не передаётся; добавлен протокол STAND_INCIDENT |
| **Т7** `at-test-*` не использовать | **частично** | в `run-tests` — 0. В дереве доставки осталось **10 вхождений**: `playwright-cli` (5), `prepare-mr-branch` (3), `jira-issue-autotest` (1), `.agent-kit/craft/.../output-blocks.md` (1) |
| ~~Т8~~ | снят Президентом | — |
| **Т9** цельное состояние | решено планом как поимённый импорт | расхождение с буквой вынесено вопросом, не решено молча. `params.py` побайтно из `kit` — это и есть цельность там, где она проверяема |
| **Т10** не сращён с репо | **сдвиг большой, не закрыт** | см. блокер 1 и остатки ниже |

**Поправка к моему же числу.** В первой редакции этого отчёта стояло «`one-web` без `-kmp` — 12».
Правильное число — **9**. Двенадцать получились из-за моей оснастки: материализатор дерева
я запустил дважды в один каталог, и `CLAUDE.md` (`merge_strategy: append_section`) прирос сам
к себе — три строки посчитались дважды. Пересобрал в чистый каталог, сверил размер
(9842 → 4921 байт). Остальные метрики (`.kit` 25, `$HOME/Projects` 30, `at-test-` 10,
`.agent-kit` 55 файлов) не затронуты — это не append-файлы, их sha256 в обоих деревьях совпали.

### Т4 — два оставшихся проблемных места (не блокер, правка на две строки)

`write-autotest/SKILL.md:23` и `fix-failed-test/SKILL.md:69` ссылаются на
**`.kit/schema.md` проекта** как на источник контракта обязательного делегирования. Файла
у инженера не будет. Контракт тут же изложен своими словами, так что агент не встанет, — но это
ссылка на авторитет в том самом инструменте, которого «быть не должно».

Ещё три файла — тела субагентов — говорят «Стек проекта — параметр `stack` секции `[craft]`
в `.kit/answers.toml`» **без оговорки «если проект ведёт базу kit»**, которую в 0.4.0 добавили
шести файлам канона. Три тела субагентов правку не получили.

## 5-бис. Живых вызовов `python -m tools.testops` в лейне нет — смотрел глазами

Отдельный пункт, добавленный тимлидом: легко перепутать описание дефекта с инструкцией его
повторить. Прочитал **все 10 оставшихся упоминаний**, не грепом — по одному, с контекстом.

**В CHANGELOG — 8, ни одного живого вызова:**

- `:10` «Тела **звали** `python -m tools.testops …`» — прошедшее время, тут же: модуля в
  `one-web-kmp` **нет**;
- `:21` «Вызовы переписаны — все 18: **с** `… -m tools.testops <оп>` **на**
  `… ".agent-kit/tms/scripts/testops_cli.py" <оп>`» — форма «с X на Y», направление однозначно;
- `:24` «Всего упоминаний `tools/testops` в лейне **было** 50 в 15 файлах» — прошедшее;
- `:87` — запись версии `[0.4.0]` про то, что `post_install_notes` приведены к факту;
- `:183`, `:350`, `:376`, `:378` — записи версий **`[0.3.0]`, `[0.2.0]` и `[0.1.0]`**.

Две последние — единственные в **настоящем** времени: «лейн привязан к репо one-web-kmp
(autocore/venv/`tools.testops`/glab/CI)» и «Allure TestOps через read-only модуль `tools/testops`
**в самом репо**». Проверил, под каким заголовком они стоят: обе внутри `## [0.1.0] — 2026-07-21`,
то есть это запись о том, каким лейн **был** в 0.1.0. По Keep a Changelog прошлые записи не
переписывают — формально верно. Замечу лишь, что тремя строками ниже, в той же секции, устаревшее
утверждение помечено «**ФЛАГ (снят 2026-07-23)**»; у этих двух такой пометки нет, хотя приём
в файле уже заведён.

**В манифесте — 2, оба поясняющие и оба в прошедшем времени:**

- `:268` «…до этого переноса тела звали **несуществующий** `python -m tools.testops` из репо
  потребителя» — прямо в комментарии к ингредиенту `tms-read`;
- `:792` «До этого тела звали `python -m tools.testops …`, **которого в `one-web-kmp` нет**».

**Вывод: живых вызовов ноль, ни одно упоминание не читается как рекомендация.** Пункт закрыт.

**Одна неточность в числе — и она в поставляемом файле.** CHANGELOG `:24` утверждает: «оставшиеся
**9** это описание самого дефекта в CHANGELOG и два поясняющих комментария в манифесте». Фактически
**10**: восемь в CHANGELOG (не семь) + два в манифесте. Считал обе формы, `tools.testops`
и `tools/testops`, по 4 каждой. Смысл верен, арифметика на единицу мимо. Само по себе пустяк —
но это второй раз подряд, когда в поставляемый CHANGELOG уезжает непроверенное число, а первый
раз был блокером прошлого гейта. Правка на один символ.

## ЗАКРЫТИЕ БЛОКЕРА 1 — снимаю, проверено на собранном дереве

Проверял так же, как доказывал: пересобрал дерево установки на `625a4fb` и смотрел, что получает
инженер, а не что лежит в исходниках.

**В доставленных `CLAUDE.md` / `AGENTS.md` / `.codex/config.toml`: `Selenium` — 0, `one-web` без
`-kmp` — 0.** Все шесть строк, которые я цитировал как доказательство блокера, исчезли.

**Но «Selenium исчез» — недостаточный критерий, и я проверял не его.** Проверял, что написанное
стало правдой:

| Что теперь утверждает пак | Проверка | Итог |
|---|---|---|
| стек `pytest + Playwright`, `pytest-playwright-canvas`, headless chromium | имя стека встречается в **20 доставляемых файлах**, включая канон `.agent-kit/craft/`; `run-tests:24` — «стек знает **один** браузер: headless chromium Playwright» | сходится |
| репозиторий `one-web-kmp` | в паках `one-web` без суффикса не осталось | сходится |
| клиент TestOps `.agent-kit/tms/scripts/testops_cli.py` + скилл `tms-read` | оба пути **есть в дереве**; шим запускается (проверено в п.2) | сходится |
| «`tools/testops` в репо проекта НЕТ — не зови `python -m tools.testops`» | в целевом `one-web-kmp` в `tools/` действительно `enums`/`helpers`/`i18n`/`data` | **правда, и это запрет, а не вызов** |
| «`.kit` заводить не требуется» | `params.load()` без `.kit` отдаёт `DEFAULTS` — проверено импортом | сходится |
| лейн — «11 скиллов» | `grep -c "kind: skill_spec"` = **11** | сходится |

Два оставшихся `tools.testops` в дереве доставки — обе строки в паках, и обе **запретительные**
(«НЕ зови», «не звать»). Тот же приём, что я одобрил в `run-tests`: запрет полезнее молчания.

**Проза роли поправлена заодно и по делу:** `manifest.yaml` (`description` / `persona.scope` /
`target_tasks` / `post_install_notes`), `README.md`. Отдельно замечу машиночитаемую правку, которой
я не требовал и которая правильная: `stack.optional: [one-web]` → `[one-web-kmp]` — роль ссылалась
на идентификатор стека, которого нет.

**Блокер 1 снят.**

### Что закрыто из моих не-блокеров

- **Исполняемые selenoid-команды — убраны.** Вызовов `.claude/scripts/selenoid_mac_safari.py` /
  `selenoid_win_firefox.py` в дереве **0** (были: скриптов не существует ни в поставке, ни в репо —
  агент копировал команду в пустоту). Оставшиеся 6 упоминаний `selenoid` — все **отрицания**:
  «grid и selenoid в нём нет», «команд такого вида здесь не строим». Исключение одно и оно
  законное: `exclusion-patterns.md:15` перечисляет `.claude/scripts/selenoid_*.py` как **паттерн
  исключения** из MR — это список того, что не коммитить, а не команда.
- **`__pycache__` — закрыт.** В `gitignore-secrets.txt` добавлены `__pycache__/` и `*.pyc`;
  ингредиент валиден (`repo_config`, `append_lines`, `marker_id`), доставляется дословно.
- **Счёт `tools/testops` исправлен на 10** — ровно с той раскладкой, которую я дал
  («8 в CHANGELOG, обе формы по 4, + 2 в манифесте»).
- **`one-web` без `-kmp` в дереве: 9 → 6.** Все 6 оставшихся — «референс-инсталляция one-web»
  в вербатим-клиенте TestOps и `tms-read`: это утверждение о том, на какой инсталляции Allure
  подтверждены эндпоинты. Факт, а не сращённость с репо. По существу метрика закрыта.

### Расхождение между заявленным и фактическим — одно, и оно важно

**Битых ссылок по-прежнему 20, а не меньше.** Тимлид передал: «исполнитель заявляет, что удвоения
`references/references/` больше нет (у меня грепом 0)». Прогнал сам — **число не изменилось, и
набор целей идентичен прежнему** посимвольно: `references/input-sources.md`,
`references/sanitization.md`, `references/tc-review.md`, `references/phases.md`,
`references/diff-routing-guards.md` — все пять удвоений на месте.

**Почему grep дал 0 и почему это ложный отрицательный.** Строки `references/references/` в файлах
нет и быть не может: в теле написано `references/X.md`, а удвоение возникает **при разрешении** —
файл-владелец сам лежит в `references/`, поэтому относительный путь складывается в
`references/references/X.md`. Грепом по исходнику это не ловится **по построению**. Ловится только
разрешением ссылок по дереву установки — тем самым инструментом из ветки 2.

**Справедливости ради: CHANGELOG этого не заявляет.** Я прочитал обе новые секции `[0.5.0]` —
там аккуратно перечислено ровно сделанное (`prepare-mr-branch`, `batch-report.md`, gitignore,
паки роли) и **нет** ни слова про `references/references/`. То есть поставляемый файл честен;
разошлись устная передача и факт, а не CHANGELOG и код. Отмечаю это прямо, потому что вопрос
был поставлен как «расхождение между коммитами и заявленным в CHANGELOG» — такого расхождения нет.

### ~~Новое наблюдение того же класса~~ — СНЯТО, закрыто на `d792969`

На `625a4fb` я нашёл в `jira-issue-autotest/SKILL.md` готовый к копированию блок
`glab ci run --variables TEST_BROWSER:browser_lin_chrome …` при том, что в выгрузке `one-web-kmp`
нет ни `.gitlab-ci.yml`, ни этих переменных, — и отнёс это к тому же классу, что убранные
selenoid-команды.

**Наблюдение было верным на `625a4fb` и закрыто на `d792969`. Перепроверил по телу, а не грепом:**
исполняемого блока нет, `glab ci run` в телах скиллов — **0** (три оставшихся вхождения: описание
правки в CHANGELOG и два комментария манифеста). Фаза 5 переписана в развилку «пайплайн настроен /
пайплайна нет»: в первой ветке агенту велено взять имена переменных **из конфигурации самого
пайплайна проекта** и не угадывать, во второй — прогнать локально через `/run-tests`. Два
оставшихся `TEST_BROWSER` — внутри предупреждения «CI в рабочей копии не обнаружен… команда,
которую агент копирует, не должна вести в несуществующее», с честной оговоркой, что пайплайн может
жить вне рабочей копии. Это сильнее, чем удаление: шаг остался выполнимым в обоих случаях.

**Моя ошибка в предыдущей редакции:** я перенёс этот пункт с `625a4fb` в разбор `d792969`, не
перечитав файл, — хотя `--stat` дельты показывал у него 58 изменённых строк. Ровно та ловушка,
от которой я предостерегал по `tools.testops`: смотреть надо глазами и заново, а не переносить
прежнюю находку.

## ФИНАЛЬНАЯ ДЕЛЬТА `625a4fb` → `d792969` (+ ветка 2 `ed63b6a`)

### Инструмент приёмки был сломан — и это важнее находок, которые он давал

Прочитал починку (`6f55b4f`) построчно. Дефект настоящий: `_sibling_links` спрашивал
`(<каталог владельца>/PROGRESS.md).is_file()`, а рядом лежит `references/progress.md` строчными.
**APFS на macOS регистронезависима и отвечала «есть»; на ext4 в CI — «нет».** То есть проверка
давала **разный вердикт на разных ОС**, и гейт на ней стоять не мог. Починка правильная: имя
сверяется с фактическими элементами каталога через `iterdir()`, а не вопросом к ФС.

**Снимаю свой прежний вывод.** В редакции этого отчёта от `fc09c2f` я писал про `PROGRESS.md`:
«рантайм-артефакт батча, не поставляемый файл. Ложное срабатывание». Симптом я назвал верно,
причину — нет: дело было не в природе файла, а в дефекте инструмента. Разница практическая:
из моего объяснения следовал бы фильтр рантайм-артефактов, который заглушил бы настоящий класс
дефекта «файл авторен рядом, но не доставлен». Исполнитель это заметил, фильтр не завёл
и в CHANGELOG объяснил почему. Это лучше, чем предлагал я.

### Цепочка битых ссылок — пересчитал сам исправленным инструментом

| Точка | Битых ссылок | Заявлено |
|---|---|---|
| база `9be499d` | **88** | 88 |
| `87ade2d` (конец `[0.4.0]`) | **20** | 20 |
| HEAD `d792969` | **0** | 0 |

**Секции состыкованы:** `[0.4.0]` заканчивается на 20 (строка 366), `[0.5.0]` с 20 и начинается
(строка 128) — разрыва между записями нет. Режим `paths` на HEAD тоже чист, режим `all` — чист.

**Критерий приёмки плана «проверка ссылок по дереву установки обоих CLI = 0 битых» ВЫПОЛНЕН.**
Весь день он был единственным невыполнимым числом; теперь он выполнен и перепроверен мной.

**Сверх заявленного:** прогнал `--all-profiles` — по всему каталогу **424 → 246**. Ветка не только
вычистила свой профиль, но и не создала долга нигде.

### Golden — сверил со собранным деревом, а не поверил снимку

- **Тронуто ровно 2 файла из 36**, оба `iva-role-qa`. Чужие golden не тронуты.
- **79 ключей** в обоих CLI. Сравнил ключи с фактическим деревом установки поимённо:
  расхождений нет. Два кажущихся — артефакты кодировки, а не дефекты: `.mcp.json` у claude —
  это `merge_json`, а не файл, и `user/.codex/config.toml` = `~/.codex/config.toml`.
- **Плейсхолдеров `{ingredient_id}` в ключах — 0. Значений `removed` — 0.**

**И golden — не бумажный: два теста приёмки реально позеленели.** Прогнал `tests/e2e_install`
на базе и на HEAD и сравнил списки:

| | база `9be499d` | HEAD `d792969` |
|---|---|---|
| `tests/e2e_install` | 35 failed, 57 passed | **33 failed, 59 passed** |

Позеленели ровно `test_install_flow_roles_generic[claude-code-iva-role-qa]` и `[codex-iva-role-qa]`.
**Новых падений — ноль** (`comm` по спискам пуст). Ветка починила приёмку своего профиля
и не сломала ничего чужого.

### CI-гейт подключён — шаг 0 плана закрыт

Этого в задании не было, нашёл сам: ветка 2 выросла ещё одним коммитом (`ed63b6a`), и в
`profile-version-discipline.yml` добавлены **два шага**: `--mode paths --all-profiles` по всему
каталогу и `--mode links --profile iva-role-qa`. Ограничение вторым списком **обосновано прямо
в workflow**: на весь каталог режим даёт 128 находок в чужих профилях, и включить его целиком
значит упереть первый же чужой PR в чужой долг — кончится снятием гейта. `continue-on-error`
отвергнут там же с верным доводом: «молчащий гейт хуже отсутствующего». Расширение — дописыванием
`--profile`, а не снятием ограничения. Это ровно тот пункт, который я весь день держал в открытых.

### Развязка с чужим модулем `base` — сценарий остался исполнимым

Это была главная качественная проверка: убрать вызов недоступного скрипта легко, оставить шаг
выполнимым — трудно. Прошёл `write-autotest` глазами по доставленному телу.

**Шаг 2 исполним.** Вместо вызова `task_meta.py` из модуля `base` в теле теперь лежит **сам
JSON-объект `meta.json` целиком** — все восемь полей, каждое выводится из самой задачи, плюс
явное «мета идемпотентна: файл уже есть — не перезаписывать». Внешних данных шаг не требует.
Это не удаление вызова, а перенос того, что вызов производил.

- `task_meta.py`: **3 упоминания на CLI** (6 на оба), все — честная оговорка «в поставку лейна
  не входит… пишется напрямую».
- **`.kit/schema.md` — 0.** Контракт fallback-делегирования выписан в телах. Ровно то, что я
  называл в Т4 как «два оставшихся проблемных места» — закрыто.
- `craft:update` — **1** упоминание вместо 10, и оно предупреждающее: «Производителя дельты
  в поставке лейна нет».
- Схема строки метрик перенесена в доставляемый `.agent-kit/craft/shared/ledger-and-deviations.md`.

**Побочный эффект, который я проверил отдельно:** счётчики `spawn_agent`/`wait_agent`/`close_agent`
в трёх codex-телах выросли на 1 (15→16, 18→19, 12→13). Это **не новый вызов и не потеря** —
выписанный inline контракт fallback упоминает `spawn_agent` прозой («`spawn_agent` не создал
субагента, отклонил носитель»). Механизм делегирования цел.

### Т7/Т10 — остатки, которые весь день были красными

| Метрика | Было (`625a4fb`) | Стало (`d792969`) |
|---|---|---|
| `$HOME/Projects` в дереве | 30 | **0** |
| `at-test-*` в дереве | 10 | **1** |
| битых ссылок | 20 | **0** |

**`rebuild-autocore` не удалён — расцеплен**, скилл на месте в дереве. Абсолютной раскладки
машины (`$HOME/Projects/AT/autocore`) и `rm -rf` по ней в нём больше нет.

**Единственный оставшийся `at-test-` — не стенд.** `playwright-cli/SKILL.md:93`: это **логин
почтового юзера** `config.credentials.mail_user`, поданный как «в референсе one-web-kmp это
`at-test-1` / `test`». Имена стендов там теперь не зашиты вовсе: «проверь наличие учёток на своём
(`config.yaml`, активное окружение)». Заявленное «стенды 16 → 0» **верно про стенды**; мой греп
по префиксу ловит совпадение имени учётки. Расхождением не считаю.

### Числа в поставляемом CHANGELOG — на этот раз сходятся

Проверял придирчиво, потому что за день туда трижды уезжало непроверенное.

| Заявлено | Проверка | Итог |
|---|---|---|
| ингредиентов 71 → 72 | `grep -c ingredient_id` = **72** | сходится |
| `replace`-файлов 56, не 45 | `grep -c 'merge_strategy: "replace"'` = **56** | сходится |
| цепочка 88 → 20 → 0 | три прогона исправленным инструментом | сходится |
| golden 79 ключей, `removed` 0 | разбор JSON | сходится |
| `task_meta.py` 14 → 6 | 3 на CLI × 2 | сходится |
| `craft:update` 10 → 1 | греп по дереву | сходится |

**Ни одного расхождения не нашёл.** Отдельно отмечу: CHANGELOG сам, без принуждения, помечает
прежние числа (`26 → 20 → 10 → 6`) как снятые сломанным инструментом и объясняет причину. Это
противоположность тому, с чего день начинался.

## 6. Гит-чистота

- Секретов, `.env`, ключей, бинарников, `__pycache__` / `.DS_Store` / `.venv` /
  worktree-артефактов в диффе **нет** (скрипт + ручная проверка).
- **AI-подписей нет** ни в заголовках, ни в телах коммитов, ни в контенте: `Generated with`,
  `Co-Authored-By`, `claude.ai/code`, `Claude-Session` — ноль вхождений. Все 9 коммитов
  за подписью «Александр Шульга», сообщения содержательные.
- **Ветки не запушены.** `git ls-remote --heads origin` не знает ни `feat/qa-canon-delivery`,
  ни `chore/qa-acceptance-tooling`; ни один remote-ref не содержит `fc09c2f` / `30d123e`.
- Ветка 2 — дерево чистое, незакоммиченного 0.
- **Ветка 1 — в дереве появился посторонний файл**, уже после моих прогонов: см. не-блокер 8.

## 7. Отступление от verbatim — одно, оправдано

`launch.py:168`, комментарий:

```
-        # references/launch-input.md скилла craft:fix)
+        # .agent-kit/craft/skills/fix-failed-test/references/launch-input.md)
```

**Логика не тронута ни на символ** — это единственная изменённая строка файла, и она внутри
комментария. Путь `kit` в дереве Тактикума не разрешается, новый — разрешается (файл на месте,
проверил). Отступление правильное и заявлено честно.

**Рядом — непоследовательность, которую назову:** в `testops/__init__.py:22` осталась ссылка
на скилл **`tms:read`** (kit-имя), которого в дереве доставки нет — там `tms-read`. Файл вербатим,
поэтому формально всё правильно; но одну такую ссылку поправили, а другую нет. Докстринг,
влияние минимальное.

## 8. Переименование `tms:read` → `tms-read` — корректно

- Скилл существует: `ingredient_id: tms-read`, плюс три справочника `tms-read-{aql-notes,launch-json,tc-plain}`.
- **Доставляется на оба CLI**: `.claude/skills/tms-read/SKILL.md` и `.agents/skills/tms-read/SKILL.md`.
- Справочники — в нейтральном `.agent-kit/tms/skills/read/references/`.
- **Висячих ссылок в обе стороны нет.** `tms:read` осталось в 4 местах: комментарий манифеста,
  две строки CHANGELOG (описание самой правки) и упомянутый докстринг вербатим-файла. Ни одна
  не является ссылкой, по которой агент пойдёт.
- Проверка ссылок это подтверждает: `aql-notes.md` и `launch-json.md` из списка битых **ушли**.

## 9. Тесты и рендер — прогон на `fc09c2f`

> ⚠️ **Числа этого раздела промежуточные.** Финальные прогоны и разбор — в «ФИНАЛЬНОЙ ДЕЛЬТЕ».
> Главное расхождение: `e2e_install` на финальном HEAD **33 failed / 59 passed** (два теста
> позеленели), а не 35/57, как ниже.

```
cd apps/backend && uv run --extra dev pytest tests/catalog/ -q
→ 710 passed in 19.75s
```

`check_profile_version_discipline.py` → `OK — 48 profile(s) clean`, rc=0.
Дублей `target_file` — 0 из 57. Ингредиентов лейна: база **15** → `87ade2d` **60** → HEAD **71**
(совпадает с заявленным «60 → 71»).

Рендер обоих CLI — числа выше, п.2: плейсхолдеров 0, склеек 0, sha256 путей `.agent-kit/`
между клиентами совпадают полностью.

**`tests/e2e_install` — прогнал на базе и на HEAD:**

| | база `9be499d` | HEAD `fc09c2f` |
|---|---|---|
| `tests/e2e_install` | 35 failed, 57 passed | 35 failed, 57 passed |

**Список упавших идентичен посимвольно** (`diff` пуст). Ветка ничего не ломает — и ничего
из красноты приёмки не чинит.

## БЛОКЕРЫ — действующих нет

За день гейт нашёл три блокера. Все закрыты, все проверены мной повторно.

1. **Ложная метрика в поставляемом CHANGELOG** (найдено прошлым гейтом) — закрыто на `87ade2d`,
   затем цепочка ещё раз пересчитана исправленным инструментом.
2. **Роль не бампнута, ребро `depends_on` заморожено** (найдено прошлым гейтом) — роль 0.5.4,
   остаток требования — порядок сида, см. ниже.
3. **Дефект A: паки роли описывали чужой стек** (найдено этим гейтом, в отчётах исполнителей
   отсутствовало) — закрыто на `8d3b87c`, проверено на собранном дереве обоих CLI.

**Остаётся один процедурный пункт — порядок сида.** `depends_on` роли версии не несёт: ребро
выбирается в момент сида по последней активной версии базы (`seed_profile.py` — `status == "active"`,
`order_by(seeded_at.desc()).limit(1)`). Значит: **сначала сид лейна `iva-qa-autotest-base` 0.5.0,
только потом сид роли `iva-role-qa` 0.5.4.** Наоборот — ребро свяжется с 0.4.0, и вся работа
не доедет при всех зелёных гейтах. Проверить это заранее нечем; место — runbook доставки.

## ЧТО ЗАКРЫТО И ЧТО ОСТАЛОСЬ ПО ТРЕБОВАНИЯМ ИНЖЕНЕРА

Сверка по первоисточнику [[требования Евгения (ИВА) к QA-профилю — дословно]], состояние на
`d792969`. Проверял на собранном дереве установки, не в исходниках.

| № | Требование | Итог | Чем закрыто / что осталось |
|---|---|---|---|
| **Т1** | клиент TestOps в поставке | **ЗАКРЫТ** | пакет + шим в `.agent-kit/tms/scripts/`; гейт **запустил `--help` в собранном дереве** без `.kit`, на обоих CLI; `params.py` побайтно из `kit` |
| **Т2** | ничего не требовать от репо | **ЗАКРЫТ** | живых вызовов `tools/testops` — 0 (в паках стоит прямой запрет); из требований остались только среда (venv/autocore/внешние CLI) и `requests`/`pyyaml`, которые в `one-web-kmp` уже есть |
| **Т3** | reference-файлы доезжают | **ЗАКРЫТ** | 56 файлов в `.agent-kit/`, один комплект на оба CLI, sha256 совпадают; **битых ссылок 0** |
| **Т4** | `.kit` не требовать | **ЗАКРЫТ** | императивов нет; `.kit/schema.md` — 0; в паках «заводить `.kit` не требуется»; `params.load()` без `.kit` отдаёт дефолты |
| **Т5** | `AUT_OVERVIEW.md` не требовать | **ЗАКРЫТ** | везде «опциональна / если проект её ведёт» |
| **Т6** | локальный запуск | **ЗАКРЫТ** | `run-tests` без матриц, selenoid, `--browser-tag`, `--local`; `--env` не передаётся; маркеры сверены с `pytest.ini` целевого репо |
| **Т7** | `at-test-*` не использовать | **ЗАКРЫТ** | стендов в дереве 0; единственное совпадение по префиксу — логин почтового юзера, не стенд |
| ~~Т8~~ | путь к python | снят Президентом | — |
| **Т9** | брать цельное состояние | **решено осознанно** | поимённый импорт из среза `kit` с провенансом по файлам; расхождение с буквой вынесено вопросом, не решено молча |
| **Т10** | не сращён с репо, удобен, универсален | **закрыт по проверяемой части** | `$HOME/Projects` 0, `one-web` без `-kmp` 0 в живых телах, паки описывают `one-web-kmp`/Playwright. **Остаток — 6 упоминаний «референс-инсталляция one-web» в вербатим-клиенте**: это факт о том, где подтверждены эндпоинты Allure, а не привязка |

### Что осталось честно открытым

1. **Порядок сида** — единственный пункт с последствиями. См. блок выше.
2. **Универсальность (Т10) остаётся заявленной, а не доказанной.** Профиль перестал быть сращённым
   с `one-web`, но проверен ровно на одном репозитории — `one-web-kmp`. Что он ляжет на второй,
   мы не знаем и утверждать не должны.
3. **`e2e_install` красный 33 тестами** — теми же, что на `main`. Ветка их не создавала
   и не обязана чинить; но пока они красные, полный прогон приёмки остаётся неинформативным.

**Открытым НЕ считаю** `browser_lin_chrome` как пример данных в таблицах
`fix-failed-test/references/`: это dual-stack обучение, объявленное сознательно («упоминание
допустимо, готовая команда — нет») и выдержанное последовательно. Это решение, а не недоделка.

### Закрыто из моих прежних не-блокеров

- ~~Битых ссылок 20 при пороге 0~~ → **0**.
- ~~Селениумная модель рядом с `run-tests`~~ → исполняемых команд 0, остались отрицания.
- ~~`at-test-*` ×10~~ → 0 стендов.
- ~~`$HOME/Projects` ×30~~ → **0**, скилл расцеплен без удаления.
- ~~Приёмка не гейт~~ → golden перегенерирован, 2 теста позеленели, **CI-гейт подключён**.
- ~~`__pycache__` в репо инженера~~ → в доставляемом `.gitignore`.
- ~~Ссылка на несуществующий `.agent-kit/craft/README.md`~~ → заменена; сверх того заведён
  настоящий `.agent-kit/README.md` — карта дома с предупреждением «канон перезаписывается,
  править на месте нельзя».
- ~~Посторонний файл в worktree~~ → дерево чистое.

## Что честно сказать Президенту

1. **Работа сделана и проверена запуском, а не отчётом.** Клиент TestOps стартует из собранного
   дерева без `.kit`, канон одинаков на обоих CLI побайтно, 710 тестов зелёные, битых ссылок 0.

2. **Гейт за день нашёл три блокера, и один из них не был виден никому, кроме гейта.** Паки роли
   (`CLAUDE.md` / `AGENTS.md`) — то, что агент читает раньше любого скилла — описывали профиль как
   Selenium поверх `one-web` с `tools.testops` в репозитории, ровно наоборот к тому, что чинилось
   весь день. В отчётах исполнителей этого не было. Без гейта работа уехала бы к инженеру с этим
   внутри, и зонд дал бы ложный отрицательный.

3. **Сломанным оказался сам измеритель — и это поймали и починили.** Проверка ссылок спрашивала
   файловую систему про `PROGRESS.md`, а регистронезависимая macOS отвечала «есть» из-за соседнего
   `progress.md`: **разный вердикт на macOS и в Linux-CI**. Все числа за день были сняты сломанным
   инструментом, включая мои. Пересчитано одной мерой: 88 → 20 → 0. **Моё собственное объяснение
   («рантайм-артефакт, ложное срабатывание») было неверным, и я его снял** — исполнитель разобрался
   лучше и не стал заводить фильтр, который заглушил бы настоящий класс дефектов.

4. **Приёмка перестала быть словом.** Golden перегенерирован и сверен мной с фактическим деревом;
   два теста `e2e_install` реально позеленели, новых падений ноль; проверка ссылок подключена в CI.
   Ограничение гейта списком профилей обосновано в самом workflow — расширять дописыванием, а не
   снятием. Это ровно то, чего не было утром.

5. **Осталось одно, и оно про процедуру, а не про код: порядок сида.** Сначала лейн 0.5.0, потом
   роль 0.5.4. Наоборот — работа не доедет до инженера при всех зелёных гейтах. Проверить заранее
   нечем.

6. **Универсальность заявлена, но не доказана.** Профиль перестал быть сращённым с `one-web`,
   однако проверен на одном репозитории. Что он ляжет на второй — мы не знаем.

7. **Необратимость остаётся ровно там, где её назвал план.** Механики удаления файлов у потребителя
   нет: 56 файлов, записанных ему на диск, останутся навсегда.

### Чего мы НЕ можем доказать сами — граница честности

- **Что прогон тестов у инженера пройдёт.** Нет `kmp-stage`, локального dev-сервера, VPN, секретов
  TestOps. Доказано: маркеры тела есть в его `pytest.ini`, несуществующих опций тело не передаёт,
  стенд по ветке не резолвится, шим запускается. Что pytest дойдёт до зелёного — только он.
- **Что клиент TestOps достучится до Allure** с его машины по сети и токену.
- **Что делегирование субагентам стартует живьём.** Целостность проверена диффом и счётчиками;
  последний `SPAWN_OK` — стенд 24.07, свежего прогона нет.
- **Что ребро связалось с 0.5.0** — видно только после сида, по факту.

## Что проверено чужими руками и что моими

Скрипт закрыл: ветка / чистота дерева / секреты / мусор / AI-подписи / скоуп / 17 метрик против
порогов — по обеим веткам. Ветка 2: **9 из 9, нарушений нет**.

Моё: сборка настоящего дерева установки рендерером каталога на каждом сдвиге HEAD и **живой запуск
шима** без `.kit`; побайтная сверка семи файлов клиента с источником `kit`; **пересчёт всей цепочки
битых ссылок исправленным инструментом** на трёх точках истории плюс `--all-profiles`; чтение
починки инструмента построчно; сверка ключей golden с фактическим деревом поимённо; прогон
`e2e_install` на базе и на HEAD со сравнением списков падений; прохождение `write-autotest` глазами
на исполнимость без модуля `base`; разбор роста счётчиков делегирования; сверка маркеров `run-tests`
с `pytest.ini` целевого репо; чтение всех упоминаний `tools.testops` по одному с контекстом; разбор
выбора ребра `depends_on` по коду сида; чтение нового CI-workflow; и обнаружение дефекта A, которого
не было ни в одном отчёте.

**Что я сам сделал неверно и снял — три вещи:**

1. Объяснение находок по `PROGRESS.md` («рантайм-артефакт, ложное срабатывание») — причина была
   в регистронезависимости ФС, а не в природе файла.
2. Число «`one-web` без `-kmp` — 12»: завышено артефактом моей оснастки (двойной прогон
   материализатора в один каталог удваивал `append_section`-файлы). Верное — 9, затем 6.
3. **Пункт про `glab ci run` в `jira-issue-autotest`**: верный на `625a4fb`, но я перенёс его
   в разбор `d792969`, не перечитав файл, хотя дельта показывала у него 58 изменённых строк.
   Команда там уже убрана, шаг переписан в развилку. Ошибка ровно того рода, от которого
   я предостерегал команду: проверять глазами и заново, а не переносить прежнюю находку.

Все три исправлены в тексте выше.

Relations:
- implements [[plan-qa-rabota-2026-07-28]]
- relates_to [[gate-qa-canon-2026-07-28]]
- relates_to [[требования Евгения (ИВА) к QA-профилю — дословно]]
- relates_to [[qa-ramki-kontrolya-2026-07-28]]
- relates_to [[impl-qa-canon-2026-07-28]]
- relates_to [[impl-qa-acceptance-2026-07-28]]
- relates_to [[qa-komplekt-k-pokazu-2026-07-28]]
