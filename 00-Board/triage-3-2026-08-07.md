---
type: report
status: draft
date: 2026-08-07
author: verifier
tags:
- triage
- taiga
- board
permalink: tacticum/00-board/triage-3-2026-08-07
---

# Триаж кандидатов на закрытие — блок 3 (#640, #641, #663, #657, #661, #697, #713)

Проверка по существу: дифф каждого коммита против текста задачи, состояние `origin/main`
на сегодня (HEAD `d036f3d8`), прогон тестов в отдельном временном worktree и чтение боевой
БД каталога на `tacticum_prod`. Рабочее дерево `tacticum-dev` не трогалось; временный
worktree `vf-triage3` создан от `origin/main` и снят после прогонов.

Все 14 коммитов из выгрузок действительно в `origin/main` —
`git merge-base --is-ancestor <sha> origin/main` = 0 для `486e757a`, `116252e1`,
`f6fb02d1`, `ebccfcd6`, `6eafd422`, `3bb30fe3`, `309289af`, `4fe722fb`, `b0094ae0`,
`9917c59e`, `480e1656`, `6a119251`, `ecc9d5e3`, `5f986cb3`.

Что прогонялось (worktree `vf-triage3`, `apps/backend`, uv + docker-postgres):

| Прогон | Результат |
|---|---|
| `pytest tests/workspace/test_provision_installation.py` | 10 passed |
| `pytest tests/identity/test_whoami_mcp.py tests/e2e_install/test_install_flow.py` | 63 passed |
| `pytest tests/e2e_install/test_start_task_no_subagent.py` | 19 passed |
| `python scripts/check_profile_version_discipline.py` | `OK — 64 profile(s) clean` |

## Вторым проходом: тела коммитов целиком (шаблон «ссылка = долг ОСТАЁТСЯ»)

По находке `triage-1` перечитал все 14 коммитов через `git log --no-walk --format=%B` — не
заголовок, а тело, и с проверкой, в каком контексте стоит номер. Шаблон **подтвердился на
четырёх коммитах из моего набора**; вердикты от этого не изменились ни одного, но два из них
теперь опираются на прямой текст автора, а не только на мои замеры.

| Коммит | Что на самом деле сказано про номер | Тип |
|---|---|---|
| `480e1656` | «Sibling profiles + the agent-auto-trigger vector **tracked in Taiga #697**» | долг #697 остаётся |
| `9917c59e` | «RE wiki-publisher (**US #661**) no longer reads … — all profile content must come from the stats API» — коммит делает US #669, а #661 назван потребителем | долг #661 остаётся |
| `f6fb02d1` | «The defect is inherited by the whole profile line (porting to rn/mail/task-workflow **tracked as US #641**)» | долг #641 остаётся |
| `3bb30fe3` | «Reference fix kmp 0.2.4 (PR#52), porting **tracked as US #641**» | долг #641 остаётся |

Разница между #641 и #697 — в том, что было ПОТОМ, и решает порядок по времени, а не наличие
маркера: у #641 после двух откладывающих ссылок 10.07 в тот же день пришёл `6eafd422`, который
работу делает («port kmp 0.2.4-0.2.6 fixes to rn/mail/task-workflow», «Refs: Taiga US #641»), и
`ebccfcd6` его дочинил. У #697 после откладывающей ссылки 21.07 пришёл `6a119251` 23.07, который
сделал **часть** — и оба долга из фразы `480e1656` («sibling profiles» и «agent-auto-trigger
vector») по замеру на сегодняшнем main живы. Маркерное слово одно и то же, исход разный, и
отличить их можно только прочитав, что именно доехало.

Отдельно: у #640 три привязанных коммита, но `f6fb02d1` — это референс-фикс топологии, то есть
работа #641; #640 он упоминает как соседа по эпику. Содержание #640 живёт только в `116252e1` и
`486e757a`. Ни у #663, ни у #657 откладывающих формулировок в телах нет вовсе — оба тела
описывают работу как выполненную и совпадают с диффом.

---

## #640 — iva-kmp-brownfield 0.2.2, оптимизации O1-O4

**Просили:** четыре текстовые правки профиля (батчинг read-only вызовов, tree-first резолв
токенов, лимиты выдачи, прокидка `kb_run_id`/`installation_id` в субагентов) + приёмка из
4 пунктов, включая A/B-замер с порогами.

**Что реально:** `116252e1` (0.2.2) кладёт ровно этот состав — Efficiency rules во все три
repo-config фрагмента (O1 + O3-lite), «Output discipline» в codegraph-first-navigation,
tree-first резолв в coder ×3 поверхности и design-token-usage (O2), §3.1 context
pass-through в tacticum-workflow ×3 (O4). `486e757a` (0.2.3) переносит батчинг из фрагментов
в тела агентов. Правила живы в `origin/main` и сегодня (Zero-resolve rule в
tacticum-workflow ×3 поверхности, batch-правила в coder/workflow/фрагментах).

**Чем подтверждается:** дифф `116252e1` + CHANGELOG [0.2.2]; приёмка 1-2 (регресс + E2E)
отработана по комментарию 09.07 15:31; приёмка 3-4 (A/B + fidelity) отработана и
опубликована таблицей в комментарии 09.07 16:57. `check_profile_version_discipline` зелёный
и сегодня (64 профиля). В проде kmp-инсталляция `687db0e8` живёт на 0.12.1, то есть 0.2.2/0.2.3
давно раскатаны.

**Нюанс:** численные пороги A/B по O1 и O4b НЕ достигнуты (ходов 137 при цели ≤90,
транскрипт 0.70МБ при ≤0.45, мульти-tool ходов 0). Но это не незавершённая работа: последний
комментарий — явное закрытие итераций владельцем («инструкционные итерации по O1
останавливаем», «Итоговый счёт эпика E43»), потолок помечен как ограничение рантайма, а
upgrade path O5 (bulk-эндпоинты на бэке) — это другая работа на сервере, а не хвост текстовых
правок. **Лиду на заметку:** если под O5 отдельной задачи в трекере нет, её стоит завести
перед закрытием #640 — иначе путь потеряется.

**Вердикт: ЗАКРЫТЬ.**

---

## #641 — портирование main-session-orchestration (kmp 0.2.4) в rn / mail / task-workflow

**Просили:** перенести 1:1 четыре части фикса (команды approve-docs + run-implementation,
§3.1 Runtime check на 3 поверхностях, CLAUDE.md-фрагмент, AGENTS.md Depth rule) в три
профиля с bump + CHANGELOG; комментарии расширили объём до 0.2.5 (task-dir resolution) и
0.2.6 (codegraph readiness, только rn/mail).

**Что реально:** `6eafd422` — все три профиля, все перечисленные поверхности:
approve-docs + run-implementation ×3, tacticum-workflow ×3 формата ×3 профиля, оба фрагмента,
run-coder/run-tester/run-test-runner (rn/mail) с task-dir, `local-code-intelligence` только
у rn/mail (у task-workflow codegraph нет — как и записано в комментарии). `ebccfcd6` —
переиздание rn 0.4.7 / mail 0.6.7 после инцидента с root-owned файлами на VPS. ADR-0055
лежит в `docs/adr/0055-profile-orchestration-topology-per-cli.md`.

**Чем подтверждается (боевая БД, не отчёт):** в `tacticum_catalog` на проде инсталляции
`55bdb714` (rn, 0.5.4), `7f35e710` (mail, 0.8.1) и `687db0e8` (kmp, 0.12.1) — все на версиях
много выше 0.4.7/0.6.7, то есть остаток «re-pin» из комментария 15:06 давно закрыт (что и
подтверждает комментарий 18:06). У всех трёх в теле ингредиентов `approve-docs` и
`run-implementation` есть «top-level session» и нет старого «Hand off to the tacticum-workflow
sub-agent» — то есть дефект из описания в проде отсутствует.

**Что осталось непроверенным:** приёмка п.2 (живая поведенческая проверка — после
`/approve-docs` в main-чате спавнятся coder+tester, fail-loud срабатывает на попытке hand-off)
нигде не задокументирована как выполненная; в комментариях она отложена «до первого живого
прогона после re-pin», и подтверждения нет. Автотеста на approve-docs в репозитории нет
(`test_start_task_no_subagent.py` — про start-task и появился позже, в рамках #697).

**Вердикт: ЗАКРЫТЬ** — материальная часть доставлена и проверяема в проде; незакрыт только
ритуал живого наблюдения на версиях, которые с тех пор сменились 5+ релизами.

---

## #663 — provision_installation: workspace_ambiguous при мульти-workspace org

**Просили:** три пункта резолюции без выбора пользователем — (1) find-first по всем
workspace организации, (2) дефолтный workspace + миграция и бэкфилл, для org `iva` назначить
`base`, (3) fallback с самоописательными кандидатами `{id, slug, name}` + `workspace_id` в
строках `whoami`.

**Что реально:** `4fe722fb` кладёт все четыре куска: find-first ДО резолюции workspace с
возвратом `created=false`; `Workspace.is_default` + миграция (в main она под номером
`0036_workspace_is_default.py` — переномерована из 0035 при слиянии) с бэкфиллом
одно-workspace организаций и partial unique index; структурные `candidates` в
`workspace_ambiguous`; `workspace_id` в `whoami`. Миграция сознательно НЕ проставляет дефолт
мульти-workspace организациям — это оставлено админу (записано в докстринге).

**Чем подтверждается:** прогон `tests/workspace/test_provision_installation.py` — **10
passed**, включая `test_provision_find_first_returns_existing_install_across_workspaces`,
`test_provision_creates_in_default_workspace`,
`test_provision_ambiguous_error_carries_structured_candidates`;
`tests/identity/test_whoami_mcp.py` + `tests/e2e_install/test_install_flow.py` — **63 passed**
(там же e2e-кейс мульти-workspace провижина из `309289af`). Плюс боевая БД: у организации
`4034524f-…` (три workspace — `base`, `fnf`, `smoke-5d7bb70c`, ровно как в описании
инцидента) `base` помечен `is_default = t`. То есть и ручной пункт «для org iva назначить
base» выполнен в проде.

**Вердикт: ЗАКРЫТЬ.**

---

## #657 — installation_id: context.yaml-first во все 5 профилей

**Просили:** change-set из 4 пунктов по пяти профилям (web, kmp, rn, mail, go-backend) —
шаг 0 в телах агентов во всех 3 форматах, правка трёх скиллов, правка трёх фрагментов, и
явно «не трогать» approve-docs/run-implementation.

**Что реально:** `b0094ae0` — 57 файлов, все пять профилей. Тела агентов: `agents/`,
`agents-codex/`, `agents-copilot/` у каждого. Скиллы: kb-navigation у всех пяти,
design-system-discovery и design-token-usage у четырёх фронтовых (у go-backend их нет — там
дизайн-системы не применяются). Фрагменты: CLAUDE.md, AGENTS.md, copilot-instructions у всех
пяти. approve-docs/run-implementation в диффе отсутствуют — «не трогать» соблюдено. Web
поднят до 0.1.3, как и требовалось.

**Чем подтверждается:** содержательный дифф, а не заголовок: в теле агента шаг 1 «Read
`.tacticum/context.yaml` → take `installation_id`», шаг 2 `kb_discover(installation_id=<id>)`
с прямым «do NOT rely on server auto-resolve», STOP-правило «silently degrading to
Glob/Grep/Serena as a KB substitute is a hard-rule violation» — присутствует во всех 17 телах
агентов линейки. Старая формулировка «omit it — the server resolves it» в живых текстах
`origin/main` не осталась (уцелевшие вхождения — историческая строка CHANGELOG и
несвязанное «omit it» про поля формы в coder.md). Регрессия закрыта оракулом
`assert_no_stale_id_advice` из `309289af` — прогон `test_install_flow.py` зелёный (в тех же
63 passed).

**Остаток в тексте задачи** — «после деплоя backend-фикса #656 договориться, оставлять ли
серверный авто-резолв single-installation phk» — это решение по отдельной issue, а не хвост
этого change-set.

**Вердикт: ЗАКРЫТЬ.**

---

## #661 — [E-STATS.S2] wiki-выгрузки через stats API + cron на VPS

**Просили:** перевести две wiki-выгрузки на stats API, новый `wiki_sync_kb_repos.py`,
cron/systemd на хосте с доступом к wiki.iva.ru и mcp.tacticum.dev, обновить память
`deployment_prod_catalog_mcp`. DoD: обе страницы обновляются по расписанию, ручной прогон
боевым токеном зелёный, в репо нет секретов.

**Что реально:** в `origin/main` этого репозитория работы нет вовсе. Единственный
ссылающийся коммит `9917c59e` — это серверная US #669 (поля publisher в `GET
/stats/profiles`), то есть блокер, а не сама задача. Клиентская часть по решению 15:06
вынесена в другой репозиторий — `git.cifragen.ru/tacticum/re`, MR !23 (влит) и MR !24
(деплой-файлы, на момент последнего комментария НЕ влит).

**Тело `9917c59e` подтверждает это буквально:** «RE wiki-publisher (US #661) no longer reads
`docs/user_manuals` + templates from a local checkout — all profile content must come from the
stats API». Номер #661 стоит в описании ПОТРЕБИТЕЛЯ, ради которого расширяли эндпоинт; сам
коммит — это US #669. Классический случай «ссылка ≠ закрытие».

**Явный остаток назван в последнем комментарии (15.07 16:20):** 403-негатив (#667) — нужен
токен без scope от Diaret; Serena-память `deployment_prod_catalog_mcp` (#668); **смержить
MR !24**.

**Что не проверено и почему:** хост, на котором стоит cron (прод RE, 194.36.208.242 —
в ssh-manager это алиас `adp_emb`), из этой сессии недоступен: три попытки подключения
завершились `Failed to connect: Timed out while waiting for handshake` (хосту нужен
IVA-VPN). Ни crontab, ни `/opt/kbconsole/logs/wiki-sync.log`, ни состояние MR !24 я не видел
— утверждения комментариев о расписании остаются непроверенными отчётами.

**Вердикт: НЕ ЗАКРЫВАТЬ.** Перепроверять, когда будет доступ к RE-хосту и к GitLab: если
MR !24 влит, а #667/#668 закрыты — задача становится кандидатом на закрытие.

---

## #697 — тиражирование фикса «/start-task Phase 1 в top-level» на 7 стек-профилей

**Просили:** в КАЖДОМ из 7 профилей пять пунктов (start-task без спавна, `tool_timeout_sec
= 120` в `config.toml.template`, приведение фрагментов, quickstart, bump + CHANGELOG). Плюс
комментарий 20.07 расширил скоуп **вектором 2**: убрать/смягчить авто-триггер агента
`tacticum-workflow` (`Triggers on "start task", …, "ТЗ"`) и добавить runtime-гейт на
MCP-диспатч при depth ≥ 1 — в 9 профилях-носителях агента, включая firebird-web-brownfield
и iva-ios-brownfield, которые СТО отдельно подтвердил в скоупе.

**Тело `480e1656` само называет #697 долгом:** коммит чинит только `tacticum-dev-base` и
заканчивается строкой «Sibling profiles + **the agent-auto-trigger vector tracked in Taiga
#697**». То есть на 21.07 оба вектора числились за этой issue как невыполненные — и вопрос
только в том, что из этого сделал `6a119251` два дня спустя.

**Что реально сделано** (`480e1656` + `6a119251`): вектор 1 закрыт у пяти standalone —
web 0.1.9, kmp 0.4.3, rn 0.4.13, go 0.2.3, mail 0.6.14, плюс база; `tool_timeout_sec`
добавлен в их codex-шаблоны; фрагменты приведены; регрессия покрыта тестом.

**Чего нет — проверено по `origin/main` командами, а не по описанию:**

1. **Вектор 2 не сделан нигде.** Все 8 профилей-носителей агента
   (`brownfield-task-workflow`, `firebird-web-brownfield`, `iva-brownfield-mail`,
   `iva-go-backend-brownfield`, `iva-ios-brownfield`, `iva-kmp-brownfield`,
   `iva-rn-brownfield`, `iva-web-brownfield`) до сих пор несут `Triggers on … "ТЗ"` в
   `description` агента. Runtime-гейта на MCP-диспатч нет ни в одном: единственный гейт в
   телах — старый «if you are a sub-agent with NO tool to spawn agents», ровно тот, про
   который комментарий пишет «его недостаточно». Поиск по `spawned sub-agent|depth ≥|MCP
   dispatch` даёт одно попадание на всю линейку (kmp, и то про Phase 1). Значит
   firebird-web-brownfield и iva-ios-brownfield, попавшие в скоуп именно вектором 2,
   получили в `6a119251` только bump манифеста и строку CHANGELOG.
2. **brownfield-task-workflow — п.1 в рамках #697 не был сделан вовсе.** В дереве коммита
   `6a119251` его `start-task.md` всё ещё содержит буквально `Invoke the tacticum-workflow
   sub-agent` и `Hand off to the tacticum-workflow sub-agent with:` — тот самый баг-паттерн
   из описания. Починен он только 2026-08-05 отдельным коммитом `630869fb` («Fix /start-task
   no-spawn regression»), который на #697 не ссылается. **П.2 у него не сделан до сих пор:**
   `tool_timeout_sec` в его `config.toml.template` отсутствует (по всему репо: 6 шаблонов из
   25 содержат параметр).
3. **iva-java-backend-brownfield** — профиля в `origin/main` нет вообще (есть только
   `iva-java-development-base` и `iva-role-java`), то есть один из семи не тронут.
4. Регрессионный тест `test_start_task_no_subagent.py` (19 passed) покрывает 5 профилей +
   2 role-эры и **не покрывает brownfield-task-workflow**; iva-go-backend-brownfield из него
   исключён комментарием как deprecated — и это подтверждается манифестом: у go-backend в
   main `version: 0.3.2`, `deprecated: true`.

**Замер аудита 05.08 перепроверен на сегодняшнем `origin/main` (d036f3d8), а не принят на
веру:** codex-шаблонов `config.toml.template` — 25, с `tool_timeout_sec` — 6, без — **19**.
За два дня цифра не изменилась ни на единицу, аудит по этому пункту не устарел. Из семи
профилей скоупа параметр есть у пяти; brownfield-task-workflow без него,
iva-java-backend-brownfield в main отсутствует.

**Вердикт: НЕ ЗАКРЫВАТЬ.** Предупреждение лида подтвердилось и оказалось даже шире:
не только `tool_timeout_sec`, но и весь вектор 2 остался несделанным.

---

## #713 — FRQA-S14: диаграммы взаимодействий в FR (drawio на wiki)

**Просили:** шаг «Диаграммы взаимодействий» в fr-authoring — blueprint сервисов всегда,
sequence «Пользователь ↔ UI» при UI-скоупе, детерминированный mxfile-шаблон, правила
честности на диаграммы, публикация через `confluence_upload_attachment` + drawio-макрос,
перезаливка в `/update-feature`, деградация без PAT. Приёмка: смоук на тестовой странице с
подтверждением рендера **владельцем**, и прогон `/start-feature`, дающий FR с blueprint.

**Что реально:** `5f986cb3` кладёт шаг 7б в `fr-authoring/SKILL.md` целиком: оба типа
диаграмм с условием UI-скоупа, координатная сетка участников и сообщений, готовый скелет
mxfile, запрет свободного рисования, трассировка стрелок к FT-n/UC-n, публикация вложением +
макрос в шаге 9, перезаливка тем же именем в `/update-feature`, новый раздел «Диаграммы
взаимодействий» в скелете FR. `ecc9d5e3` зеркалит это в базовый профиль. Фича пережила
последующие переработки — сегодня шаг живёт в `iva-analysis-fr` и `iva-system-analyst`.

**Релиз состоялся — проверено в боевом каталоге.** Тело `5f986cb3` заканчивается словами
«still unreleased 0.1.3», и это правда: в проде у `iva-fr-analyst` версия 0.1.3 засижена БЕЗ
шага диаграмм. Фича появляется с **0.1.5** и присутствует во всех последующих активных
версиях до сегодняшней 0.2.2, а у профиля-наследника `iva-analysis-fr` — с самой первой (0.1.0).
Итого 16 активных версий каталога несут шаг 7б. Вопрос «доехало ли до пользователей» закрыт.

**Что не подтверждено:** обе приёмки. Комментарий 20.07 прямо называет остаток — «Осталась
визуальная приёмка рендера owner'ом: страница 211846230 (после проверки страницу удалить)»;
подтверждения нигде нет, более поздних комментариев по задаче нет. Второй пункт приёмки
(прогон `/start-feature` даёт FR с blueprint) тоже нигде не зафиксирован. Проверить это из
репозитория нечем: смотреть надо на wiki.iva.ru, а он за IVA-VPN и из этой сессии недоступен.

**Вердикт: СПОРНО.** Реализация полная и живая, но обе приёмки упираются в человеческое
подтверждение, которого в задаче нет. Решается одним вопросом владельцу: «рендер на 211846230
смотрели?» — если да, задача закрывается без дополнительной работы.

---

## Сводка

```
#640 · ЗАКРЫТЬ · состав O1-O4 в main (116252e1 0.2.2 + 486e757a 0.2.3), приёмка 1-4 отработана, финальный A/B 09.07 закрывает итерации решением владельца; discipline-чек зелёный (64 профиля)
#641 · ЗАКРЫТЬ · две ссылки из четырёх откладывающие («porting tracked as US #641»), но 6eafd422 в тот же день порт делает во все 3 профиля + ebccfcd6 дочиняет; в боевой БД у 55bdb714/7f35e710/687db0e8 approve-docs и run-implementation содержат top-level session и не содержат старый hand-off, re-pin выполнен
#663 · ЗАКРЫТЬ · 4fe722fb закрывает все 3 пункта фикса (find-first, is_default+миграция 0036, структурные candidates, workspace_id в whoami); tests/workspace/test_provision_installation.py 10 passed, e2e 63 passed; в проде у org iva base помечен is_default
#657 · ЗАКРЫТЬ · b0094ae0 покрывает все 5 профилей ×3 формата агентов + 3 скилла + 3 фрагмента, approve-docs не тронут как и требовалось; формулировка «omit it» в живых текстах не осталась, оракул assert_no_stale_id_advice зелёный
#661 · НЕ ЗАКРЫВАТЬ · единственный коммит 9917c59e делает US #669 и называет #661 потребителем («RE wiki-publisher (US #661) no longer reads…»); работы в origin/main нет, она в репозитории RE; последний комментарий называет остаток — MR !24 не влит, #667/#668 открыты; RE-хост недоступен, cron не проверен
#697 · НЕ ЗАКРЫВАТЬ · 480e1656 сам пишет «sibling profiles + the agent-auto-trigger vector tracked in Taiga #697», и оба долга живы: вектор 2 не сделан нигде (Triggers on «ТЗ» в 8 профилях из 8, runtime-гейта на MCP-диспатч нет), brownfield-task-workflow в рамках задачи не чинился и до сих пор без tool_timeout_sec (19 шаблонов из 25 без него — замер на сегодня), iva-java-backend-brownfield в main отсутствует
#713 · СПОРНО · реализация полная и раскатана — в боевом каталоге шаг 7б есть в 16 активных версиях (fr-analyst с 0.1.5 по 0.2.2, analysis-fr с 0.1.0), но обе приёмки не подтверждены: визуальный рендер у владельца назван остатком, прогон /start-feature нигде не зафиксирован
```

---

ВЕРДИКТ: частично (4 к закрытию, 2 не закрывать, 1 спорно)
Проверено: 7 задач блока 3 — дифф 14 коммитов против текста задач + тела всех 14 коммитов
целиком на шаблон «ссылка = долг остаётся», состояние origin/main (HEAD d036f3d8), тесты в
изолированном worktree, боевая БД каталога tacticum_prod
Данные: 14/14 коммитов — ancestor origin/main; из них 4 упоминают номер как ОТЛОЖЕННЫЙ
(480e1656→#697, 9917c59e→#661, f6fb02d1 и 3bb30fe3→#641); тесты: 10 + 63 + 19 passed,
0 failed; check_profile_version_discipline — 64 профиля clean; tool_timeout_sec — 6
codex-шаблонов из 25, то есть 19 без него (замер 07.08 совпал с аудитом 05.08 в точности;
в скоупе #697: 5 из 7 профилей, brownfield-task-workflow без параметра,
iva-java-backend-brownfield отсутствует); авто-триггер «ТЗ» — 8 профилей из 8 носителей
агента (0 исправлено); прод: base у org iva is_default=t, инсталляции 55bdb714/7f35e710/
687db0e8 на 0.5.4/0.8.1/0.12.1 с top-level-текстом команд, шаг 7б диаграмм в 16 активных
версиях аналитических профилей (в 0.1.3, названной в задаче, его нет — фича с 0.1.5)
Подтверждение: `git merge-base --is-ancestor` ×14 · `git log --no-walk --format=%B` ×14 ·
`pytest tests/workspace/
test_provision_installation.py` = 10 passed · `pytest tests/identity/test_whoami_mcp.py
tests/e2e_install/test_install_flow.py` = 63 passed · `pytest tests/e2e_install/
test_start_task_no_subagent.py` = 19 passed · `python scripts/
check_profile_version_discipline.py` = «OK — 64 profile(s) clean» · SELECT по
tacticum_catalog на tacticum_prod (workspaces.is_default, installations.profile_version_pinned,
ingredients.body ILIKE) · прогоны 2026-08-07
НЕ проверено: cron и логи wiki-sync на прод-RE (#661) — хост 194.36.208.242 не отвечает на
handshake, нужен IVA-VPN; состояние MR !23/!24 в git.cifragen.ru — доступа к GitLab нет;
рендер drawio на wiki.iva.ru стр. 211846230 (#713) — за VPN; поведенческая приёмка #641
(живой прогон /approve-docs со спавном воркеров) — автотеста нет, наблюдения не было;
A/B-числа #640 взяты из комментариев задачи, своего замера трейсов я не делал; тесты гонял
только относящиеся к разбираемым задачам, полный сьют бэкенда не прогонял; тела коммитов
читал только у 14 привязанных — задачи могли получать работу и коммитами БЕЗ номера, такие
я не искал.
