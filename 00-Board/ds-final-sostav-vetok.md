---
title: Финальный состав веток перед PR — сверено по удалённому репозиторию
type: note
status: current
created: 2026-07-27
repo: tacticum-dev
project: дизайн-система / неделя KMP
tags:
- board
- design-system
- release
permalink: tacticum/00-board/ds-final-sostav-vetok
---

# Состав: 12 веток в удалённом репозитории

Не по памяти — по `git branch -r` с датами коммитов. Все двенадцать доставлены 2026-07-27.

## Расхождение в счёте — откуда взялось

**У ГД 10 рабочих + рунбук = 11. По факту 11 рабочих + рунбук = 12.**
Недосчитана одна из двух последних доставок: `fix/ds-mode-shape-mismatch` (фикс потери тёмной темы,
пришёл от внешней сверки уже вечером) либо `feat/kmp-agents-router` (маршрутизатор, доставлен
последним). Обе появились после того, как состав считался в прошлый раз.

## PR 1 — платформенный фикс, мержится ПЕРВЫМ

| # | Ветка | Вершина |
|---|---|---|
| 1 | `fix/skill-spec-assets-metadata` | `488601a` |

Отдельная линия от `main`, **не предок цепочки профилей** (проверено `merge-base --is-ancestor`).
Чинит установку под вторым CLI — **дефект живой, подтверждён на проде**.

## PR 2 — профили, вершина `feat/kmp-agents-router` (`2a514c4`)

Стек из шести веток, снизу вверх:

| # | Ветка | Вершина |
|---|---|---|
| 2 | `fix/kmp-role-compose-skills` | `47426bf` |
| 3 | `feat/kmp-theme-update` | `2177d5c` |
| 4 | `feat/kmp-screen-redesign` | `c94cb17` |
| 5 | `feat/kmp-component-authoring` | `c49ce52` |
| 6 | `feat/kmp-figma-numeric-compare` | `e66ad4d` |
| 7 | `feat/kmp-agents-router` | `2a514c4` ← **вершина PR** |

## PR 3 — сборщик токенов, вершина `fix/ds-mode-shape-mismatch` (`ef043cb`)

Стек из четырёх веток, снизу вверх:

| # | Ветка | Вершина |
|---|---|---|
| 8 | `fix/ds-bindings-preserve` | `161f347` |
| 9 | `fix/ds-hidden-and-aliases` | `117e9ba` |
| 10 | `feat/ds-profiles-config` | `4897f2b` |
| 11 | `fix/ds-mode-shape-mismatch` | `ef043cb` ← **вершина PR** |

Профилей не касается, сида и перевода закреплённых версий не требует.

## Документация

| # | Ветка | Вершина |
|---|---|---|
| 12 | `docs/rollback-runbook-ds-2026-07-27` | `9aa06c1` |

Только новый файл рунбука. Отдельным PR или приложением к любому — на код и профили не влияет.

## Отбракованная — риска нет

`feat/kmp-token-migration` (первая, ошибочная формулировка порции 1) **в удалённый репозиторий не
запушена вовсе** — проверено, ветки там нет. Значит подобрать её случайно при сборке PR невозможно.
Живёт только локальным worktree и помечена статусом в своей заметке.

## Итого

**11 рабочих + 1 документация = 12.** Три PR по вершинам плюс документация.
Порядок: PR 1 → PR 2, PR 3 независимо.