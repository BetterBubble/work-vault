---
title: gate-qa-web-2026-07-29
type: report
permalink: tacticum/00-board/gate-qa-web-2026-07-29
tags:
- gate
- qa
- controller
---

# Гейт: `refactor/qa-lane-split` + `feat/qa-role-web` (HEAD `6d6343e`)

**Вердикт: NO-GO — один блокер.** Блокер механический и чинится в один коммит; вся содержательная
часть работы гейт прошла, включая три собственные мутации контролёра.

## Блокер

**Дисциплина версий краснеет на втором PR.** Коммит `6d6343e` (последний, лёг ПОСЛЕ снятия чисел
тимлидом) правит `templates/iva-pytest-playwright-canvas-autotest-base/manifest.yaml` — комментарий
про `playwright-cli` и абзац `description` — а `version` остаётся `"0.1.0"`, записи в CHANGELOG нет.

```
$ uv run python ../../scripts/check_profile_version_discipline.py --diff-against refactor/qa-lane-split
Profile version discipline violations (1):
  ✗ iva-pytest-playwright-canvas-autotest-base: content changed (manifest.yaml) but
    manifest.version stayed at '0.1.0'. Bump it and add a CHANGELOG entry
```

Против `origin/main` чисто (57 профилей) только потому, что canvas-лейна на `origin/main` ещё нет —
новый профиль бампа не требует. Но CI (`profile-version-discipline.yml`) диффает против
`origin/${{ github.base_ref }}`, то есть против БАЗЫ PR, и обе базы дают красный:

- база `refactor/qa-lane-split` → воспроизведено выше;
- база `main` после мержа PR-1 → на main приедет canvas-лейн 0.1.0 с манифестом ДО `6d6343e`,
  и PR-2 меняет содержимое того же 0.1.0. Это ровно тот отказ, ради которого проверка написана
  (`version_already_exists_with_different_content` на сиде).

Единственный коммит, тронувший canvas-лейн во второй ветке, — `6d6343e`. Правится бампом до 0.1.1 с
записью в CHANGELOG либо переносом `6d6343e` в первую ветку. Решает тимлид, контролёр не правит.

## Прошло

- **Механический гейт** `gate-check.sh` — нарушений нет: ветка не main, дерево чистое, секретов и
  мусора в диффе нет, AI-подписей в коммитах нет, коммитов 16, автор один.
- **`pytest tests/catalog/ tests/e2e_install/`** — `tests=1009 failures=0 errors=0 skipped=0`
  (снято через junit-xml, потому что итоговая строка pytest в этой конфигурации не печатается).
  Число тимлида подтверждено точно.
- **Ссылки** — `--profile iva-role-qa --profile iva-role-qa-web --mode links`: 4 пары чисто.
  `--mode paths --all-profiles`: 102 пары чисто. Оба числа подтверждены.
- **Скоуп против плана** `design-qa-split-2026-07-29` — совпадает по §1–§7, разрастания нет.

## Проверка, которая проверяет — три мутации контролёра (все откачены, дерево чистое)

Тимлид просил перепроверить своей мутацией. Сделаны три, каждая краснит:

1. `stacks/{stack}/pytest-runner.md` → несуществующий файл — **8 падений**, обе роли, оба CLI.
2. Глубокий путь `stacks/{stack}/rules/page-objects.md` → несуществующий — **4 падения**.
   Закрывает вопрос «а если `{stack}` не последний сегмент».
3. Каталог стека не приехал под `stacks/` (11 `target_file` уведены в другой префикс) —
   **6 падений**, включая `test_role_has_exactly_one_stack_home`. Закрывает вопрос «а если каталога
   стека вовсе нет»: молчаливого зелёного не будет.

Дыра «резолв `{stack}` молча отбрасывает кандидатов» закрыта по существу, а не по галочке.

## Регресс canvas-роли — главный риск, не подтвердился

- **Дорога к канону цела.** Инвариант «в каждом теле минимум один якорь с каталогом» проверен
  независимо: из 20 тел процессного лейна, упоминающих файлы канона стека, **якорь есть у 20**,
  тел с голым именем файла — **0**.
- **Расцепление доведено до конца.** Литеральных `pytest-playwright-canvas` в телах процессного
  лейна остался **один**, и он законный: `craft-stack/SKILL.md` называет оба стека по делу.
- **Команда запуска у canvas рабочая.** База `<python-проекта> -m pytest -k <test>`; канон canvas
  (`pytest-runner.md`) прямо говорит, что больше ничего не нужно. `--headed` в каноне canvas
  сохранён (6 мест), в процессном лейне остался как явный «референс canvas».
- **Деревья ролей.** Композиции `iva-role-qa` и `iva-role-qa-web` отличаются ровно одним лейном —
  стек-лейном. 34 теста на лейны и непересечение `target_file` зелёные.

## Долг `iva-role-go` — предсуществующий, доказано

116 битых ссылок — не наши. Ветки не трогают ни одного go-файла, но чекер менялся (`_PATH_CHARS`
получил `{}`), поэтому эффект изолирован отдельно: на том же дереве запущен чекер с откаченным
поведением `origin/main` — **116 и 116**. Ни одной новой битой ссылки не добавлено.

## Не блокер, но

1. **Три тела субагентов не объясняют `{stack}`.** `agents/dom-explorer.md`, `agents/code-writer.md`,
   `agents/codebase-analyst.md` и их codex-двойники несут якорь `.agent-kit/craft/stacks/{stack}/…`,
   но откуда берётся значение — не говорят; объясняют это только SKILL-тела. Субагент читает своё
   тело изолированно. Не смертельно: каталог в якоре назван, в нём ровно один подкаталог, `ls`
   решает вопрос за шаг. Но сказано это только в скиллах.
2. **`pytest-randomly` у canvas остался без ответа.** Флаг `-p no:randomly` убран из шаблонов
   правильно, но новая формулировка в `reproduce-phases.md` («проект рандомизирует порядок → добавь
   флаг») отправляет агента за ответом, которого в каноне canvas нет: слово `randomly` в
   `iva-pytest-playwright-canvas-autotest-base` не встречается **ни разу**, тогда как Selenium-канон
   документирует его явно (`pytest-runner.md:46`). Асимметрия: canvas потерял ответ, который раньше
   был вшит в шаблон, и теперь агенту надо смотреть `requirements.txt` продукта.
3. **`ROLES_WITH_STACK` — захардкоженный список из двух ролей.** Сейчас он полон (проверено: стек-лейн
   в `depends_on` только у `iva-role-qa` и `iva-role-qa-web`). Но третью роль со стек-лейном в него
   никто не заставит вписать, и она окажется вне проверки молча — тот же класс, ради которого тест и
   писался.
4. **Новый внутренний IP в репозитории.** `10.6.0.219` (mac-selenoid) в
   `iva-pytest-selenium-autotest-base/ingredients/stack/recon.md`, 3 вхождения, на `origin/main` его
   нет. Не секрет (RFC1918, вне сети бесполезен), и внутренние хосты в репозитории уже есть
   (`jira.iva.ru`, `helm.tacticum.ru`) — то есть практике не противоречит. Стоит осознанного решения,
   если репозиторий публичный или станет им.
5. **Отступление от плана §7.3 по версии.** План предписывал `tacticum-autotest-core` = **1.0.0**
   («пути канона несовместимы с 0.5.0»), фактически **0.2.0**. Отступление обоснованное: profile_id
   новый, на `origin/main` его нет, нумерация начинается заново, а мажор в плане подразумевал
   преемственность с 0.5.0. Отмечено, чтобы расхождение было осознанным, а не тихим.

## Расхождение с числами тимлида

Одно: «дисциплина версий → 57 чисто, в т.ч. `--diff-against refactor/qa-lane-split`». Против
`origin/main` — да, 57 чисто. Против `refactor/qa-lane-split` — **1 нарушение** (команда и вывод
выше). Причина расхождения: числа снимались до коммита `6d6343e`, который и внёс нарушение.

## Чего гейт не проверял

Деплоя в этой работе нет — пункт неприменим. Живого прогона роли на реальном проекте контролёр не
делал: проверено дерево установки, содержание тел и приёмочные проверки, но не работа роли на живом
репозитории под инженером.

- relates_to [[design-qa-split-2026-07-29]]
- relates_to [[gate-qa-canon-2026-07-28]]

---

# Дельта-гейт: `d649d87`..`b5b5838` (HEAD `b5b5838`)

**Вердикт по дельте: GO.** Блокер снят, все три моих замечания закрыты (одно — не полностью,
см. ниже). Новых блокеров нет. Полный гейт не переигрывался: проверено только легшее после `6d6343e`.

## Блокер снят — проверено самостоятельно

Canvas-лейн доведён до **0.1.2** с двумя записями в CHANGELOG (`0.1.1` — правка манифеста из
`6d6343e`, `0.1.2` — контент `pytest-runner.md` из `2c58139`). Дисциплина версий: **57 чисто при
обеих базах** — и `--diff-against refactor/qa-lane-split`, и `--diff-against origin/main`. Тот
случай, когда бампов сделано ровно столько, сколько содержательных правок, а не один «на всё».

`gate-check.sh` на новом HEAD: нарушений нет, 20 коммитов, дерево чистое, автор один, ни мусора,
ни AI-подписей в дельте.

**Приёмка подтверждена независимо:** `tests=1044 failures=0 errors=0 skipped=0` (junit). Сходится и
арифметически: 1009 → 1044 это ровно +35, а файл `test_stack_placeholder_links.py` вырос с 12
тестов (3 функции × 2 CLI × 2 роли) до 47.

## 1. Правка canvas-канона — факт проверен по выгрузке, не по пересказу

Читал `~/tacticum-vault/90-Materials/one-web-kmp-main.zip` напрямую:

- `requirements.txt` — `pytest`, `pytest-xdist`, `allure-pytest`, `allure-python-commons`,
  `playwright` и прочее. **`pytest-randomly` отсутствует.**
- `pytest.ini` — `addopts = -v -p no:cacheprovider --tb=native --showlocals --strict-markers
  --alluredir=./allure-raw`. Рандомизации нет.
- Поиск строки `randomly` по **всей** выгрузке — **0 вхождений**.

Факт верен. **Границу воркер не перегнул:** формулировка «свойство ПРОЕКТА, а не стека» стоит прямо
под утверждением про стек, рядом лежит `grep -ri "pytest-randomly" requirements.txt pytest.ini
pyproject.toml` и явное «нашёлся — добавляй, не нашёлся — не добавляй». Флаг стеку не запрещён, он
привязан к проверяемому факту репозитория. Замечание №2 прежнего гейта закрыто.

Побочно выяснено (и это меняет оценку прежней правки, а не новой): **`-p no:randomly` не роняет
pytest, когда плагина нет.** Проверено прогоном — `pytest ... -p no:randomly` при отсутствующем
`pytest_randomly` даёт EXIT=0. То есть удаление флага в `67bc0fc` чинило честность канона, а не
падение; общая фраза того CHANGELOG про «неизвестная раннеру опция роняет pytest на старте» верна
для `--headed`/`--local`/`--browser-tag`, но не для `-p no:randomly`, который идёт первым пунктом
того же списка. На дельту не влияет, на мерж не влияет.

## 2. Полнота новых проверок — источники действительно независимы

`test_every_role_with_a_stack_dir_is_under_check` сверяет два вывода, и это **не один факт,
прочитанный дважды**:

- **Источник A** (`_stack_lane_slug`) — манифесты лейнов, поле `metadata.target_file`, своя
  регулярка без якоря.
- **Источник B** (`checker._stack_slug`) — фактическое дерево установки после резолва `depends_on`
  и CLI-раскладки, своя регулярка `^[a-z]+/…` по УСТАНОВОЧНОМУ пути.

Общий у них только YAML на диске — то есть сам проверяемый факт, а не механизм его вывода.
Доказано мутацией: тимлид ломал регулярку источника A, я сломал **только источник B**
(`_STACK_HOME_RE` в чекере, источник A не тронут) — **8 падений**, в том числе именно
`test_every_role_with_a_stack_dir_is_under_check` с веткой «числится ролью со стеком, но каталога
стека в дереве нет». Расхождение ловится с обеих сторон, а не с одной.

**Проверено и то, ради чего всё делалось.** Завёл третью роль со стек-лейном (копия
`iva-role-qa-web` под другим `profile_id`), тест НЕ правил: файл вырос с 47 до **55 собранных**
тестов, все прошли. Новая роль подхватывается выводом автоматически. Замечание №3 прежнего гейта
закрыто по существу.

## 3. Golden — сверено составом

Обе выгрузки, оба CLI:

- путей **80 → 80**, множество путей **побайтово то же** (diff отсортированных ключей пуст);
- изменившихся хэшей ровно **4** на каждый CLI, и это ровно те тела, что правились:
  `stacks/pytest-playwright-canvas/pytest-runner.md` + три субагента
  (`.claude/agents/*.md` для claude-code, `.codex/agents/*.toml` для codex).

Заявление тимлида подтверждено.

**Golden `iva-role-qa-web` в этой ветке не нужен, но долг реальный.** Покрытие golden в этом
репозитории — не автоматическое: под каждый профиль пишется отдельная тест-функция в
`test_install_flow.py` с захардкоженным `profile_id`. Без golden живут и `iva-role-architect`, и
`tacticum-role-techwriter` — прецедент есть. При этом состав `iva-role-qa-web` НЕ беззащитен: роль
входит в `ROLE_LANES` и покрыта `test_golden_parity_union_equals_sum_of_lanes` (композиция = объединение
лейнов, строгая дизъюнктность), плюс все четыре теста плейсхолдера на обоих CLI и `check_install_links`
в CI. Чего нет — побайтовой фиксации СОДЕРЖИМОГО. Заводить её здесь означало бы дописать
e2e-функцию с сидом, то есть расширить объём ветки, стоящей на гейте. Рекомендую **отдельной
задачей**, а не в этом PR — но записать, потому что дрейфовать будет именно вторая роль: её
стек-лейн новый.

## Не блокер, но (по дельте)

1. **Замечание №1 закрыто на шесть тел из восьми.** Все шесть субагентов (`dom-explorer`,
   `code-writer`, `codebase-analyst` × claude/codex) теперь объясняют `{stack}` явно, с инструкцией
   поиска («смотри, что лежит в `.agent-kit/craft/stacks/`»). Но два SKILL-тела верхнего уровня
   по-прежнему несут якорь без объяснения: `skills/run-tests/SKILL.md:12` и
   `skills/prepare-mr-branch/SKILL.md:73`. `run-tests` вызывается самостоятельно («прогони тесты») и
   как раз собирает команду по канону стека, так что случай не гипотетический. Масштаб прежний —
   один `ls` — но записывать это как «закрыто полностью» было бы неправдой.
2. **Мелкая фактическая неточность в новом тексте canvas-канона.** Сказано «в `requirements.txt` из
   pytest-плагинов только `pytest` и `pytest-xdist`». На деле там же `allure-pytest==2.13.0` — тоже
   pytest-плагин, и именно он даёт `--alluredir`, на который строкой выше ссылается тот же файл.
   На вывод про рандомизацию не влияет, но канон противоречит сам себе в соседних абзацах.

## Чего дельта-гейт не проверял

Прежние замечания №4 (внутренний IP `10.6.0.219`) и №5 (отступление от плана §7.3 по версии
`tacticum-autotest-core`) дельта не трогала — остаются как были, решение по ним за тимлидом.
Живого прогона роли на реальном репозитории под инженером по-прежнему не делалось.

---

# Финальный гейт: `8ad7489`..`bc18903` (HEAD `bc18903`)

**Вердикт: NO-GO — один блокер, узкий.** Правка `--env` доведена в четырёх копиях
утверждения из шести; две оставшиеся дают ровно тот отказ, ради которого блокер и заводили.
Всё остальное в дельте — включая оба заявленных блокера, общий тест `ide_targets` и golden —
проверку прошло. Правка блокера — по абзацу в двух файлах.

Дельта оказалась из **пяти** коммитов, а не четырёх: тимлид не назвал `8ad7489` и `0eb79ef`,
которые закрывают мои прежние замечания. Они проверены вместе с остальными.

## Блокер: `--env` не досинхронизирован в Block 3 `prepare-mr-branch`

Правка `run-tests` сделана **правильно** — стек-зависимой, не перевёрнутой на Selenium
(разбор ниже). Но то же утверждение живёт в шести местах, и синхронизированы четыре:

| Место | Состояние |
|---|---|
| `run-tests/SKILL.md` §«Стенд (env)» + таблица «чего не добавляем» | исправлено |
| `prepare-mr-branch/SKILL.md:73` (Phase 6) | исправлено |
| `jira-issue-autotest/SKILL.md` claude + codex | исправлено, копии побайтово совпадают |
| **`prepare-mr-branch/SKILL.md:308–309` (Block 3)** | **не тронуто** |
| **`prepare-mr-branch/references/output-blocks.md:144,152`** | **не тронуто** |

Оставшийся текст даёт агенту ровно две ветки: «дефолтный стенд инжектит conftest — не
ставим» и «явный `--env` только когда прогон осознанно уводится на другой стенд». На
Selenium ложны обе: дефолта нет, и пользователь никуда прогон не уводил, — а флаг
обязателен. Агент опускает `--env`, и Block 3 печатает человеку команду, падающую с
`KeyError` до первого теста. Это тот же отказ, тот же стек и тот же механизм, что в
исправленном блокере, только в артефакте, который отдают в руки.

Смягчение есть и оно частичное: `output-blocks.md:138` честно говорит «состав команды
берётся из канона стека, а не из этого файла». Но пятью строками ниже тот же абзац даёт
конкретное правило про `--env`, и конкретное правило для агента сильнее отсылки.

CHANGELOG при этом **не врёт**: в `0.4.0` сказано «то же исправлено в трёх копиях:
`prepare-mr-branch` (Phase 6) и `jira-issue-autotest` обоих CLI» — Block 3 в область не
заявлен. То есть это недоведённая правка, а не ложное утверждение о ней.

## 1. Правка `--env` — стек-зависимая, факт сверен по обеим выгрузкам

Проверял распаковкой сам, оба `conftest.py`:

- `one-web-kmp` (canvas): `parser.addoption('--env', ..., default='kmp-stage')` (строка 367),
  и сверх того строки 9–11 дописывают `--env` в `sys.argv`, если его нет. Флаг действительно
  не нужен — **canvas не сломан**.
- `one-web` (Selenium): `parser.addoption('--env', action='store', help='Environment name')`
  (строка 310) — **без `default`**.

Заявленное следствие тоже проверено, а не принято на слово: `get_config()[option.env]` в
session-фикстуре `setup` (строка 123). При `option.env = None` это `KeyError`, и, поскольку
фикстура сессионная, падают все тесты разом — как и сказано.

Утверждение сделано условным, обоим стекам отвечают их каноны: Selenium-канон ставит
`--env <default_env>` во всех примерах команд, canvas-канон явно говорит «не ставим».
Копии в `jira-issue-autotest` для обоих CLI **не разошлись** — абзац идентичен.

Мелочь на будущее: `run-tests` велит «прочитай раздел про `--env` в `pytest-runner.md`
своего стека», но раздела с таким заголовком нет ни в одном каноне — ответ лежит в таблице
флагов. Находится, но фраза обещает структуру, которой нет.

## 2. Общий тест `ide_targets` — послабление не протекло

Фактическое состояние каталога проверено: `best-effort` объявляет **ровно одна** роль
(`iva-role-qa-web`), остальные шестнадцать — `full`. Утечки нет.

Мутировал с другой стороны, чем тимлид (он ломал «понижение без записи»), — три мутации,
все краснеют, откат чист:

1. в список внесена роль, которая объявляет `full` (`iva-role-qa`) → `assert 'full' ==
   'best-effort'`, устаревшая запись поймана;
2. в список внесена несуществующая роль → красит `test_codex_best_effort_exceptions_are_justified`;
3. причина-отписка («так надо») → `assert 8 >= 40`.

Вместе с мутацией тимлида закрыты все четыре направления. Значение вне
`full`/`best-effort` (например `unsupported`) тоже не проходит ни по одной ветке.

**Понижение ничего не сломало:** `ide_targets` **не имеет потребителей в коде бэкенда** —
это декларативные метаданные, проверяемые только тестами. `best-effort` — валидное значение
енума в `templates/_schema/manifest.v2.schema.json`. Само понижение честное: web-роль
доставляет **0** путей `playwright-cli` (проверено по дереву установки), тогда как
canvas-роль — **17** на обоих CLI. То есть у web-роли драйвер и правда только от апстрима.

## 3. `selenoid_*.py` — заявленного как существующее не осталось

Прошёл все упоминания в живых лейнах: манифест и README web-роли, README и манифест
стек-лейна, `exclusion-patterns.md` процессного лейна — везде «профиль НЕ доставляет».
В `recon.md` секция прямо озаглавлена «Хелпер — в поставке профиля его НЕТ», вызовы стоят
под «Если скрипт в проекте **есть**» и помечены как контракт-спецификация. В
`manual-walkthrough.md` таблица инструментов says «**если он в проекте есть**», а шаг 3.2
даёт `ls`-проверку и обе ветки: «Есть — запускай / Нет — переходи к 3.3 и скажи
пользователю прямо, а не подставляй несуществующую команду». Два оставшихся упоминания —
в замороженном монолите `iva-qa-autotest-base`, вне области.

**Деградация исполнима, не отписка:** шаг 1 велит продолжать разведку через
`playwright-cli open --browser=webkit`, и такой флаг у инструмента действительно есть
(`playwright-cli/SKILL.md:249`).

## 4. Обход для codex — команда корректна

`mkdir -p .agents/skills/playwright-cli && cp -R .claude/skills/playwright-cli/.
.agents/skills/playwright-cli/` — форма `cp -R src/. dst/` копирует именно содержимое,
включая точечные файлы; каталог назначения совпадает с домом навыков Codex (в дереве
установки codex скиллы лежат под `repo/.agents/skills/`). Про необновляемость сказано
рядом и явно: «Копия не мешает повторному `install --skills`, но и не обновляется сама —
после обновления инструмента повторить».

## Ревизия Т1–Т10 — выборочно

- **README процессного лейна теперь совпадает с фактом.** Прежний текст утверждал, что
  стековые куски вырезаны; добавленный абзац прямо говорит, что разрез вынес ФАЙЛЫ, а
  стековые УТВЕРЖДЕНИЯ остались и всплыли на втором стеке, с перечнем — включая `--env`
  и `KeyError`. Ровно тот случай, когда норма перестала описывать несуществующее.
- **`retro`** — захардкоженные `at-creation-cmain`/`at-creation-crc` заменены на
  плейсхолдеры с `git rev-parse --verify` и веткой «второй линии нет — пропускаем».
- **Шапки скиллов стек-нейтральны:** `description` у `fix-failed-test` обоих CLI теперь
  «в тест-проекте», не «в one-web-kmp». Оставшиеся упоминания `one-web-kmp` в телах — все
  под пометкой «референс», то есть по конвенции.
- **Контракт `{stack}` при двух каталогах** (`agent-kit/README.md`) не противоречит коду:
  чекер краснит на двух каталогах в дереве ОДНОЙ роли (реальный дефект композиции), а
  документ описывает две РОЛИ в одном workspace — это разные вещи. Решение «не падать, а
  выбирать и назвать вслух» обосновано.

## Мои прежние замечания закрыты полностью

- **`{stack}` без объяснения** — было 2 из 8 тел (`run-tests`, `prepare-mr-branch`), стало
  **0**. Якорный инвариант держится: 20 тел с каноном стека, якорь у 20.
- **`allure-pytest` назван не-плагином** — исправлено точно: «из pytest-плагинов там
  `pytest-xdist` — параллель, и `allure-pytest` — отчёты и `--alluredir`; порядок тестов не
  трогает ни один».
- **Golden `iva-role-qa-web`** — заведён (74 пути на оба CLI), роль внесена в живой смоук и
  в `_GENERIC_ROLES` с `absent: playwright-cli`, то есть под проверку встало именно то
  различие, ради которого роль заведена. Долг, записанный прошлым разделом, погашен.

## Механика и числа

`gate-check.sh`: нарушений нет, 25 коммитов, дерево чистое, автор один, в дельте ни мусора,
ни AI-подписей. Дисциплина версий — **57 чисто при обеих базах**. Приёмка подтверждена
независимо: `tests=1054 failures=0 errors=0 skipped=0` (junit). Golden по составу:
`iva-role-qa` 80/80 на оба CLI, `iva-role-qa-web` 74/74; `playwright-cli` в golden web-роли
отсутствует (0), в canvas-роли присутствует (8).

## Не блокер, но (по дельте)

1. **Отсылка к несуществующему разделу.** `run-tests` шлёт «прочитай раздел про `--env` в
   `pytest-runner.md` своего стека»; раздела с таким заголовком нет ни в canvas-каноне, ни в
   Selenium-каноне — ответ в таблице флагов. Находится, но обещана структура, которой нет.
2. **Selenium-канон не говорит, ПОЧЕМУ `--env` обязателен.** Флаг стоит во всех примерах
   команд, и этого хватает, чтобы скопировать; но строка таблицы объясняет лишь «какой
   стенд», а не «без него `KeyError`». Агент, решивший флаг опустить, опровержения там не
   найдёт.

## Чего гейт не проверял

Прежние замечания №4 (внутренний IP `10.6.0.219`) и №5 (версия `tacticum-autotest-core`)
дельта не трогала. Живого прогона роли на реальном репозитории под инженером по-прежнему не
делалось — проверено дерево установки, содержание тел, факты по выгрузкам и приёмочные
проверки.

---

# Догон: `0d02225` (HEAD `0d02225`)

**Вердикт: GO.** Блокер закрыт по существу, оба моих «не блокера» закрыты, новых блокеров
нет. Остаточное замечание одно, ниже, и оно не блокирующее.

## Блокер закрыт — и закрыт правильным способом

Правило про `--env` в Block 3 **не убрано, а сделано стек-зависимым** — это верный выбор:
я в прошлом разделе писал, что конкретное указание для агента сильнее отсылки, и лечится это
не удалением конкретики, а её исправлением. Правлено в обоих файлах:

- `prepare-mr-branch/SKILL.md:310` — «`--env` — тоже свойство стека, и „по умолчанию не
  ставим“ тут неверно», с обеими ветками и словами «обязателен **в печатаемой команде**»;
- `output-blocks.md:144` — то же для первой формулировки;
- `output-blocks.md:156` — **вторая формулировка тоже переписана**. Прежняя («явный `--env`
  добавляется, только если пользователь сам увёл прогон») была тем же тупиком с другой
  стороны; я её отдельно не называл, найдена самостоятельно. Теперь на её месте — как
  выбрать стенд, когда флаг обязателен, и требование назвать выбранный стенд вслух рядом
  с блоком.

## 1. Шестого места нет

Искал по смыслу, а не по фразе: прогнал все живые лейны и роли на утверждения про стенд с
модальностью («не ставим / не добавляем / обязателен / всегда / только если»). Нашлось 23
совпадения; после разбора картина такая:

- **в общих телах** (процессный лейн, `iva-qa-delivery-base`) — все утверждения условные:
  `run-tests` (§«Стенд (env)», указатель, таблица «чего не добавляем»),
  `prepare-mr-branch` Phase 6 и Block 3, `output-blocks.md`, `jira-issue-autotest` обоих
  CLI. Безусловных не осталось;
- **в канонах стеков** абсолютные формулировки законны и на месте: selenium
  (`pytest-runner.md`, `batch-conventions.md`), canvas (`pytest-runner.md`,
  `batch-conventions.md`);
- остальное — ложные срабатывания на слово «стенд» в другом смысле (санитизация TC,
  доступность TMS, параллельность браузеров).

Формулировки копий между собой не разошлись: абзац в `jira-issue-autotest` claude и codex
идентичен, а Block 3 и `run-tests` говорят одно и то же разными словами, но с одинаковым
выводом. Заодно проверено заявление «Selenium-канон ставит `--env` и в батч-конвенциях» —
верно, `batch-conventions.md:47,68`.

## 2. Симметрия — canvas не сломан

Разделы заведены в обоих канонах, и canvas получил именно свой:

- selenium — «`--env` обязателен — почему, а не просто „ставь“»;
- canvas — «`--env` не нужен — почему», и текст **не заставляет ставить флаг**: «Поэтому
  `--env` в команды этого стека не добавляется — он ничего не меняет». Явный флаг оставлен
  осмысленным только для осознанного увода на другой стенд, как и было.

Предупреждение против переноса вывода стоит с **обеих** сторон, а не только со стороны
Selenium: canvas-канон отдельным блоком говорит, что у мультибраузерного стека дефолта нет
и там отсутствие флага даёт `KeyError`.

## 3. Механизм записан фактом — сверено по выгрузкам

Цитаты кода сверил сам, побайтово:

| Утверждение канона | Выгрузка | Совпадает |
|---|---|---|
| `parser.addoption('--env', action='store', help='Environment name')` | `one-web/conftest.py:310` | да |
| `parser.addoption('--env', action='store', help='Environment name', default='kmp-stage')` | `one-web-kmp/conftest.py:367` | да |
| `'config': get_config()[option.env]` | `one-web/conftest.py:123` | да |
| фикстура `setup` сессионная | `one-web/conftest.py:116` — `@pytest.fixture(scope='session')`, `def setup` на 117 | да |

Деталь про сессионность фикстуры действительно объясняет 28.07 и в канон записана верно:
падает не тест, а прогон целиком, и со стороны человека это «всё сломалось».

**Уточнение к постановке:** ссылок на НОМЕРА строк в канонах нет — там процитированы
литералы кода. Это устойчивее (расходиться нечему), но проверять на разъезд номеров было
нечего; описание задачи здесь чуть расходится с артефактом.

## Мои прежние «не блокера» закрыты

- **Указатель вёл на несуществующий заголовок** — исправлено: `run-tests` теперь шлёт к
  разделу, начинающемуся с `--env` («`--env` обязателен — почему» либо «`--env` не нужен —
  почему»), и разделы с такими заголовками реально заведены в обоих канонах.
- **Selenium-канон не объяснял, почему флаг обязателен** — теперь объясняет механизмом:
  объявление без дефолта → `option.env` = `None` → `config[None]` → `KeyError` → падает весь
  прогон, потому что фикстура сессионная. Плюс отсылка к инциденту 28.07.

## Числа — подтверждены независимо

`gate-check.sh`: нарушений нет, 26 коммитов, дерево чистое, ни мусора, ни AI-подписей.
Приёмка: `tests=1054 failures=0 errors=0 skipped=0` (junit). Дисциплина версий — **57 чисто
при обеих базах**. Ссылки — 4 пары чисто. Бампы соответствуют заявленным: процессный лейн
0.4.1, selenium 0.2.1, canvas 0.1.5. CHANGELOG честен: прямо сказано «был доведён в четырёх
местах из шести», с причиной, почему Block 3 хуже остальных мест (команду отдают человеку).

Golden: состав не сдвинулся — `iva-role-qa` 80→80, `iva-role-qa-web` 74→74, множество путей
идентично; изменилось ровно **4 файла на каждый CLI** и у обеих ролей симметрично
(`prepare-mr-branch/SKILL.md`, `output-blocks.md`, `run-tests/SKILL.md` + `pytest-runner.md`
своего стека).

## Не блокер, но — одно остаточное

**Таблица примеров в `run-tests` (§«Примеры», строки 336–346) не согласована с собственной
нормой скилла.** Колонка называется «Финальная команда», но `--env` там нет ни в одной из
восьми строк — на Selenium каждая из этих «финальных команд» падает с `KeyError`. Показательно,
что та же таблица уже сделана стек-зависимой по другому флагу (последняя строка:
`<опция стека>` с пометкой «референс canvas: `--headed`»), то есть про стек-зависимость в
этом месте думали, но закрыли один флаг из двух.

Почему всё же не блокер: заголовок таблицы объявляет её назначение — «для понимания
**парсинга ввода**», то есть она учит отображению «`smoke` → `-m suite_smoke`», а не составу
команды; а нормативные разделы выше (§«Стенд (env)» и таблица «чего не добавляем», строка
166) теперь корректны и дважды отправляют за составом в канон стека. Риск — что агент возьмёт
строку таблицы как готовую; стоит поправить при ближайшем касании файла, отдельного захода не
требует.

## Чего гейт не проверял

Прежние замечания №4 (внутренний IP `10.6.0.219`) и №5 (версия `tacticum-autotest-core`
0.2.0 против плановой 1.0.0 — на сегодня 0.4.1) остаются как были, решение за тимлидом.
Живого прогона роли на реальном репозитории под инженером контролёр не делал ни разу за все
четыре захода: проверены дерево установки, содержание тел, факты по выгрузкам и приёмочные
проверки.
