---
title: Таск-менеджер «идеальный» — прогресс фаз (Ф0–Ф2 сделано, Ф3–Ф4 осталось)
type: session
permalink: tacticum/01-sessions/task-menedzher-idealnyi-progress-faz-f0-f2-sdelano-f3-f4-ostalos
tags:
- helm
- task-manager
- phases
- checkpoint
- session
- sql-read-path
---

Чекпойнт 2026-07-09. Пауза по просьбе пользователя. Ветка **`feature/task-manager-ideal`** (локально, **НЕ запушена**, от main `5e53057`). Правило сессии: локально, без пуша/деплоя, проверять достоверность (golden-тест).

## Сделано (влито, main зелёный: 813 passed, ruff чисто, фронт собирается)
- [done] **Ф0** (`4b4c0cf`) — read-path таск-менеджера на SQL вместо `load_graph` всего графа. `application/tasks.py: compute_task_hygiene` (SQL-агрегации по signal/initiative/dependency), серверные фильтры, индексы (миграция `f9a0b1c2d3e4`). **Golden-тест** `test_tasks_sql_golden.py`: SQL == старый граф-расчёт (достоверность). Архитектурное решение: [[Граф Signal→Initiative vs реляционные витрины — read-path на SQL]].
- [done] **Ф1** (`3426e37`) — копилот-гейт: очередь orphan-задач → `MockTaskGoalJudge` предлагает инициативу+confidence → оператор approve/reject/edit через HITL `review.py`+`operator_review` (`ENTITY_TASK_INITIATIVE`). Защита от затирания ингестом: `_approved_task_links` в `repository.persist_graph` (операторская привязка переживает `clear_graph`/пере-инжест). Фронт-вкладка в Settings.
- [done] **Ф2** (`f2b566d`) — квадранты «объём×покрытие» (медиана-порог) по команде/продукту/проекту. `domain/task_quadrants.py`, `GET /api/tasks/quadrants?axis=`, `resolve_product` в `eva_source.py`, скаттер-SVG в TaskHygiene. На SQL-пути (не load_graph).

## Осталось
- [todo] **Ф3 — тренд orphan-rate (snapshot read-model).** Референс-дизайн готов в worktree `.claude/worktrees/agent-abaa6f103883e0f39`: таблица `task_hygiene_snapshot` (миграция), `scripts/refresh_task_hygiene_snapshot.py` (зовёт ту же `compute_task_hygiene` — единый источник расчёта), `GET /api/tasks/hygiene-trend`, SVG-линия тренда. Образец паттерна — HRD (`refresh_hrd_snapshot.py`).
- [todo] **Ф4 — Jira↔EVA расхождение.** `GET /api/tasks/tracker-diff`: покрытие продуктов по трекерам, дубли (точный epic_key/ключ → фаззи позже), SoR-гэп. `domain/tracker_diff.py`. Зависит от Ф2 (product-резолв).

## Как продолжать (важный урок)
- [warning] **НЕ параллелить через worktree.** Пробовал — провал: Serena пишет в АКТИВИРОВАННЫЙ проект (=main), а `.venv` импортит `helm` из main `src/` → worktree-изоляция не работает, воркеры перекрёстно загрязняют файлы друг друга. Надёжно — **последовательно прямо на main** (так прошли Ф0/Ф1/Ф2), правки через **Edit/Write** (не Serena-символьные), в конце `git status` не пустой.
- Дизайн Ф3/Ф4 переиспользовать из worktree-референсов (читать файлы, применять чисто на main).
- Task list сессии: #14 Ф0 ✓, #15 Ф1 ✓, #16 Ф2 ✓, #17 Ф3 todo, #18 Ф4 todo.

## Отношения
- relates_to [[Граф Signal→Initiative vs реляционные витрины — read-path на SQL]]
- relates_to [[Helm — Карта срезов (7 вью дашборда)]]
