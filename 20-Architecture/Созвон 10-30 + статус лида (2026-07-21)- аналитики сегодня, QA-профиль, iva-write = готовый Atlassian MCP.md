---
title: 'Созвон 10-30 + статус лида (2026-07-21): аналитики сегодня, QA-профиль, iva-write
  = готовый Atlassian MCP'
type: report
permalink: tacticum/20-architecture/sozvon-10-30-status-lida-2026-07-21-analitiki-segodnia-qa-profil-iva-write-gotovyi-atlassian-mcp
status: current
created: 2026-07-21 12:30
updated: 2026-07-21 12:30
project: helm / ИИ-контур ИВА
source: helm/data/Созвон 10-30.rtf + iva-qa.rar
tags:
- call
- direction
- analyst-training
- qa-profile
- iva-write
- my-todo
- helm
- tacticum-dev
---

# Созвон 10-30 + статус лида (2026-07-21)

## Статус лида (P1 — в изоляции, НЕ в main)
- `tacticum-dev` worktree `feat/iva-write-base`: **2A iva-write-base (скелет лейна) + 2B три роли-пресета** (iva-role-architect / iva-role-qa / tacticum-role-techwriter) + обновлён тест композиции. 13 файлов, 664 строки. НЕ мержено.
- `helm` ветка `feat/my-todo` (в origin): **2C my_todo**. НЕ мержено.
- Guardrail «не трогать аналитиков» соблюдён — правки только в новых файлах/лейнах.

## Ключевые решения созвона 10-30
1. **Аналитикам СЕГОДНЯ дают ТЕКУЩИЙ протестированный `iva-role-analyst`** (тот, что мы валидируем), НЕ модульную сборку лида. На модульную переведут позже. → наша валидация напрямую обслуживает сегодняшний rollout.
2. **Аналитики = узкое место №1.** Максимум change-management сегодня. Тарасова (УВАН/ИВА-1, есть кодекс-учётки) и Лавров (MCU, у команды НЕТ учёток/инструментов вообще). Встреча: аналитики отдельно сегодня; общая регулярка на среду (Солонко ставит календарь).
3. **Что реально нужно аналитикам (со слов Тарасовой):** BRD всегда · **описание API — КЛЮЧЕВОЕ** (для разработчиков) · use cases · **sequence diagram (SD) — просят, а мы НЕ делаем → добавить/договориться** · тест-кейсы (кто генерит — аналитик или QA/Брейкин — TBD). **CJM НЕ делают → можно убрать** из скоупа аналитика.
4. **iva-write (2A) — упрощение:** на MVP НЕ строить свой, взять **готовый open-source Atlassian MCP + личные PAT** (= `iva-mcp`/iva-atlassian, уже есть; пользователю выдали токен). Позже — свой с централизованным выпуском токенов (gateway резолвит PHK identity → инжектит актора). Дима против «строить свой» ради имени продукта на MVP.
5. **Хранилище артефактов:** пока Jira + Wiki (там живут), не строить новое. Целевка — привязки/ссылки из Helm (feature-driven).
6. **Feature-driven:** ~1500 фич в Helm, ~20 продуктовых областей (звонки/мессенджер/диск/таски), кросс-продуктовые, у каждой TPO балансирует приоритет. my_todo/management-MCP = целевка (что критично, что на мне, что согласовать synchronously) — подтверждает наши 2C+2A.
7. **iva-read обновлён:** миграция Evo→Jira (почта перешла), новые проекты заиндексированы.

## Два активных трека (задача пользователю от Diaret)
### Трек A — обучение аналитиков (P0 сегодня)
Понять пайплайн (мы его открываем тестом), довести профиль, **написать инструкцию**. Наш валидационный прогон = черновик пайплайна: `/start-feature` (FR + Q-гейт) → per-artifact (BRD/ADR/PIN/TC) → публикация в Jira/Wiki личным PAT через iva-mcp. Учесть: добавить SD, убрать CJM, вернуть потерянные правки профиля (у Diaret). Storage — Jira+Wiki.
### Трек B — QA-профиль (параллельно)
QA-скиллы (`iva-qa.rar`) = **автотест-автоматизация** (Playwright/TestOps/Allure): write-autotest, batch-autotest, playwright-cli, fix-failed-test, run-tests, jira-issue-autotest, prepare-mr-branch, rebuild-autocore, retro. Лид собрал `iva-role-qa` из существующего (плейсхолдер) → **обогатить реальными QA-скиллами**. Развилка «кто генерит тест-кейсы» (аналитик vs QA) — TBD.

## Валидация профиля аналитика (идёт)
На требовании Диск `1.5-d22bead37d3` через реальный `/start-feature`: FR ✅ (структура+достоверность, 2 находки recall — недообследовал API `/resources/*` и DESIGN-стр.131670871). Токен: получен СВОЙ (kb-verify разблокирован, Q-5 закрываем). См. [[verdikt-test-profilia-analitika-1-v-1-na-1.0-414-rabotaet-probel-tolko-v-tokene]].
